---
import { cx } from '@krds-community/recipes';
import type { ButtonProps } from './types';

export interface Props extends ButtonProps {
  label?: string;
}

const {
  label,
  variant,
  size,
  disabled,
  type = 'button',
  class: className,
  ...rest
} = Astro.props;
---

<button
  {...rest}
  type={type}
  disabled={disabled}
  class={cx('krds-btn', size, 'text', variant, className)}
>
  <slot>{label}</slot><slot name="icon" />
</button>
