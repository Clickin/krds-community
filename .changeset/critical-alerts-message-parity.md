---
"@krds-community/solid": patch
"@krds-community/vue": patch
---

CriticalAlerts now accepts `message` (falling back to `text`, then `title`/`label`) as the alert text, matching the React component's item contract.
