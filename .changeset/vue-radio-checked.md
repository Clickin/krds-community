---
"@krds-community/vue": patch
---

Add `checked` and `defaultChecked` props to `RadioButton`, `RadioChip`, and `RadioSize`, matching the other choice components. Unknown `checked` attributes are no longer forwarded onto the wrapper element.
