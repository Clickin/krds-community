id: calendar-range
status: passing
upstream:
  version: 1.1.0
  files:
    - upstream/krds-html/html/code/calendar_range.html
fixtures:
  - id: calendar-range.default
    sourceSelector: ".krds-calendar-area"
    # The official area is a zero-height relative wrapper around an absolute calendar surface.
    visualSelector: ".calendar-wrap"
    mandatory: true
    viewport: desktop
    props: {}
    states:
      - id: default
        setup:
          - action: add-class
            target: fixture
            value: active
      - id: focus-visible
        setup:
          - action: add-class
            target: fixture
            value: active
          - action: keyboard-focus
            target: fixture
contract:
  semanticElement: native-element
  accessibility: [semantic-element, accessible-name, keyboard, focus-visible]
