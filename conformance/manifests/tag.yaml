id: tag
status: passing
upstream:
  version: 1.1.0
  files:
    - upstream/krds-html/html/code/tag.html
fixtures:
  - id: tag.default
    sourceSelector: ".krds-btn-tag"
    mandatory: true
    viewport: desktop
    props: {}
    states: [default, focus-visible]
contract:
  semanticElement: native-element
  accessibility: [semantic-element, accessible-name, keyboard, focus-visible]
