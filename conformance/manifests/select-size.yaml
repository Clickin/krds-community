id: select-size
status: passing
upstream:
  version: 1.1.0
  files:
    - upstream/krds-html/html/code/select_size.html
fixtures:
  - id: select-size.default
    sourceSelector: ".krds-form-select"
    sourceAncestorSelector: ".form-group"
    mandatory: true
    viewport: desktop
    props: {}
    states: [default, focus-visible]
errata:
  - errata.select.description-relationships
contract:
  semanticElement: label-and-select
  accessibleRole: combobox
  requiredAttributes:
    label: [for]
    select: [id, title=선택]
  accessibility: [semantic-element, accessible-name, keyboard, focus-visible]
