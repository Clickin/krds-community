id: switch
status: passing
upstream:
  version: 1.1.0
  files:
    - upstream/krds-html/html/code/toggle_switch.html
    - upstream/krds-html/html/code/toggle_switch_size.html
fixtures:
  - id: switch.default.medium
    sourceSelector: ".krds-form-toggle-switch input"
    mandatory: true
    viewport: desktop
    props:
      size: medium
    states: [default, checked, disabled, focus-visible]
  - id: switch.default.large
    sourceSelector: ".krds-form-toggle-switch.large input"
    mandatory: true
    viewport: desktop
    props:
      size: large
    states: [default, checked, disabled, focus-visible]
contract:
  semanticElement: input[type=checkbox]
  accessibleRole: checkbox
  requiredAttributes: {}
  accessibility:
    [label-for-id, checked-state, disabled-semantics, visible-focus, native-form-participation]
