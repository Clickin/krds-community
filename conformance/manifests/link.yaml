id: link
status: passing
upstream:
  version: 1.1.0
  files:
    - upstream/krds-html/html/code/link.html
fixtures:
  - id: link.default
    sourceSelector: "a.krds-btn.link"
    mandatory: true
    viewport: desktop
    props: {}
    states: [default, focus-visible]
contract:
  semanticElement: link
  accessibility: [semantic-element, accessible-name, keyboard, focus-visible]
