---
type Props = {
  componentId: string;
  title?: string;
};

const { componentId, title } = Astro.props;
---

<div class="component-page" data-component={componentId}>
  {title && <h1>{title}</h1>}
  <slot />
</div>

<style>
  .component-page {
    max-width: 92rem;
  }
</style>
