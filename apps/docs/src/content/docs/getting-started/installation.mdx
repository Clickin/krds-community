---
title: 빠른 시작
description: KRDS Community 패키지를 설치하고 첫 프레임워크 네이티브 컴포넌트를 렌더링하는 순서입니다.
---

import FrameworkCodeTabs from "@docs/FrameworkCodeTabs.astro";

각 프레임워크 패키지는 해당 프레임워크의 네이티브 컴포넌트를 제공합니다. 공통 계약은 `@krds-community/recipes`, canonical token 값은 `@krds-community/tokens`에서 생성되며, 기초 스타일은 선택적으로 `@krds-community/styles`에서 가져옵니다.

```sh
pnpm add @krds-community/<framework> @krds-community/styles
```

`<framework>`를 `react`, `vue`, `svelte`, `solid`, `angular`, `astro` 중 선택합니다. 각 프레임워크 패키지 이름은 같고 `@krds-community/` 접두사 뒤에 프레임워크 이름만 바뀝니다.

이미 Astro 프로젝트를 사용 중이라면 `astro` 패키지가 이미 설치되어 있으므로 `@krds-community/astro`와 `@krds-community/styles`만 추가하면 됩니다.

| 프레임워크 | 패키지                    |
| ---------- | ------------------------- |
| React      | `@krds-community/react`   |
| Vue        | `@krds-community/vue`     |
| Svelte     | `@krds-community/svelte`  |
| SolidJS    | `@krds-community/solid`   |
| Angular    | `@krds-community/angular` |
| Astro      | `@krds-community/astro`   |

설치 후 public export를 직접 import하는 최소 사용 예시는 다음과 같습니다. 각 컴포넌트의 props/events/forms/a11y 안내는 [컴포넌트 문서](../../components/)에서 확인하세요.

<FrameworkCodeTabs
  title="설치 예제"
  examples={[
    {
      framework: "react",
      label: "React",
      language: "tsx",
      code: `import { Button } from '@krds-community/react';\n\nexport function SaveButton() {\n  return <Button type="submit">저장</Button>;\n}`,
    },
    {
      framework: "vue",
      label: "Vue",
      language: "vue",
      code: `<script setup lang="ts">\nimport { Button } from '@krds-community/vue';\n<\/script>\n\n<template>\n  <Button type="submit">저장</Button>\n</template>`,
    },
    {
      framework: "svelte",
      label: "Svelte",
      language: "svelte",
      code: `<script lang="ts">\n  import { Button } from '@krds-community/svelte';\n<\/script>\n\n<Button type="submit">저장</Button>`,
    },
    {
      framework: "solid",
      label: "SolidJS",
      language: "tsx",
      code: `import { Button } from '@krds-community/solid';\n\nexport function SaveButton() {\n  return <Button type="submit">저장</Button>;\n}`,
    },
    {
      framework: "angular",
      label: "Angular",
      language: "ts",
      code: `import { KrdsButtonComponent } from '@krds-community/angular';\n\n@Component({\n  standalone: true,\n  imports: [KrdsButtonComponent],\n  template: '<krds-button type="submit">저장</krds-button>',\n})\nexport class SaveButtonComponent {}`,
    },
    {
      framework: "astro",
      label: "Astro",
      language: "astro",
      code: `---\nimport { Button } from '@krds-community/astro';\n---\n\n<Button type="submit">저장</Button>`,
    },
  ]}
/>

## CLI로 컴포넌트 복사하기

[KRDS CLI](../cli/)를 사용하면 개별 컴포넌트의 소스 코드를 프로젝트에 직접 복사할 수 있습니다. 설치·복사 명령과 의존성 안내는 [KRDS CLI 문서](../cli/)를 참고하세요.
