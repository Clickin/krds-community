---
title: 로그인
description: 사용자의 신원을 확인하여 서비스에 접근하도록 하는 서비스 패턴입니다.
---

import FrameworkPreview from "@docs/FrameworkPreview.astro";
import PatternReference from "../../../components/PatternReference.astro";
import { Button as ReactButton } from "@krds-community/react";
import { Button as VueButton } from "@krds-community/vue";
import { Button as SvelteButton } from "@krds-community/svelte";
import { Button as SolidButton } from "@krds-community/solid";
import { AngularButton } from "@docs/angular-previews.ts";
import { Button as AstroButton } from "@krds-community/astro";
import { TextInput as ReactTextInput } from "@krds-community/react";
import { TextInput as VueTextInput } from "@krds-community/vue";
import { TextInput as SvelteTextInput } from "@krds-community/svelte";
import { TextInput as SolidTextInput } from "@krds-community/solid";
import { AngularTextInput } from "@docs/angular-previews.ts";
import { TextInput as AstroTextInput } from "@krds-community/astro";
import { ValidatedInput as ReactValidatedInput } from "@krds-community/react/extra";
import { ValidatedInput as VueValidatedInput } from "@krds-community/vue/extra";
import { ValidatedInput as SvelteValidatedInput } from "@krds-community/svelte/extra";
import { ValidatedInput as SolidValidatedInput } from "@krds-community/solid/extra";
import { AngularValidatedInput } from "@docs/angular-previews.ts";
import { ValidatedInput as AstroValidatedInput } from "@krds-community/astro/extra";
import { Modal as ReactModal } from "@krds-community/react";
import { Modal as VueModal } from "@krds-community/vue";
import { Modal as SvelteModal } from "@krds-community/svelte";
import { Modal as SolidModal } from "@krds-community/solid";
import { AngularModal } from "@docs/angular-previews.ts";
import { Modal as AstroModal } from "@krds-community/astro";
import { Header as ReactHeader } from "@krds-community/react";
import { Header as VueHeader } from "@krds-community/vue";
import { Header as SvelteHeader } from "@krds-community/svelte";
import { Header as SolidHeader } from "@krds-community/solid";
import { AngularHeader } from "@docs/angular-previews.ts";
import { Header as AstroHeader } from "@krds-community/astro";
import { Tab as ReactTab } from "@krds-community/react";
import { Tab as VueTab } from "@krds-community/vue";
import { Tab as SvelteTab } from "@krds-community/svelte";
import { Tab as SolidTab } from "@krds-community/solid";
import { AngularTab } from "@docs/angular-previews.ts";
import { Tab as AstroTab } from "@krds-community/astro";

<PatternReference patternId="login" />

## 공식 문서

로그인은 사용자의 신원을 확인하는 과정으로 사용자가 서비스에 접근할 수 있도록 하는 수단이다. 사용자에게 개인화된 경험을 제공하거나 사용자의 신분/신원을 인증하고자 하는 경우에 사용하기 적합하다.

### 유형

**지식 기반 인증** 사용자만이 유일하게 알고 있는 내용에 기반한 사용자 확인 방법 예) 아이디/패스워드, PIN

**소유 기반 인증** 사용자가 보유하고 있는 품목을 활용한 인증 방법 예) 토큰, 공동인증, 간편인증, 문자 인증

**생체 기반 인증** 사용자의 생체 정보, 행동에 기반한 인증 방법 예) 지문 인증, 페이스 아이디

**다중 요소 인증** 여러 가지 인증 방식을 결합하여 제공하는 인증 방법

## 사용성 가이드라인

### 로그인 기능 찾기

**필수: 로그인 링크는 모든 화면에서 일관된 위치에 주목도 있게 배치한다.**
로그인으로 이동할 수 있는 링크가 메인 화면 또는 일부 화면에만 제공될 경우, 로그인이 필요한 사용자는 특정 화면으로 이동하는 경우에만 기능을 이용할 수 있어 이용에 어려움을 겪게 된다. 주요 정보 및 서비스를 확인하는 데 로그인이 필요한 경우, 서비스 첫 화면에 로그인을 유도하는 문구와 링크를 강조하여 표시함으로써 로그인을 유도할 수 있다.

**권장: 헤더에서 제공되는 로그인 링크는 사용자 아이콘과 레이블로 구성한다.**
아이콘만 단독으로 사용되는 경우 사용자에 따라 링크의 목적을 빠르게 이해하기 어려울 수 있으므로 텍스트 레이블을 함께 제공하는 것이 좋다.

**모범 사례**

<FrameworkPreview
  kind="good"
  title="헤더에서 제공되는 로그인 링크는 사용자 아이콘과 레이블로 구성한다."
>
  <div slot="react">
    <ReactHeader
      logoLabel="서비스명"
      loginLabel="로그인"
      loginHref="#login"
      aria-label="서비스 헤더"
      client:only="react"
    />
  </div>
  <div slot="vue">
    <VueHeader
      logoLabel="서비스명"
      loginLabel="로그인"
      loginHref="#login"
      aria-label="서비스 헤더"
      client:only="vue"
    />
  </div>
  <div slot="svelte">
    <SvelteHeader
      logoLabel="서비스명"
      loginLabel="로그인"
      loginHref="#login"
      aria-label="서비스 헤더"
      client:only="svelte"
    />
  </div>
  <div slot="solid">
    <SolidHeader
      logoLabel="서비스명"
      loginLabel="로그인"
      loginHref="#login"
      aria-label="서비스 헤더"
      client:only="solid-js"
    />
  </div>
  <div slot="astro">
    <AstroHeader
      logoLabel="서비스명"
      loginLabel="로그인"
      loginHref="#login"
      aria-label="서비스 헤더"
    />
  </div>
  <div slot="angular">
    <AngularHeader
      logoLabel="서비스명"
      loginLabel="로그인"
      loginHref="#login"
      aria-label="서비스 헤더"
      client:only="@analogjs/astro-angular"
    />
  </div>

```tsx fw=react
import { Header } from "@krds-community/react";

export function Example() {
  return (
    <Header logoLabel="서비스명" loginLabel="로그인" loginHref="#login" aria-label="서비스 헤더" />
  );
}
```

```vue fw=vue
<script setup>
import { Header } from "@krds-community/vue";
</script>

<template>
  <Header logoLabel="서비스명" loginLabel="로그인" loginHref="#login" aria-label="서비스 헤더" />
</template>
```

```svelte fw=svelte
<script>
import { Header } from '@krds-community/svelte';
</script>

<Header
  logoLabel="서비스명"
  loginLabel="로그인"
  loginHref="#login"
  aria-label="서비스 헤더"
/>
```

```tsx fw=solid
import { Header } from "@krds-community/solid";

export function Example() {
  return (
    <Header logoLabel="서비스명" loginLabel="로그인" loginHref="#login" aria-label="서비스 헤더" />
  );
}
```

```ts fw=angular
import { KrdsHeaderComponent } from '@krds-community/angular';

// template
<krds-header
  logoLabel="서비스명"
  loginLabel="로그인"
  loginHref="#login"
  aria-label="서비스 헤더"
></krds-header>
```

```astro fw=astro
---
import { Header } from "@krds-community/astro";
---

<Header
  logoLabel="서비스명"
  loginLabel="로그인"
  loginHref="#login"
  aria-label="서비스 헤더"
/>
```

</FrameworkPreview>

**피해야 할 사례**

<FrameworkPreview
  kind="bad"
  title="헤더에서 제공되는 로그인 링크는 사용자 아이콘과 레이블로 구성한다."
>
  <div slot="react">
    <header
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "space-between",
        padding: "16px 24px",
        borderBottom: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
      }}
    >
      <strong>서비스명</strong>
      <a href="#login" aria-label="로그인" style={{ display: "inline-flex" }}>
        <svg viewBox="0 0 24 24" width="24" height="24" fill="none" stroke="currentColor" aria-hidden="true">
          <circle cx="12" cy="8" r="4"></circle>
          <path d="M4 20c0-4 4-6 8-6s8 2 8 6"></path>
        </svg>
      </a>
    </header>
  </div>
  <div slot="vue">
    <header
      style="display:flex;align-items:center;justify-content:space-between;padding:16px 24px;border-bottom:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1)"
    >
      <strong>서비스명</strong>
      <a href="#login" aria-label="로그인" style="display:inline-flex">
        <svg viewBox="0 0 24 24" width="24" height="24" fill="none" stroke="currentColor" aria-hidden="true">
          <circle cx="12" cy="8" r="4"></circle>
          <path d="M4 20c0-4 4-6 8-6s8 2 8 6"></path>
        </svg>
      </a>
    </header>
  </div>
  <div slot="svelte">
    <header
      style="display:flex;align-items:center;justify-content:space-between;padding:16px 24px;border-bottom:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1)"
    >
      <strong>서비스명</strong>
      <a href="#login" aria-label="로그인" style="display:inline-flex">
        <svg viewBox="0 0 24 24" width="24" height="24" fill="none" stroke="currentColor" aria-hidden="true">
          <circle cx="12" cy="8" r="4"></circle>
          <path d="M4 20c0-4 4-6 8-6s8 2 8 6"></path>
        </svg>
      </a>
    </header>
  </div>
  <div slot="solid">
    <header
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "space-between",
        padding: "16px 24px",
        borderBottom: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
      }}
    >
      <strong>서비스명</strong>
      <a href="#login" aria-label="로그인" style={{ display: "inline-flex" }}>
        <svg viewBox="0 0 24 24" width="24" height="24" fill="none" stroke="currentColor" aria-hidden="true">
          <circle cx="12" cy="8" r="4"></circle>
          <path d="M4 20c0-4 4-6 8-6s8 2 8 6"></path>
        </svg>
      </a>
    </header>
  </div>
  <div slot="astro">
    <header
      style="display:flex;align-items:center;justify-content:space-between;padding:16px 24px;border-bottom:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1)"
    >
      <strong>서비스명</strong>
      <a href="#login" aria-label="로그인" style="display:inline-flex">
        <svg viewBox="0 0 24 24" width="24" height="24" fill="none" stroke="currentColor" aria-hidden="true">
          <circle cx="12" cy="8" r="4"></circle>
          <path d="M4 20c0-4 4-6 8-6s8 2 8 6"></path>
        </svg>
      </a>
    </header>
  </div>
  <div slot="angular">
    <header
      style="display:flex;align-items:center;justify-content:space-between;padding:16px 24px;border-bottom:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1)"
    >
      <strong>서비스명</strong>
      <a href="#login" aria-label="로그인" style="display:inline-flex">
        <svg viewBox="0 0 24 24" width="24" height="24" fill="none" stroke="currentColor" aria-hidden="true">
          <circle cx="12" cy="8" r="4"></circle>
          <path d="M4 20c0-4 4-6 8-6s8 2 8 6"></path>
        </svg>
      </a>
    </header>
  </div>

```tsx fw=react
export function Example() {
  return (
    <header
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "space-between",
        padding: "16px 24px",
        borderBottom: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
      }}
    >
      <strong>서비스명</strong>
      <a href="#login" aria-label="로그인" style={{ display: "inline-flex" }}>
        <svg
          viewBox="0 0 24 24"
          width="24"
          height="24"
          fill="none"
          stroke="currentColor"
          aria-hidden="true"
        >
          <circle cx="12" cy="8" r="4"></circle>
          <path d="M4 20c0-4 4-6 8-6s8 2 8 6"></path>
        </svg>
      </a>
    </header>
  );
}
```

```vue fw=vue
<template>
  <header
    style="display:flex;align-items:center;justify-content:space-between;padding:16px 24px;border-bottom:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1)"
  >
    <strong>서비스명</strong>
    <a href="#login" aria-label="로그인" style="display:inline-flex">
      <svg
        viewBox="0 0 24 24"
        width="24"
        height="24"
        fill="none"
        stroke="currentColor"
        aria-hidden="true"
      >
        <circle cx="12" cy="8" r="4"></circle>
        <path d="M4 20c0-4 4-6 8-6s8 2 8 6"></path>
      </svg>
    </a>
  </header>
</template>
```

```svelte fw=svelte
<header
  style="display:flex;align-items:center;justify-content:space-between;padding:16px 24px;border-bottom:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1)"
>
  <strong>서비스명</strong>
  <a href="#login" aria-label="로그인" style="display:inline-flex">
    <svg viewBox="0 0 24 24" width="24" height="24" fill="none" stroke="currentColor" aria-hidden="true">
      <circle cx="12" cy="8" r="4"></circle>
      <path d="M4 20c0-4 4-6 8-6s8 2 8 6"></path>
    </svg>
  </a>
</header>
```

```tsx fw=solid
export function Example() {
  return (
    <header
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "space-between",
        padding: "16px 24px",
        borderBottom: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
      }}
    >
      <strong>서비스명</strong>
      <a href="#login" aria-label="로그인" style={{ display: "inline-flex" }}>
        <svg
          viewBox="0 0 24 24"
          width="24"
          height="24"
          fill="none"
          stroke="currentColor"
          aria-hidden="true"
        >
          <circle cx="12" cy="8" r="4"></circle>
          <path d="M4 20c0-4 4-6 8-6s8 2 8 6"></path>
        </svg>
      </a>
    </header>
  );
}
```

```ts fw=angular
// template
<header
  style="display:flex;align-items:center;justify-content:space-between;padding:16px 24px;border-bottom:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1)"
>
  <strong>서비스명</strong>
  <a href="#login" aria-label="로그인" style="display:inline-flex">
    <svg viewBox="0 0 24 24" width="24" height="24" fill="none" stroke="currentColor" aria-hidden="true">
      <circle cx="12" cy="8" r="4"></circle>
      <path d="M4 20c0-4 4-6 8-6s8 2 8 6"></path>
    </svg>
  </a>
</header>
```

```astro fw=astro
<header
  style="display:flex;align-items:center;justify-content:space-between;padding:16px 24px;border-bottom:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1)"
>
  <strong>서비스명</strong>
  <a href="#login" aria-label="로그인" style="display:inline-flex">
    <svg viewBox="0 0 24 24" width="24" height="24" fill="none" stroke="currentColor" aria-hidden="true">
      <circle cx="12" cy="8" r="4"></circle>
      <path d="M4 20c0-4 4-6 8-6s8 2 8 6"></path>
    </svg>
  </a>
</header>
```

</FrameworkPreview>

**필수: 로그인 링크는 항상 ‘로그인’ 화면으로 연결되어야 한다.**
로그인 링크의 목적지는 로그인 프로세스를 시작하는 화면 또는 로그인할 수 있는 다양한 계정이 있는 계정 선택 화면으로 설정해야 한다.

**화면 너비가 충분하지 않은 경우에도 로그인 링크를 직관적으로 인지할 수 있는 형태로 표현한다.**
작은 화면 너비에서도 가능한 한 로그인 링크를 숨기지 않고 헤더나 탭바 영역에 로그인/마이페이지 링크를 상시 배치한다. 만약 서비스 이용에 로그인이 중요하지 않다면 메뉴 레이어에서만 로그인 링크를 제공할 수 있다.

**로그인 버튼은 사용자가 잘 볼 수 있도록 상시 노출한다.**
서비스 중심의 모바일 앱에서는 탭바 하단에 상시 노출하여 표시한다. 웹과 모바일을 일원화하여 사용할 경우 상단에 배치할 수 있다.

**모범 사례**

<FrameworkPreview
  kind="good"
  title="로그인 버튼은 사용자가 잘 볼 수 있도록 상시 노출한다."
>
  <div slot="react">
    <div
      style={{
        maxWidth: "360px",
        margin: "0 auto",
        border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        borderRadius: "8px",
        overflow: "hidden",
      }}
    >
      <div
        style={{
          minHeight: "140px",
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
          padding: "24px",
          color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)",
        }}
      >
        콘텐츠 영역
      </div>
      <div
        style={{
          display: "flex",
          padding: "12px 16px",
          borderTop: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        }}
      >
        <div style={{ flex: 1 }}>
          <ReactButton variant="primary" client:only="react">
            로그인
          </ReactButton>
        </div>
      </div>
    </div>
  </div>
  <div slot="vue">
    <div
      style="max-width:360px;margin:0 auto;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px;overflow:hidden"
    >
      <div
        style="min-height:140px;display:flex;align-items:center;justify-content:center;padding:24px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)"
      >
        콘텐츠 영역
      </div>
      <div
        style="display:flex;padding:12px 16px;border-top:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1)"
      >
        <div style="flex:1">
          <VueButton variant="primary" client:only="vue">
            로그인
          </VueButton>
        </div>
      </div>
    </div>
  </div>
  <div slot="svelte">
    <div
      style="max-width:360px;margin:0 auto;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px;overflow:hidden"
    >
      <div
        style="min-height:140px;display:flex;align-items:center;justify-content:center;padding:24px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)"
      >
        콘텐츠 영역
      </div>
      <div
        style="display:flex;padding:12px 16px;border-top:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1)"
      >
        <div style="flex:1">
          <SvelteButton variant="primary" client:only="svelte">
            로그인
          </SvelteButton>
        </div>
      </div>
    </div>
  </div>
  <div slot="solid">
    <div
      style={{
        maxWidth: "360px",
        margin: "0 auto",
        border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        borderRadius: "8px",
        overflow: "hidden",
      }}
    >
      <div
        style={{
          minHeight: "140px",
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
          padding: "24px",
          color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)",
        }}
      >
        콘텐츠 영역
      </div>
      <div
        style={{
          display: "flex",
          padding: "12px 16px",
          borderTop: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        }}
      >
        <div style={{ flex: 1 }}>
          <SolidButton variant="primary" client:only="solid-js">
            로그인
          </SolidButton>
        </div>
      </div>
    </div>
  </div>
  <div slot="astro">
    <div
      style="max-width:360px;margin:0 auto;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px;overflow:hidden"
    >
      <div
        style="min-height:140px;display:flex;align-items:center;justify-content:center;padding:24px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)"
      >
        콘텐츠 영역
      </div>
      <div
        style="display:flex;padding:12px 16px;border-top:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1)"
      >
        <div style="flex:1">
          <AstroButton variant="primary">로그인</AstroButton>
        </div>
      </div>
    </div>
  </div>
  <div slot="angular">
    <div
      style="max-width:360px;margin:0 auto;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px;overflow:hidden"
    >
      <div
        style="min-height:140px;display:flex;align-items:center;justify-content:center;padding:24px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)"
      >
        콘텐츠 영역
      </div>
      <div
        style="display:flex;padding:12px 16px;border-top:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1)"
      >
        <div style="flex:1">
          <AngularButton
            variant="primary"
            label="로그인"
            client:only="@analogjs/astro-angular"
          >
            로그인
          </AngularButton>
        </div>
      </div>
    </div>
  </div>

```tsx fw=react
import { Button } from "@krds-community/react";

export function Example() {
  return (
    <div
      style={{
        maxWidth: "360px",
        margin: "0 auto",
        border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        borderRadius: "8px",
        overflow: "hidden",
      }}
    >
      <div
        style={{
          minHeight: "140px",
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
          padding: "24px",
          color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)",
        }}
      >
        콘텐츠 영역
      </div>
      <div
        style={{
          display: "flex",
          padding: "12px 16px",
          borderTop: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        }}
      >
        <div style={{ flex: 1 }}>
          <Button variant="primary">로그인</Button>
        </div>
      </div>
    </div>
  );
}
```

```vue fw=vue
<script setup>
import { Button } from "@krds-community/vue";
</script>

<template>
  <div
    style="max-width:360px;margin:0 auto;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px;overflow:hidden"
  >
    <div
      style="min-height:140px;display:flex;align-items:center;justify-content:center;padding:24px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)"
    >
      콘텐츠 영역
    </div>
    <div
      style="display:flex;padding:12px 16px;border-top:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1)"
    >
      <div style="flex:1">
        <Button variant="primary">로그인</Button>
      </div>
    </div>
  </div>
</template>
```

```svelte fw=svelte
<script>
import { Button } from '@krds-community/svelte';
</script>

<div
  style="max-width:360px;margin:0 auto;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px;overflow:hidden"
>
  <div
    style="min-height:140px;display:flex;align-items:center;justify-content:center;padding:24px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)"
  >
    콘텐츠 영역
  </div>
  <div
    style="display:flex;padding:12px 16px;border-top:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1)"
  >
    <div style="flex:1">
      <Button variant="primary">로그인</Button>
    </div>
  </div>
</div>
```

```tsx fw=solid
import { Button } from "@krds-community/solid";

export function Example() {
  return (
    <div
      style={{
        maxWidth: "360px",
        margin: "0 auto",
        border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        borderRadius: "8px",
        overflow: "hidden",
      }}
    >
      <div
        style={{
          minHeight: "140px",
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
          padding: "24px",
          color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)",
        }}
      >
        콘텐츠 영역
      </div>
      <div
        style={{
          display: "flex",
          padding: "12px 16px",
          borderTop: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        }}
      >
        <div style={{ flex: 1 }}>
          <Button variant="primary">로그인</Button>
        </div>
      </div>
    </div>
  );
}
```

```ts fw=angular
import { KrdsButtonComponent } from '@krds-community/angular';

// template
<div
  style="max-width:360px;margin:0 auto;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px;overflow:hidden"
>
  <div
    style="min-height:140px;display:flex;align-items:center;justify-content:center;padding:24px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)"
  >
    콘텐츠 영역
  </div>
  <div
    style="display:flex;padding:12px 16px;border-top:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1)"
  >
    <div style="flex:1">
      <krds-button variant="primary" label="로그인">로그인</krds-button>
    </div>
  </div>
</div>
```

```astro fw=astro
---
import { Button } from "@krds-community/astro";
---

<div
  style="max-width:360px;margin:0 auto;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px;overflow:hidden"
>
  <div
    style="min-height:140px;display:flex;align-items:center;justify-content:center;padding:24px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)"
  >
    콘텐츠 영역
  </div>
  <div
    style="display:flex;padding:12px 16px;border-top:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1)"
  >
    <div style="flex:1">
      <Button variant="primary">로그인</Button>
    </div>
  </div>
</div>
```

</FrameworkPreview>

**로그인 방식을 우선으로 화면에 배치한다.**
작은 화면에 특성 상 사용자가 로그인 방식을 우선적으로 확인 할 수 있도록 화면에 배치한다.

**생채인식을 사용 할 경우 자동로그인 기능을 추가한다.**
모바일 앱에 특성 상 사용자의 생채인식을 사용할 경우 자동로그인 기능을 사용할 수 있도록 한다. 로그인 화면 하단 또는 성공 직후 안내 모달에서 로그인 유지에 대한 내용을 확인할 수 있도록 한다.

**로그인 링크는 스크린 리더에서 링크로 인지될 수 있도록 한다.**
로그인 링크를 버튼 요소로 마크업하게 되면 로그인 화면으로 이동하는 동작이 발생함을 예측할 수 없다.

### 로그인 안내

로그인 안내는 다음 상황에서 사용한다.
**기본** 로그인이 필수적인 화면/기능에 접근을 시도하는 경우에 사용한다.
**추천** 로그인이 필수적인 화면/기능에 접근을 시도하는 상황에서 로그인 방식에 따라 이후 서비스 이용 과정이 달라지는 경우에 사용한다.
**제안** 로그인이 필수적인 화면/기능은 아니지만 로그인을 했을 때의 이점이 존재하여 로그인할 것을 제안하는 경우에 사용한다.

**권장: 로그인에 대한 안내는 모달로 제공하여 사용자의 과업 맥락이 유지될 수 있도록 한다.**
로그인하지 않는 옵션을 선택하여 탐색 중인 화면에 머무르고자 하는 사용자, 로그인 없이 서비스를 이용하고자 하는 사용자가 현재의 이용 맥락이 유지됨을 인지할 수 있도록 모달 레이아웃을 이용하여 안내를 제공한다.

**모범 사례**

<FrameworkPreview
  kind="good"
  title="로그인에 대한 안내는 모달로 제공하여 사용자의 과업 맥락이 유지될 수 있도록 한다."
>
  <div slot="react">
    <ReactModal
      open="true"
      title="로그인이 필요합니다"
      description="나의 신청 내역을 확인하려면 로그인해 주세요. 로그인하면 현재 화면으로 돌아와 작업을 이어갈 수 있습니다."
      cancelLabel="로그인 안 함"
      confirmLabel="로그인하기"
      client:only="react"
    />
  </div>
  <div slot="vue">
    <VueModal
      open="true"
      title="로그인이 필요합니다"
      description="나의 신청 내역을 확인하려면 로그인해 주세요. 로그인하면 현재 화면으로 돌아와 작업을 이어갈 수 있습니다."
      cancelLabel="로그인 안 함"
      confirmLabel="로그인하기"
      client:only="vue"
    />
  </div>
  <div slot="svelte">
    <SvelteModal
      open="true"
      title="로그인이 필요합니다"
      description="나의 신청 내역을 확인하려면 로그인해 주세요. 로그인하면 현재 화면으로 돌아와 작업을 이어갈 수 있습니다."
      cancelLabel="로그인 안 함"
      confirmLabel="로그인하기"
      client:only="svelte"
    />
  </div>
  <div slot="solid">
    <SolidModal
      open="true"
      title="로그인이 필요합니다"
      description="나의 신청 내역을 확인하려면 로그인해 주세요. 로그인하면 현재 화면으로 돌아와 작업을 이어갈 수 있습니다."
      cancelLabel="로그인 안 함"
      confirmLabel="로그인하기"
      client:only="solid-js"
    />
  </div>
  <div slot="astro">
    <AstroModal
      open="true"
      title="로그인이 필요합니다"
      description="나의 신청 내역을 확인하려면 로그인해 주세요. 로그인하면 현재 화면으로 돌아와 작업을 이어갈 수 있습니다."
      cancelLabel="로그인 안 함"
      confirmLabel="로그인하기"
    />
  </div>
  <div slot="angular">
    <AngularModal
      open="true"
      title="로그인이 필요합니다"
      description="나의 신청 내역을 확인하려면 로그인해 주세요. 로그인하면 현재 화면으로 돌아와 작업을 이어갈 수 있습니다."
      cancelLabel="로그인 안 함"
      confirmLabel="로그인하기"
      client:only="@analogjs/astro-angular"
    />
  </div>

```tsx fw=react
import { Modal } from "@krds-community/react";

export function Example() {
  return (
    <Modal
      open="true"
      title="로그인이 필요합니다"
      description="나의 신청 내역을 확인하려면 로그인해 주세요. 로그인하면 현재 화면으로 돌아와 작업을 이어갈 수 있습니다."
      cancelLabel="로그인 안 함"
      confirmLabel="로그인하기"
    />
  );
}
```

```vue fw=vue
<script setup>
import { Modal } from "@krds-community/vue";
</script>

<template>
  <Modal
    open="true"
    title="로그인이 필요합니다"
    description="나의 신청 내역을 확인하려면 로그인해 주세요. 로그인하면 현재 화면으로 돌아와 작업을 이어갈 수 있습니다."
    cancelLabel="로그인 안 함"
    confirmLabel="로그인하기"
  />
</template>
```

```svelte fw=svelte
<script>
import { Modal } from '@krds-community/svelte';
</script>

<Modal
  open="true"
  title="로그인이 필요합니다"
  description="나의 신청 내역을 확인하려면 로그인해 주세요. 로그인하면 현재 화면으로 돌아와 작업을 이어갈 수 있습니다."
  cancelLabel="로그인 안 함"
  confirmLabel="로그인하기"
/>
```

```tsx fw=solid
import { Modal } from "@krds-community/solid";

export function Example() {
  return (
    <Modal
      open="true"
      title="로그인이 필요합니다"
      description="나의 신청 내역을 확인하려면 로그인해 주세요. 로그인하면 현재 화면으로 돌아와 작업을 이어갈 수 있습니다."
      cancelLabel="로그인 안 함"
      confirmLabel="로그인하기"
    />
  );
}
```

```ts fw=angular
import { KrdsModalComponent } from '@krds-community/angular';

// template
<krds-modal
  open="true"
  title="로그인이 필요합니다"
  description="나의 신청 내역을 확인하려면 로그인해 주세요. 로그인하면 현재 화면으로 돌아와 작업을 이어갈 수 있습니다."
  cancelLabel="로그인 안 함"
  confirmLabel="로그인하기"
></krds-modal>
```

```astro fw=astro
---
import { Modal } from "@krds-community/astro";
---

<Modal
  open="true"
  title="로그인이 필요합니다"
  description="나의 신청 내역을 확인하려면 로그인해 주세요. 로그인하면 현재 화면으로 돌아와 작업을 이어갈 수 있습니다."
  cancelLabel="로그인 안 함"
  confirmLabel="로그인하기"
/>
```

</FrameworkPreview>

**피해야 할 사례**

<FrameworkPreview
  kind="bad"
  title="로그인에 대한 안내는 모달로 제공하여 사용자의 과업 맥락이 유지될 수 있도록 한다."
>
  <div slot="react">
    <section
      style={{
        maxWidth: "480px",
        margin: "0 auto",
        padding: "48px 24px",
        textAlign: "center",
      }}
    >
      <h3 style={{ margin: "0 0 8px" }}>로그인이 필요합니다</h3>
      <p style={{ margin: "0 0 24px", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
        로그인 후 서비스를 이용해 주세요. 로그인 화면으로 이동합니다.
      </p>
      <div style={{ display: "flex", justifyContent: "center", gap: "8px" }}>
        <ReactButton variant="primary" client:only="react">
          로그인하기
        </ReactButton>
      </div>
    </section>
  </div>
  <div slot="vue">
    <section
      style="max-width:480px;margin:0 auto;padding:48px 24px;text-align:center"
    >
      <h3 style="margin:0 0 8px">로그인이 필요합니다</h3>
      <p style="margin:0 0 24px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
        로그인 후 서비스를 이용해 주세요. 로그인 화면으로 이동합니다.
      </p>
      <div style="display:flex;justify-content:center;gap:8px">
        <VueButton variant="primary" client:only="vue">
          로그인하기
        </VueButton>
      </div>
    </section>
  </div>
  <div slot="svelte">
    <section
      style="max-width:480px;margin:0 auto;padding:48px 24px;text-align:center"
    >
      <h3 style="margin:0 0 8px">로그인이 필요합니다</h3>
      <p style="margin:0 0 24px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
        로그인 후 서비스를 이용해 주세요. 로그인 화면으로 이동합니다.
      </p>
      <div style="display:flex;justify-content:center;gap:8px">
        <SvelteButton variant="primary" client:only="svelte">
          로그인하기
        </SvelteButton>
      </div>
    </section>
  </div>
  <div slot="solid">
    <section
      style={{
        maxWidth: "480px",
        margin: "0 auto",
        padding: "48px 24px",
        textAlign: "center",
      }}
    >
      <h3 style={{ margin: "0 0 8px" }}>로그인이 필요합니다</h3>
      <p style={{ margin: "0 0 24px", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
        로그인 후 서비스를 이용해 주세요. 로그인 화면으로 이동합니다.
      </p>
      <div style={{ display: "flex", justifyContent: "center", gap: "8px" }}>
        <SolidButton variant="primary" client:only="solid-js">
          로그인하기
        </SolidButton>
      </div>
    </section>
  </div>
  <div slot="astro">
    <section
      style="max-width:480px;margin:0 auto;padding:48px 24px;text-align:center"
    >
      <h3 style="margin:0 0 8px">로그인이 필요합니다</h3>
      <p style="margin:0 0 24px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
        로그인 후 서비스를 이용해 주세요. 로그인 화면으로 이동합니다.
      </p>
      <div style="display:flex;justify-content:center;gap:8px">
        <AstroButton variant="primary">로그인하기</AstroButton>
      </div>
    </section>
  </div>
  <div slot="angular">
    <section
      style="max-width:480px;margin:0 auto;padding:48px 24px;text-align:center"
    >
      <h3 style="margin:0 0 8px">로그인이 필요합니다</h3>
      <p style="margin:0 0 24px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
        로그인 후 서비스를 이용해 주세요. 로그인 화면으로 이동합니다.
      </p>
      <div style="display:flex;justify-content:center;gap:8px">
        <AngularButton
          variant="primary"
          label="로그인하기"
          client:only="@analogjs/astro-angular"
        >
          로그인하기
        </AngularButton>
      </div>
    </section>
  </div>

```tsx fw=react
import { Button } from "@krds-community/react";

export function Example() {
  return (
    <section
      style={{
        maxWidth: "480px",
        margin: "0 auto",
        padding: "48px 24px",
        textAlign: "center",
      }}
    >
      <h3 style={{ margin: "0 0 8px" }}>로그인이 필요합니다</h3>
      <p style={{ margin: "0 0 24px", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
        로그인 후 서비스를 이용해 주세요. 로그인 화면으로 이동합니다.
      </p>
      <div style={{ display: "flex", justifyContent: "center", gap: "8px" }}>
        <Button variant="primary">로그인하기</Button>
      </div>
    </section>
  );
}
```

```vue fw=vue
<script setup>
import { Button } from "@krds-community/vue";
</script>

<template>
  <section style="max-width:480px;margin:0 auto;padding:48px 24px;text-align:center">
    <h3 style="margin:0 0 8px">로그인이 필요합니다</h3>
    <p style="margin:0 0 24px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
      로그인 후 서비스를 이용해 주세요. 로그인 화면으로 이동합니다.
    </p>
    <div style="display:flex;justify-content:center;gap:8px">
      <Button variant="primary">로그인하기</Button>
    </div>
  </section>
</template>
```

```svelte fw=svelte
<script>
import { Button } from '@krds-community/svelte';
</script>

<section
  style="max-width:480px;margin:0 auto;padding:48px 24px;text-align:center"
>
  <h3 style="margin:0 0 8px">로그인이 필요합니다</h3>
  <p style="margin:0 0 24px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
    로그인 후 서비스를 이용해 주세요. 로그인 화면으로 이동합니다.
  </p>
  <div style="display:flex;justify-content:center;gap:8px">
    <Button variant="primary">로그인하기</Button>
  </div>
</section>
```

```tsx fw=solid
import { Button } from "@krds-community/solid";

export function Example() {
  return (
    <section
      style={{
        maxWidth: "480px",
        margin: "0 auto",
        padding: "48px 24px",
        textAlign: "center",
      }}
    >
      <h3 style={{ margin: "0 0 8px" }}>로그인이 필요합니다</h3>
      <p style={{ margin: "0 0 24px", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
        로그인 후 서비스를 이용해 주세요. 로그인 화면으로 이동합니다.
      </p>
      <div style={{ display: "flex", justifyContent: "center", gap: "8px" }}>
        <Button variant="primary">로그인하기</Button>
      </div>
    </section>
  );
}
```

```ts fw=angular
import { KrdsButtonComponent } from '@krds-community/angular';

// template
<section
  style="max-width:480px;margin:0 auto;padding:48px 24px;text-align:center"
>
  <h3 style="margin:0 0 8px">로그인이 필요합니다</h3>
  <p style="margin:0 0 24px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
    로그인 후 서비스를 이용해 주세요. 로그인 화면으로 이동합니다.
  </p>
  <div style="display:flex;justify-content:center;gap:8px">
    <krds-button variant="primary" label="로그인하기">로그인하기</krds-button>
  </div>
</section>
```

```astro fw=astro
---
import { Button } from "@krds-community/astro";
---

<section
  style="max-width:480px;margin:0 auto;padding:48px 24px;text-align:center"
>
  <h3 style="margin:0 0 8px">로그인이 필요합니다</h3>
  <p style="margin:0 0 24px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
    로그인 후 서비스를 이용해 주세요. 로그인 화면으로 이동합니다.
  </p>
  <div style="display:flex;justify-content:center;gap:8px">
    <Button variant="primary">로그인하기</Button>
  </div>
</section>
```

</FrameworkPreview>

**권장: 로그인은 꼭 필요한 상황에서 유도한다.**
사용자의 작업 과정을 방해하지 말고, 로그인이 필요한 작업을 수행하는 상황에서 로그인을 요청해야 한다. 기본, 추천 유형에서 로그인 안내는 사용자가 로그인이 필수적인 화면/기능에 접근하기 위한 액션 버튼을 실행하였을 때 제공한다. 제안 유형에서 로그인 안내는 사용자가 접근을 시도한 화면이 로딩된 후에 제공한다.

**우수: 로그인을 제안하는 경우 모달 본문에서 로그인을 통해 얻을 수 있는 이점에 대해 명확한 설명을 제공해야 한다.**
로그인 안내 모달이 제공되는 순간 사용자는 이용 흐름에 방해를 받게 된다. 따라서 로그인 제안은 로그인 상태에서 서비스/기능을 이용했을 때 사용자의 편의성을 향상할 수 있는 경우에만 사용해야 하며, 로그인했을 때의 이점, 로그인하지 않았을 때의 어려움을 포함하여 충분한 설명을 제공해야 한다.

**모범 사례**

<FrameworkPreview
  kind="good"
  title="로그인을 제안하는 경우 모달 본문에서 로그인을 통해 얻을 수 있는 이점에 대해 명확한 설명을 제공해야 한다."
>
  <div slot="react">
    <ReactModal
      open="true"
      title="로그인하고 더 편리하게 이용하세요"
      description="로그인하면 나의 신청 내역을 한눈에 확인하고, 자주 사용하는 서비스를 빠르게 이용할 수 있습니다. 로그인하지 않으면 매번 정보를 다시 입력해야 합니다."
      cancelLabel="로그인 안 함"
      confirmLabel="로그인하기"
      client:only="react"
    />
  </div>
  <div slot="vue">
    <VueModal
      open="true"
      title="로그인하고 더 편리하게 이용하세요"
      description="로그인하면 나의 신청 내역을 한눈에 확인하고, 자주 사용하는 서비스를 빠르게 이용할 수 있습니다. 로그인하지 않으면 매번 정보를 다시 입력해야 합니다."
      cancelLabel="로그인 안 함"
      confirmLabel="로그인하기"
      client:only="vue"
    />
  </div>
  <div slot="svelte">
    <SvelteModal
      open="true"
      title="로그인하고 더 편리하게 이용하세요"
      description="로그인하면 나의 신청 내역을 한눈에 확인하고, 자주 사용하는 서비스를 빠르게 이용할 수 있습니다. 로그인하지 않으면 매번 정보를 다시 입력해야 합니다."
      cancelLabel="로그인 안 함"
      confirmLabel="로그인하기"
      client:only="svelte"
    />
  </div>
  <div slot="solid">
    <SolidModal
      open="true"
      title="로그인하고 더 편리하게 이용하세요"
      description="로그인하면 나의 신청 내역을 한눈에 확인하고, 자주 사용하는 서비스를 빠르게 이용할 수 있습니다. 로그인하지 않으면 매번 정보를 다시 입력해야 합니다."
      cancelLabel="로그인 안 함"
      confirmLabel="로그인하기"
      client:only="solid-js"
    />
  </div>
  <div slot="astro">
    <AstroModal
      open="true"
      title="로그인하고 더 편리하게 이용하세요"
      description="로그인하면 나의 신청 내역을 한눈에 확인하고, 자주 사용하는 서비스를 빠르게 이용할 수 있습니다. 로그인하지 않으면 매번 정보를 다시 입력해야 합니다."
      cancelLabel="로그인 안 함"
      confirmLabel="로그인하기"
    />
  </div>
  <div slot="angular">
    <AngularModal
      open="true"
      title="로그인하고 더 편리하게 이용하세요"
      description="로그인하면 나의 신청 내역을 한눈에 확인하고, 자주 사용하는 서비스를 빠르게 이용할 수 있습니다. 로그인하지 않으면 매번 정보를 다시 입력해야 합니다."
      cancelLabel="로그인 안 함"
      confirmLabel="로그인하기"
      client:only="@analogjs/astro-angular"
    />
  </div>

```tsx fw=react
import { Modal } from "@krds-community/react";

export function Example() {
  return (
    <Modal
      open="true"
      title="로그인하고 더 편리하게 이용하세요"
      description="로그인하면 나의 신청 내역을 한눈에 확인하고, 자주 사용하는 서비스를 빠르게 이용할 수 있습니다. 로그인하지 않으면 매번 정보를 다시 입력해야 합니다."
      cancelLabel="로그인 안 함"
      confirmLabel="로그인하기"
    />
  );
}
```

```vue fw=vue
<script setup>
import { Modal } from "@krds-community/vue";
</script>

<template>
  <Modal
    open="true"
    title="로그인하고 더 편리하게 이용하세요"
    description="로그인하면 나의 신청 내역을 한눈에 확인하고, 자주 사용하는 서비스를 빠르게 이용할 수 있습니다. 로그인하지 않으면 매번 정보를 다시 입력해야 합니다."
    cancelLabel="로그인 안 함"
    confirmLabel="로그인하기"
  />
</template>
```

```svelte fw=svelte
<script>
import { Modal } from '@krds-community/svelte';
</script>

<Modal
  open="true"
  title="로그인하고 더 편리하게 이용하세요"
  description="로그인하면 나의 신청 내역을 한눈에 확인하고, 자주 사용하는 서비스를 빠르게 이용할 수 있습니다. 로그인하지 않으면 매번 정보를 다시 입력해야 합니다."
  cancelLabel="로그인 안 함"
  confirmLabel="로그인하기"
/>
```

```tsx fw=solid
import { Modal } from "@krds-community/solid";

export function Example() {
  return (
    <Modal
      open="true"
      title="로그인하고 더 편리하게 이용하세요"
      description="로그인하면 나의 신청 내역을 한눈에 확인하고, 자주 사용하는 서비스를 빠르게 이용할 수 있습니다. 로그인하지 않으면 매번 정보를 다시 입력해야 합니다."
      cancelLabel="로그인 안 함"
      confirmLabel="로그인하기"
    />
  );
}
```

```ts fw=angular
import { KrdsModalComponent } from '@krds-community/angular';

// template
<krds-modal
  open="true"
  title="로그인하고 더 편리하게 이용하세요"
  description="로그인하면 나의 신청 내역을 한눈에 확인하고, 자주 사용하는 서비스를 빠르게 이용할 수 있습니다. 로그인하지 않으면 매번 정보를 다시 입력해야 합니다."
  cancelLabel="로그인 안 함"
  confirmLabel="로그인하기"
></krds-modal>
```

```astro fw=astro
---
import { Modal } from "@krds-community/astro";
---

<Modal
  open="true"
  title="로그인하고 더 편리하게 이용하세요"
  description="로그인하면 나의 신청 내역을 한눈에 확인하고, 자주 사용하는 서비스를 빠르게 이용할 수 있습니다. 로그인하지 않으면 매번 정보를 다시 입력해야 합니다."
  cancelLabel="로그인 안 함"
  confirmLabel="로그인하기"
/>
```

</FrameworkPreview>

**우수: 사용자가 이용 절차를 단축할 수 있는 로그인 방식을 추천한다.**
로그인 방식에 따라 서비스 이용 방식이 달라지는 경우 로그인 안내 과정에서 이용 절차를 단축할 수 있는 로그인 방식을 추천한다. 예를 들어, A 방식으로 로그인 했을 때에는 최종 단계에서 별도의 본인 인증 과정을 거쳐야 하지만 B 방식으로 로그인했을 때 본인 인증 과정을 건너뛸 수 있다면 로그인 안내에서 B 방식을 사용할 것을 추천하면 된다.

**모범 사례**

<FrameworkPreview
  kind="good"
  title="사용자가 이용 절차를 단축할 수 있는 로그인 방식을 추천한다."
>
  <div slot="react">
    <ReactModal
      open="true"
      title="간편 인증으로 더 빠르게 로그인하세요"
      description="A 방식(아이디/비밀번호)으로 로그인하면 최종 단계에서 별도의 본인 인증 과정을 거쳐야 합니다. B 방식(간편 인증)으로 로그인하면 본인 인증 과정 없이 바로 서비스를 이용할 수 있습니다."
      cancelLabel="A 방식으로 로그인"
      confirmLabel="B 방식으로 로그인"
      client:only="react"
    />
  </div>
  <div slot="vue">
    <VueModal
      open="true"
      title="간편 인증으로 더 빠르게 로그인하세요"
      description="A 방식(아이디/비밀번호)으로 로그인하면 최종 단계에서 별도의 본인 인증 과정을 거쳐야 합니다. B 방식(간편 인증)으로 로그인하면 본인 인증 과정 없이 바로 서비스를 이용할 수 있습니다."
      cancelLabel="A 방식으로 로그인"
      confirmLabel="B 방식으로 로그인"
      client:only="vue"
    />
  </div>
  <div slot="svelte">
    <SvelteModal
      open="true"
      title="간편 인증으로 더 빠르게 로그인하세요"
      description="A 방식(아이디/비밀번호)으로 로그인하면 최종 단계에서 별도의 본인 인증 과정을 거쳐야 합니다. B 방식(간편 인증)으로 로그인하면 본인 인증 과정 없이 바로 서비스를 이용할 수 있습니다."
      cancelLabel="A 방식으로 로그인"
      confirmLabel="B 방식으로 로그인"
      client:only="svelte"
    />
  </div>
  <div slot="solid">
    <SolidModal
      open="true"
      title="간편 인증으로 더 빠르게 로그인하세요"
      description="A 방식(아이디/비밀번호)으로 로그인하면 최종 단계에서 별도의 본인 인증 과정을 거쳐야 합니다. B 방식(간편 인증)으로 로그인하면 본인 인증 과정 없이 바로 서비스를 이용할 수 있습니다."
      cancelLabel="A 방식으로 로그인"
      confirmLabel="B 방식으로 로그인"
      client:only="solid-js"
    />
  </div>
  <div slot="astro">
    <AstroModal
      open="true"
      title="간편 인증으로 더 빠르게 로그인하세요"
      description="A 방식(아이디/비밀번호)으로 로그인하면 최종 단계에서 별도의 본인 인증 과정을 거쳐야 합니다. B 방식(간편 인증)으로 로그인하면 본인 인증 과정 없이 바로 서비스를 이용할 수 있습니다."
      cancelLabel="A 방식으로 로그인"
      confirmLabel="B 방식으로 로그인"
    />
  </div>
  <div slot="angular">
    <AngularModal
      open="true"
      title="간편 인증으로 더 빠르게 로그인하세요"
      description="A 방식(아이디/비밀번호)으로 로그인하면 최종 단계에서 별도의 본인 인증 과정을 거쳐야 합니다. B 방식(간편 인증)으로 로그인하면 본인 인증 과정 없이 바로 서비스를 이용할 수 있습니다."
      cancelLabel="A 방식으로 로그인"
      confirmLabel="B 방식으로 로그인"
      client:only="@analogjs/astro-angular"
    />
  </div>

```tsx fw=react
import { Modal } from "@krds-community/react";

export function Example() {
  return (
    <Modal
      open="true"
      title="간편 인증으로 더 빠르게 로그인하세요"
      description="A 방식(아이디/비밀번호)으로 로그인하면 최종 단계에서 별도의 본인 인증 과정을 거쳐야 합니다. B 방식(간편 인증)으로 로그인하면 본인 인증 과정 없이 바로 서비스를 이용할 수 있습니다."
      cancelLabel="A 방식으로 로그인"
      confirmLabel="B 방식으로 로그인"
    />
  );
}
```

```vue fw=vue
<script setup>
import { Modal } from "@krds-community/vue";
</script>

<template>
  <Modal
    open="true"
    title="간편 인증으로 더 빠르게 로그인하세요"
    description="A 방식(아이디/비밀번호)으로 로그인하면 최종 단계에서 별도의 본인 인증 과정을 거쳐야 합니다. B 방식(간편 인증)으로 로그인하면 본인 인증 과정 없이 바로 서비스를 이용할 수 있습니다."
    cancelLabel="A 방식으로 로그인"
    confirmLabel="B 방식으로 로그인"
  />
</template>
```

```svelte fw=svelte
<script>
import { Modal } from '@krds-community/svelte';
</script>

<Modal
  open="true"
  title="간편 인증으로 더 빠르게 로그인하세요"
  description="A 방식(아이디/비밀번호)으로 로그인하면 최종 단계에서 별도의 본인 인증 과정을 거쳐야 합니다. B 방식(간편 인증)으로 로그인하면 본인 인증 과정 없이 바로 서비스를 이용할 수 있습니다."
  cancelLabel="A 방식으로 로그인"
  confirmLabel="B 방식으로 로그인"
/>
```

```tsx fw=solid
import { Modal } from "@krds-community/solid";

export function Example() {
  return (
    <Modal
      open="true"
      title="간편 인증으로 더 빠르게 로그인하세요"
      description="A 방식(아이디/비밀번호)으로 로그인하면 최종 단계에서 별도의 본인 인증 과정을 거쳐야 합니다. B 방식(간편 인증)으로 로그인하면 본인 인증 과정 없이 바로 서비스를 이용할 수 있습니다."
      cancelLabel="A 방식으로 로그인"
      confirmLabel="B 방식으로 로그인"
    />
  );
}
```

```ts fw=angular
import { KrdsModalComponent } from '@krds-community/angular';

// template
<krds-modal
  open="true"
  title="간편 인증으로 더 빠르게 로그인하세요"
  description="A 방식(아이디/비밀번호)으로 로그인하면 최종 단계에서 별도의 본인 인증 과정을 거쳐야 합니다. B 방식(간편 인증)으로 로그인하면 본인 인증 과정 없이 바로 서비스를 이용할 수 있습니다."
  cancelLabel="A 방식으로 로그인"
  confirmLabel="B 방식으로 로그인"
></krds-modal>
```

```astro fw=astro
---
import { Modal } from "@krds-community/astro";
---

<Modal
  open="true"
  title="간편 인증으로 더 빠르게 로그인하세요"
  description="A 방식(아이디/비밀번호)으로 로그인하면 최종 단계에서 별도의 본인 인증 과정을 거쳐야 합니다. B 방식(간편 인증)으로 로그인하면 본인 인증 과정 없이 바로 서비스를 이용할 수 있습니다."
  cancelLabel="A 방식으로 로그인"
  confirmLabel="B 방식으로 로그인"
/>
```

</FrameworkPreview>

**권장: 로그인 안내 모달에는 ‘로그인 안 함’ 옵션을 제공하여 원하지 않는 이동 동작이 발생하지 않도록 한다.**
현재 화면에 머무르기로 결정한 사용자가 서비스를 계속 이용할 수 있도록 모달 푸터에 ‘로그인 안 함’ 옵션을 제공해야 한다.

**모범 사례**

<FrameworkPreview
  kind="good"
  title="로그인 안내 모달에는 ‘로그인 안 함’ 옵션을 제공하여 원하지 않는 이동 동작이 발생하지 않도록 한다."
>
  <div slot="react">
    <ReactModal
      open="true"
      title="로그인이 필요합니다"
      description="이용을 계속하려면 로그인해 주세요. 로그인하지 않고 현재 화면에 머무를 수도 있습니다."
      cancelLabel="로그인 안 함"
      confirmLabel="로그인하기"
      client:only="react"
    />
  </div>
  <div slot="vue">
    <VueModal
      open="true"
      title="로그인이 필요합니다"
      description="이용을 계속하려면 로그인해 주세요. 로그인하지 않고 현재 화면에 머무를 수도 있습니다."
      cancelLabel="로그인 안 함"
      confirmLabel="로그인하기"
      client:only="vue"
    />
  </div>
  <div slot="svelte">
    <SvelteModal
      open="true"
      title="로그인이 필요합니다"
      description="이용을 계속하려면 로그인해 주세요. 로그인하지 않고 현재 화면에 머무를 수도 있습니다."
      cancelLabel="로그인 안 함"
      confirmLabel="로그인하기"
      client:only="svelte"
    />
  </div>
  <div slot="solid">
    <SolidModal
      open="true"
      title="로그인이 필요합니다"
      description="이용을 계속하려면 로그인해 주세요. 로그인하지 않고 현재 화면에 머무를 수도 있습니다."
      cancelLabel="로그인 안 함"
      confirmLabel="로그인하기"
      client:only="solid-js"
    />
  </div>
  <div slot="astro">
    <AstroModal
      open="true"
      title="로그인이 필요합니다"
      description="이용을 계속하려면 로그인해 주세요. 로그인하지 않고 현재 화면에 머무를 수도 있습니다."
      cancelLabel="로그인 안 함"
      confirmLabel="로그인하기"
    />
  </div>
  <div slot="angular">
    <AngularModal
      open="true"
      title="로그인이 필요합니다"
      description="이용을 계속하려면 로그인해 주세요. 로그인하지 않고 현재 화면에 머무를 수도 있습니다."
      cancelLabel="로그인 안 함"
      confirmLabel="로그인하기"
      client:only="@analogjs/astro-angular"
    />
  </div>

```tsx fw=react
import { Modal } from "@krds-community/react";

export function Example() {
  return (
    <Modal
      open="true"
      title="로그인이 필요합니다"
      description="이용을 계속하려면 로그인해 주세요. 로그인하지 않고 현재 화면에 머무를 수도 있습니다."
      cancelLabel="로그인 안 함"
      confirmLabel="로그인하기"
    />
  );
}
```

```vue fw=vue
<script setup>
import { Modal } from "@krds-community/vue";
</script>

<template>
  <Modal
    open="true"
    title="로그인이 필요합니다"
    description="이용을 계속하려면 로그인해 주세요. 로그인하지 않고 현재 화면에 머무를 수도 있습니다."
    cancelLabel="로그인 안 함"
    confirmLabel="로그인하기"
  />
</template>
```

```svelte fw=svelte
<script>
import { Modal } from '@krds-community/svelte';
</script>

<Modal
  open="true"
  title="로그인이 필요합니다"
  description="이용을 계속하려면 로그인해 주세요. 로그인하지 않고 현재 화면에 머무를 수도 있습니다."
  cancelLabel="로그인 안 함"
  confirmLabel="로그인하기"
/>
```

```tsx fw=solid
import { Modal } from "@krds-community/solid";

export function Example() {
  return (
    <Modal
      open="true"
      title="로그인이 필요합니다"
      description="이용을 계속하려면 로그인해 주세요. 로그인하지 않고 현재 화면에 머무를 수도 있습니다."
      cancelLabel="로그인 안 함"
      confirmLabel="로그인하기"
    />
  );
}
```

```ts fw=angular
import { KrdsModalComponent } from '@krds-community/angular';

// template
<krds-modal
  open="true"
  title="로그인이 필요합니다"
  description="이용을 계속하려면 로그인해 주세요. 로그인하지 않고 현재 화면에 머무를 수도 있습니다."
  cancelLabel="로그인 안 함"
  confirmLabel="로그인하기"
></krds-modal>
```

```astro fw=astro
---
import { Modal } from "@krds-community/astro";
---

<Modal
  open="true"
  title="로그인이 필요합니다"
  description="이용을 계속하려면 로그인해 주세요. 로그인하지 않고 현재 화면에 머무를 수도 있습니다."
  cancelLabel="로그인 안 함"
  confirmLabel="로그인하기"
/>
```

</FrameworkPreview>

**피해야 할 사례**

<FrameworkPreview
  kind="bad"
  title="로그인 안내 모달에는 ‘로그인 안 함’ 옵션을 제공하여 원하지 않는 이동 동작이 발생하지 않도록 한다."
>
  <div slot="react">
    <ReactModal
      open="true"
      title="로그인이 필요합니다"
      description="이용을 계속하려면 로그인해 주세요."
      confirmLabel="로그인하기"
      client:only="react"
    />
  </div>
  <div slot="vue">
    <VueModal
      open="true"
      title="로그인이 필요합니다"
      description="이용을 계속하려면 로그인해 주세요."
      confirmLabel="로그인하기"
      client:only="vue"
    />
  </div>
  <div slot="svelte">
    <SvelteModal
      open="true"
      title="로그인이 필요합니다"
      description="이용을 계속하려면 로그인해 주세요."
      confirmLabel="로그인하기"
      client:only="svelte"
    />
  </div>
  <div slot="solid">
    <SolidModal
      open="true"
      title="로그인이 필요합니다"
      description="이용을 계속하려면 로그인해 주세요."
      confirmLabel="로그인하기"
      client:only="solid-js"
    />
  </div>
  <div slot="astro">
    <AstroModal
      open="true"
      title="로그인이 필요합니다"
      description="이용을 계속하려면 로그인해 주세요."
      confirmLabel="로그인하기"
    />
  </div>
  <div slot="angular">
    <AngularModal
      open="true"
      title="로그인이 필요합니다"
      description="이용을 계속하려면 로그인해 주세요."
      confirmLabel="로그인하기"
      client:only="@analogjs/astro-angular"
    />
  </div>

```tsx fw=react
import { Modal } from "@krds-community/react";

export function Example() {
  return (
    <Modal
      open="true"
      title="로그인이 필요합니다"
      description="이용을 계속하려면 로그인해 주세요."
      confirmLabel="로그인하기"
    />
  );
}
```

```vue fw=vue
<script setup>
import { Modal } from "@krds-community/vue";
</script>

<template>
  <Modal
    open="true"
    title="로그인이 필요합니다"
    description="이용을 계속하려면 로그인해 주세요."
    confirmLabel="로그인하기"
  />
</template>
```

```svelte fw=svelte
<script>
import { Modal } from '@krds-community/svelte';
</script>

<Modal
  open="true"
  title="로그인이 필요합니다"
  description="이용을 계속하려면 로그인해 주세요."
  confirmLabel="로그인하기"
/>
```

```tsx fw=solid
import { Modal } from "@krds-community/solid";

export function Example() {
  return (
    <Modal
      open="true"
      title="로그인이 필요합니다"
      description="이용을 계속하려면 로그인해 주세요."
      confirmLabel="로그인하기"
    />
  );
}
```

```ts fw=angular
import { KrdsModalComponent } from '@krds-community/angular';

// template
<krds-modal
  open="true"
  title="로그인이 필요합니다"
  description="이용을 계속하려면 로그인해 주세요."
  confirmLabel="로그인하기"
></krds-modal>
```

```astro fw=astro
---
import { Modal } from "@krds-community/astro";
---

<Modal
  open="true"
  title="로그인이 필요합니다"
  description="이용을 계속하려면 로그인해 주세요."
  confirmLabel="로그인하기"
/>
```

</FrameworkPreview>

### 로그인 방식 확인/선택

**레이아웃** 로그인 방식의 제시 방식에 따른 유형이다.
**목록·탭·단일형** 한 화면에서 로그인 방식에 대한 비교와 선택을 수행할 수 있게 함
**단계형** 하나 이상의 레이아웃을 사용하여 단계를 거쳐 로그인 방식 선택지를 좁히도록 함
**절차** 단계형 방식을 사용하는 경우, 사용자가 가장 마지막에 로그인한 인증 방식을 저장하여 다음 로그인 시도 시 단계를 거치지 않고 저장된 로그인 방식을 이용할 수 있게 함

**우수: 빈번하게 사용되는 로그인 방식에 우선적으로 접근할 수 있는 수단을 제공한다.**
사용자 데이터를 기반으로 사용자들이 보편적으로 많이 사용하거나 서비스 관점에서 추천하는 방식을 먼저 노출한 뒤, 필요한 경우 다른 로그인 방법을 선택할 수 있도록 제시한다.

**권장: 동일한 수준의 로그인 방식을 탭으로 구분하여 선택하게 하지 않는다.**
탭은 사용자가 한 번에 두 개 이상의 콘텐츠 섹션을 확인하면서 정보를 비교하는 데 적합한 레이아웃이 아니다. 로그인 주체, 로그인 방식이 명확하게 구분되는 경우에만 사용자가 탭 컴포넌트를 기반으로 선택하도록 설계한다.

**피해야 할 사례**

<FrameworkPreview
  kind="bad"
  title="동일한 수준의 로그인 방식을 탭으로 구분하여 선택하게 하지 않는다."
>
  <div slot="react">
    <ReactTab
      aria-label="로그인 방식"
      tabs={[
        { id: "id", label: "아이디/비밀번호" },
        { id: "simple", label: "간편 인증" },
      ]}
      panels={{
        id: "아이디와 비밀번호를 입력하는 로그인 방식입니다.",
        simple: "간편 인증을 이용하는 로그인 방식입니다.",
      }}
      client:only="react"
    />
  </div>
  <div slot="vue">
    <VueTab
      aria-label="로그인 방식"
      tabs={[
        { id: "id", label: "아이디/비밀번호" },
        { id: "simple", label: "간편 인증" },
      ]}
      panels={{
        id: "아이디와 비밀번호를 입력하는 로그인 방식입니다.",
        simple: "간편 인증을 이용하는 로그인 방식입니다.",
      }}
      client:only="vue"
    />
  </div>
  <div slot="svelte">
    <SvelteTab
      aria-label="로그인 방식"
      tabs={[
        { id: "id", label: "아이디/비밀번호" },
        { id: "simple", label: "간편 인증" },
      ]}
      panels={{
        id: "아이디와 비밀번호를 입력하는 로그인 방식입니다.",
        simple: "간편 인증을 이용하는 로그인 방식입니다.",
      }}
      client:only="svelte"
    />
  </div>
  <div slot="solid">
    <SolidTab
      aria-label="로그인 방식"
      tabs={[
        { id: "id", label: "아이디/비밀번호" },
        { id: "simple", label: "간편 인증" },
      ]}
      panels={{
        id: "아이디와 비밀번호를 입력하는 로그인 방식입니다.",
        simple: "간편 인증을 이용하는 로그인 방식입니다.",
      }}
      client:only="solid-js"
    />
  </div>
  <div slot="astro">
    <AstroTab
      aria-label="로그인 방식"
      tabs={[
        { id: "id", label: "아이디/비밀번호" },
        { id: "simple", label: "간편 인증" },
      ]}
      panels={{
        id: "아이디와 비밀번호를 입력하는 로그인 방식입니다.",
        simple: "간편 인증을 이용하는 로그인 방식입니다.",
      }}
    />
  </div>
  <div slot="angular">
    <AngularTab
      aria-label="로그인 방식"
      tabs={[
        { id: "id", label: "아이디/비밀번호" },
        { id: "simple", label: "간편 인증" },
      ]}
      panels={{
        id: "아이디와 비밀번호를 입력하는 로그인 방식입니다.",
        simple: "간편 인증을 이용하는 로그인 방식입니다.",
      }}
      client:only="@analogjs/astro-angular"
    />
  </div>

```tsx fw=react
import { Tab } from "@krds-community/react";

export function Example() {
  return (
    <Tab
      aria-label="로그인 방식"
      tabs={[
        { id: "id", label: "아이디/비밀번호" },
        { id: "simple", label: "간편 인증" },
      ]}
      panels={{
        id: "아이디와 비밀번호를 입력하는 로그인 방식입니다.",
        simple: "간편 인증을 이용하는 로그인 방식입니다.",
      }}
    />
  );
}
```

```vue fw=vue
<script setup>
import { Tab } from "@krds-community/vue";
</script>

<template>
  <Tab
    aria-label="로그인 방식"
    :tabs="[
      { id: 'id', label: '아이디/비밀번호' },
      { id: 'simple', label: '간편 인증' },
    ]"
    :panels="{
      id: '아이디와 비밀번호를 입력하는 로그인 방식입니다.',
      simple: '간편 인증을 이용하는 로그인 방식입니다.',
    }"
  />
</template>
```

```svelte fw=svelte
<script>
import { Tab } from '@krds-community/svelte';
</script>

<Tab
  aria-label="로그인 방식"
  tabs={[
    { id: "id", label: "아이디/비밀번호" },
    { id: "simple", label: "간편 인증" },
  ]}
  panels={{
    id: "아이디와 비밀번호를 입력하는 로그인 방식입니다.",
    simple: "간편 인증을 이용하는 로그인 방식입니다.",
  }}
/>
```

```tsx fw=solid
import { Tab } from "@krds-community/solid";

export function Example() {
  return (
    <Tab
      aria-label="로그인 방식"
      tabs={[
        { id: "id", label: "아이디/비밀번호" },
        { id: "simple", label: "간편 인증" },
      ]}
      panels={{
        id: "아이디와 비밀번호를 입력하는 로그인 방식입니다.",
        simple: "간편 인증을 이용하는 로그인 방식입니다.",
      }}
    />
  );
}
```

```ts fw=angular
import { KrdsTabComponent } from '@krds-community/angular';

// component
// tabs = [{ id: 'id', label: '아이디/비밀번호' }, { id: 'simple', label: '간편 인증' }]
// panels = { id: '아이디와 비밀번호를 입력하는 로그인 방식입니다.', simple: '간편 인증을 이용하는 로그인 방식입니다.' }

// template
<krds-tab aria-label="로그인 방식" [tabs]="tabs" [panels]="panels"></krds-tab>
```

```astro fw=astro
---
import { Tab } from "@krds-community/astro";
---

<Tab
  aria-label="로그인 방식"
  tabs={[
    { id: "id", label: "아이디/비밀번호" },
    { id: "simple", label: "간편 인증" },
  ]}
  panels={{
    id: "아이디와 비밀번호를 입력하는 로그인 방식입니다.",
    simple: "간편 인증을 이용하는 로그인 방식입니다.",
  }}
/>
```

</FrameworkPreview>

**권장: 다양한 로그인 방식에 대한 분류 체계에 사용자의 관점과 언어를 반영한다.**
로그인 방식을 기술적인 관점으로 구분하지 않고 사용자의 관점에서 그 이용 목적에 따라 분류하여 제공한다. 이때 선택할 수 있는 로그인 방식 목록은 화면 구성을 고려하여 배치할 수 있다. 이렇게 함으로써 사용자는 여러 가지 로그인 방식 중에서 방문 목적에 적합한 로그인 방식을 빠르게 비교하여 선택할 수 있다.

**권장: 로그인 방식에 따라 서비스 이용 절차가 달라지는 경우, 사전에 안내를 제공한다.**
로그인 방식에 따라 서비스가 다르게 제공되며 이용할 수 있는 기능의 차이가 있을 수 있음을 사용자가 사전에 인지하고 로그인 방식을 선택할 수 있도록 한다.

**권장: 로그인 방식과 입력 항목에 대한 직관적인 안내를 제공한다.**
로그인 방식과 입력 항목에 대한 설명은 해당 항목 주변에 제공한다. 안내 정보를 본문 상단 또는 하단 영역에 서로 다른 로그인 방식에 대한 정보와 동시에 표현하게 되면 인지가 어려우며 안내 정보가 필요한 시점에 곧바로 접근할 수 없다.

**권장: 도움말 정보, 매뉴얼 링크를 제공한다.**
처음 방문자, 익숙하지 않은 사용자를 위해 사용자가 스스로 문제를 해결할 수 있는 링크를 제공한다. 필요한 경우, 로그인에 대한 도움을 받을 수 있는 관련 오프라인 채널 정보나 챗봇 링크가 제공될 수 있다.

**필수: 도움말 정보는 사용자의 다양한 이용 환경을 고려하여 작성한다.**
도움말 정보에 예시 화면 이미지가 포함되어 있거나 운영체제, 사용자 에이전트의 옵션/설정과 관련된 내용을 포함해야 하는 경우, 사용자가 다양한 디바이스를 통해 서비스를 이용하고 있음을 고려해야 한다. 특정 화면 크기, 특정 디바이스에서만 참고할 수 있는 정보는 일부 사용자의 문제 해결에 도움을 주지 못한다.

**인지 기능 테스트에 의존하지 않는 인증 방식을 제공한다.**
사용자 로그인 등과 같은 인증 과정이 인지 기능 테스트(예 - 로그인을 위한 비밀번호 입력, 터치스크린 화면의 패턴 인식, 임의의 문자열 기억, 계산 수행, 특정 객체를 포함하고 있는 이미지 찾기 등)에 의존하는 경우, 인지 기능 테스트에 의존하지 않는 인증 방법을 적어도 하나 이상 제공해야 한다. 다만, 이미 사용자 자신에게 익숙하여 별도의 인지적인 노력을 필요로 하지 않는 사용자의 이름이나 이메일 주소, 전화번호는 인지 기능 테스트로 간주하지 않는다.

**모범 사례 1**

<FrameworkPreview
  kind="good"
  title="인지 기능 테스트에 의존하지 않는 인증 방식을 제공한다."
>
  <div slot="react">
    <div
      style={{
        maxWidth: "360px",
        margin: "0 auto",
        padding: "24px",
        border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        borderRadius: "8px",
      }}
    >
      <ReactTextInput name="id" label="아이디" type="text" client:only="react" />
      <ReactTextInput name="password" label="비밀번호" type="password" client:only="react" />
      <div style={{ display: "flex", justifyContent: "flex-end", marginTop: "16px" }}>
        <ReactButton variant="primary" client:only="react">
          로그인
        </ReactButton>
      </div>
      <hr
        style={{
          margin: "24px 0 16px",
          border: "none",
          borderTop: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        }}
      />
      <p style={{ margin: "0 0 8px", fontSize: "14px", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
        인지 테스트 없이 이용할 수 있는 인증
      </p>
      <ReactButton variant="secondary" client:only="react">
        간편 인증으로 로그인
      </ReactButton>
    </div>
  </div>
  <div slot="vue">
    <div
      style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
    >
      <VueTextInput name="id" label="아이디" type="text" client:only="vue" />
      <VueTextInput name="password" label="비밀번호" type="password" client:only="vue" />
      <div style="display:flex;justify-content:flex-end;margin-top:16px">
        <VueButton variant="primary" client:only="vue">
          로그인
        </VueButton>
      </div>
      <hr
        style="margin:24px 0 16px;border:none;border-top:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1)"
      />
      <p style="margin:0 0 8px;font-size:14px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
        인지 테스트 없이 이용할 수 있는 인증
      </p>
      <VueButton variant="secondary" client:only="vue">
        간편 인증으로 로그인
      </VueButton>
    </div>
  </div>
  <div slot="svelte">
    <div
      style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
    >
      <SvelteTextInput name="id" label="아이디" type="text" client:only="svelte" />
      <SvelteTextInput name="password" label="비밀번호" type="password" client:only="svelte" />
      <div style="display:flex;justify-content:flex-end;margin-top:16px">
        <SvelteButton variant="primary" client:only="svelte">
          로그인
        </SvelteButton>
      </div>
      <hr
        style="margin:24px 0 16px;border:none;border-top:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1)"
      />
      <p style="margin:0 0 8px;font-size:14px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
        인지 테스트 없이 이용할 수 있는 인증
      </p>
      <SvelteButton variant="secondary" client:only="svelte">
        간편 인증으로 로그인
      </SvelteButton>
    </div>
  </div>
  <div slot="solid">
    <div
      style={{
        maxWidth: "360px",
        margin: "0 auto",
        padding: "24px",
        border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        borderRadius: "8px",
      }}
    >
      <SolidTextInput name="id" label="아이디" type="text" client:only="solid-js" />
      <SolidTextInput name="password" label="비밀번호" type="password" client:only="solid-js" />
      <div style={{ display: "flex", justifyContent: "flex-end", marginTop: "16px" }}>
        <SolidButton variant="primary" client:only="solid-js">
          로그인
        </SolidButton>
      </div>
      <hr
        style={{
          margin: "24px 0 16px",
          border: "none",
          borderTop: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        }}
      />
      <p style={{ margin: "0 0 8px", fontSize: "14px", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
        인지 테스트 없이 이용할 수 있는 인증
      </p>
      <SolidButton variant="secondary" client:only="solid-js">
        간편 인증으로 로그인
      </SolidButton>
    </div>
  </div>
  <div slot="astro">
    <div
      style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
    >
      <AstroTextInput name="id" label="아이디" type="text" />
      <AstroTextInput name="password" label="비밀번호" type="password" />
      <div style="display:flex;justify-content:flex-end;margin-top:16px">
        <AstroButton variant="primary">로그인</AstroButton>
      </div>
      <hr
        style="margin:24px 0 16px;border:none;border-top:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1)"
      />
      <p style="margin:0 0 8px;font-size:14px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
        인지 테스트 없이 이용할 수 있는 인증
      </p>
      <AstroButton variant="secondary">간편 인증으로 로그인</AstroButton>
    </div>
  </div>
  <div slot="angular">
    <div
      style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
    >
      <AngularTextInput name="id" label="아이디" type="text" client:only="@analogjs/astro-angular" />
      <AngularTextInput name="password" label="비밀번호" type="password" client:only="@analogjs/astro-angular" />
      <div style="display:flex;justify-content:flex-end;margin-top:16px">
        <AngularButton
          variant="primary"
          label="로그인"
          client:only="@analogjs/astro-angular"
        >
          로그인
        </AngularButton>
      </div>
      <hr
        style="margin:24px 0 16px;border:none;border-top:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1)"
      />
      <p style="margin:0 0 8px;font-size:14px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
        인지 테스트 없이 이용할 수 있는 인증
      </p>
      <AngularButton
        variant="secondary"
        label="간편 인증으로 로그인"
        client:only="@analogjs/astro-angular"
      >
        간편 인증으로 로그인
      </AngularButton>
    </div>
  </div>

```tsx fw=react
import { TextInput, Button } from "@krds-community/react";

export function Example() {
  return (
    <div
      style={{
        maxWidth: "360px",
        margin: "0 auto",
        padding: "24px",
        border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        borderRadius: "8px",
      }}
    >
      <TextInput name="id" label="아이디" type="text" />
      <TextInput name="password" label="비밀번호" type="password" />
      <div style={{ display: "flex", justifyContent: "flex-end", marginTop: "16px" }}>
        <Button variant="primary">로그인</Button>
      </div>
      <hr
        style={{
          margin: "24px 0 16px",
          border: "none",
          borderTop: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        }}
      />
      <p
        style={{
          margin: "0 0 8px",
          fontSize: "14px",
          color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)",
        }}
      >
        인지 테스트 없이 이용할 수 있는 인증
      </p>
      <Button variant="secondary">간편 인증으로 로그인</Button>
    </div>
  );
}
```

```vue fw=vue
<script setup>
import { TextInput, Button } from "@krds-community/vue";
</script>

<template>
  <div
    style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
  >
    <TextInput name="id" label="아이디" type="text" />
    <TextInput name="password" label="비밀번호" type="password" />
    <div style="display:flex;justify-content:flex-end;margin-top:16px">
      <Button variant="primary">로그인</Button>
    </div>
    <hr
      style="margin:24px 0 16px;border:none;border-top:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1)"
    />
    <p style="margin:0 0 8px;font-size:14px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
      인지 테스트 없이 이용할 수 있는 인증
    </p>
    <Button variant="secondary">간편 인증으로 로그인</Button>
  </div>
</template>
```

```svelte fw=svelte
<script>
import { TextInput, Button } from '@krds-community/svelte';
</script>

<div
  style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
>
  <TextInput name="id" label="아이디" type="text" />
  <TextInput name="password" label="비밀번호" type="password" />
  <div style="display:flex;justify-content:flex-end;margin-top:16px">
    <Button variant="primary">로그인</Button>
  </div>
  <hr
    style="margin:24px 0 16px;border:none;border-top:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1)"
  />
  <p style="margin:0 0 8px;font-size:14px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
    인지 테스트 없이 이용할 수 있는 인증
  </p>
  <Button variant="secondary">간편 인증으로 로그인</Button>
</div>
```

```tsx fw=solid
import { TextInput, Button } from "@krds-community/solid";

export function Example() {
  return (
    <div
      style={{
        maxWidth: "360px",
        margin: "0 auto",
        padding: "24px",
        border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        borderRadius: "8px",
      }}
    >
      <TextInput name="id" label="아이디" type="text" />
      <TextInput name="password" label="비밀번호" type="password" />
      <div style={{ display: "flex", justifyContent: "flex-end", marginTop: "16px" }}>
        <Button variant="primary">로그인</Button>
      </div>
      <hr
        style={{
          margin: "24px 0 16px",
          border: "none",
          borderTop: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        }}
      />
      <p
        style={{
          margin: "0 0 8px",
          fontSize: "14px",
          color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)",
        }}
      >
        인지 테스트 없이 이용할 수 있는 인증
      </p>
      <Button variant="secondary">간편 인증으로 로그인</Button>
    </div>
  );
}
```

```ts fw=angular
import { KrdsTextInputComponent, KrdsButtonComponent } from '@krds-community/angular';

// template
<div
  style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
>
  <krds-text-input name="id" label="아이디" type="text"></krds-text-input>
  <krds-text-input name="password" label="비밀번호" type="password"></krds-text-input>
  <div style="display:flex;justify-content:flex-end;margin-top:16px">
    <krds-button variant="primary" label="로그인">로그인</krds-button>
  </div>
  <hr
    style="margin:24px 0 16px;border:none;border-top:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1)"
  />
  <p style="margin:0 0 8px;font-size:14px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
    인지 테스트 없이 이용할 수 있는 인증
  </p>
  <krds-button variant="secondary" label="간편 인증으로 로그인">간편 인증으로 로그인</krds-button>
</div>
```

```astro fw=astro
---
import { TextInput, Button } from "@krds-community/astro";
---

<div
  style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
>
  <TextInput name="id" label="아이디" type="text" />
  <TextInput name="password" label="비밀번호" type="password" />
  <div style="display:flex;justify-content:flex-end;margin-top:16px">
    <Button variant="primary">로그인</Button>
  </div>
  <hr
    style="margin:24px 0 16px;border:none;border-top:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1)"
  />
  <p style="margin:0 0 8px;font-size:14px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
    인지 테스트 없이 이용할 수 있는 인증
  </p>
  <Button variant="secondary">간편 인증으로 로그인</Button>
</div>
```

</FrameworkPreview>

**모범 사례 2**

<FrameworkPreview
  kind="good"
  title="인지 기능 테스트에 의존하지 않는 인증 방식을 제공한다."
>
  <div slot="react">
    <div
      style={{
        maxWidth: "360px",
        margin: "0 auto",
        padding: "24px",
        border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        borderRadius: "8px",
      }}
    >
      <ReactTextInput name="id" label="아이디" type="text" client:only="react" />
      <ReactTextInput name="password" label="비밀번호" type="password" client:only="react" />
      <div style={{ display: "flex", justifyContent: "flex-end", marginTop: "16px" }}>
        <ReactButton variant="primary" client:only="react">
          로그인
        </ReactButton>
      </div>
      <hr
        style={{
          margin: "24px 0 16px",
          border: "none",
          borderTop: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        }}
      />
      <p style={{ margin: "0 0 8px", fontSize: "14px", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
        문자 인증으로 로그인할 수 있습니다.
      </p>
      <ReactButton variant="secondary" client:only="react">
        문자 인증으로 로그인
      </ReactButton>
    </div>
  </div>
  <div slot="vue">
    <div
      style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
    >
      <VueTextInput name="id" label="아이디" type="text" client:only="vue" />
      <VueTextInput name="password" label="비밀번호" type="password" client:only="vue" />
      <div style="display:flex;justify-content:flex-end;margin-top:16px">
        <VueButton variant="primary" client:only="vue">
          로그인
        </VueButton>
      </div>
      <hr
        style="margin:24px 0 16px;border:none;border-top:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1)"
      />
      <p style="margin:0 0 8px;font-size:14px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
        문자 인증으로 로그인할 수 있습니다.
      </p>
      <VueButton variant="secondary" client:only="vue">
        문자 인증으로 로그인
      </VueButton>
    </div>
  </div>
  <div slot="svelte">
    <div
      style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
    >
      <SvelteTextInput name="id" label="아이디" type="text" client:only="svelte" />
      <SvelteTextInput name="password" label="비밀번호" type="password" client:only="svelte" />
      <div style="display:flex;justify-content:flex-end;margin-top:16px">
        <SvelteButton variant="primary" client:only="svelte">
          로그인
        </SvelteButton>
      </div>
      <hr
        style="margin:24px 0 16px;border:none;border-top:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1)"
      />
      <p style="margin:0 0 8px;font-size:14px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
        문자 인증으로 로그인할 수 있습니다.
      </p>
      <SvelteButton variant="secondary" client:only="svelte">
        문자 인증으로 로그인
      </SvelteButton>
    </div>
  </div>
  <div slot="solid">
    <div
      style={{
        maxWidth: "360px",
        margin: "0 auto",
        padding: "24px",
        border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        borderRadius: "8px",
      }}
    >
      <SolidTextInput name="id" label="아이디" type="text" client:only="solid-js" />
      <SolidTextInput name="password" label="비밀번호" type="password" client:only="solid-js" />
      <div style={{ display: "flex", justifyContent: "flex-end", marginTop: "16px" }}>
        <SolidButton variant="primary" client:only="solid-js">
          로그인
        </SolidButton>
      </div>
      <hr
        style={{
          margin: "24px 0 16px",
          border: "none",
          borderTop: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        }}
      />
      <p style={{ margin: "0 0 8px", fontSize: "14px", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
        문자 인증으로 로그인할 수 있습니다.
      </p>
      <SolidButton variant="secondary" client:only="solid-js">
        문자 인증으로 로그인
      </SolidButton>
    </div>
  </div>
  <div slot="astro">
    <div
      style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
    >
      <AstroTextInput name="id" label="아이디" type="text" />
      <AstroTextInput name="password" label="비밀번호" type="password" />
      <div style="display:flex;justify-content:flex-end;margin-top:16px">
        <AstroButton variant="primary">로그인</AstroButton>
      </div>
      <hr
        style="margin:24px 0 16px;border:none;border-top:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1)"
      />
      <p style="margin:0 0 8px;font-size:14px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
        문자 인증으로 로그인할 수 있습니다.
      </p>
      <AstroButton variant="secondary">문자 인증으로 로그인</AstroButton>
    </div>
  </div>
  <div slot="angular">
    <div
      style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
    >
      <AngularTextInput name="id" label="아이디" type="text" client:only="@analogjs/astro-angular" />
      <AngularTextInput name="password" label="비밀번호" type="password" client:only="@analogjs/astro-angular" />
      <div style="display:flex;justify-content:flex-end;margin-top:16px">
        <AngularButton
          variant="primary"
          label="로그인"
          client:only="@analogjs/astro-angular"
        >
          로그인
        </AngularButton>
      </div>
      <hr
        style="margin:24px 0 16px;border:none;border-top:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1)"
      />
      <p style="margin:0 0 8px;font-size:14px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
        문자 인증으로 로그인할 수 있습니다.
      </p>
      <AngularButton
        variant="secondary"
        label="문자 인증으로 로그인"
        client:only="@analogjs/astro-angular"
      >
        문자 인증으로 로그인
      </AngularButton>
    </div>
  </div>

```tsx fw=react
import { TextInput, Button } from "@krds-community/react";

export function Example() {
  return (
    <div
      style={{
        maxWidth: "360px",
        margin: "0 auto",
        padding: "24px",
        border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        borderRadius: "8px",
      }}
    >
      <TextInput name="id" label="아이디" type="text" />
      <TextInput name="password" label="비밀번호" type="password" />
      <div style={{ display: "flex", justifyContent: "flex-end", marginTop: "16px" }}>
        <Button variant="primary">로그인</Button>
      </div>
      <hr
        style={{
          margin: "24px 0 16px",
          border: "none",
          borderTop: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        }}
      />
      <p
        style={{
          margin: "0 0 8px",
          fontSize: "14px",
          color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)",
        }}
      >
        문자 인증으로 로그인할 수 있습니다.
      </p>
      <Button variant="secondary">문자 인증으로 로그인</Button>
    </div>
  );
}
```

```vue fw=vue
<script setup>
import { TextInput, Button } from "@krds-community/vue";
</script>

<template>
  <div
    style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
  >
    <TextInput name="id" label="아이디" type="text" />
    <TextInput name="password" label="비밀번호" type="password" />
    <div style="display:flex;justify-content:flex-end;margin-top:16px">
      <Button variant="primary">로그인</Button>
    </div>
    <hr
      style="margin:24px 0 16px;border:none;border-top:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1)"
    />
    <p style="margin:0 0 8px;font-size:14px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
      문자 인증으로 로그인할 수 있습니다.
    </p>
    <Button variant="secondary">문자 인증으로 로그인</Button>
  </div>
</template>
```

```svelte fw=svelte
<script>
import { TextInput, Button } from '@krds-community/svelte';
</script>

<div
  style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
>
  <TextInput name="id" label="아이디" type="text" />
  <TextInput name="password" label="비밀번호" type="password" />
  <div style="display:flex;justify-content:flex-end;margin-top:16px">
    <Button variant="primary">로그인</Button>
  </div>
  <hr
    style="margin:24px 0 16px;border:none;border-top:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1)"
  />
  <p style="margin:0 0 8px;font-size:14px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
    문자 인증으로 로그인할 수 있습니다.
  </p>
  <Button variant="secondary">문자 인증으로 로그인</Button>
</div>
```

```tsx fw=solid
import { TextInput, Button } from "@krds-community/solid";

export function Example() {
  return (
    <div
      style={{
        maxWidth: "360px",
        margin: "0 auto",
        padding: "24px",
        border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        borderRadius: "8px",
      }}
    >
      <TextInput name="id" label="아이디" type="text" />
      <TextInput name="password" label="비밀번호" type="password" />
      <div style={{ display: "flex", justifyContent: "flex-end", marginTop: "16px" }}>
        <Button variant="primary">로그인</Button>
      </div>
      <hr
        style={{
          margin: "24px 0 16px",
          border: "none",
          borderTop: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        }}
      />
      <p
        style={{
          margin: "0 0 8px",
          fontSize: "14px",
          color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)",
        }}
      >
        문자 인증으로 로그인할 수 있습니다.
      </p>
      <Button variant="secondary">문자 인증으로 로그인</Button>
    </div>
  );
}
```

```ts fw=angular
import { KrdsTextInputComponent, KrdsButtonComponent } from '@krds-community/angular';

// template
<div
  style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
>
  <krds-text-input name="id" label="아이디" type="text"></krds-text-input>
  <krds-text-input name="password" label="비밀번호" type="password"></krds-text-input>
  <div style="display:flex;justify-content:flex-end;margin-top:16px">
    <krds-button variant="primary" label="로그인">로그인</krds-button>
  </div>
  <hr
    style="margin:24px 0 16px;border:none;border-top:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1)"
  />
  <p style="margin:0 0 8px;font-size:14px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
    문자 인증으로 로그인할 수 있습니다.
  </p>
  <krds-button variant="secondary" label="문자 인증으로 로그인">문자 인증으로 로그인</krds-button>
</div>
```

```astro fw=astro
---
import { TextInput, Button } from "@krds-community/astro";
---

<div
  style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
>
  <TextInput name="id" label="아이디" type="text" />
  <TextInput name="password" label="비밀번호" type="password" />
  <div style="display:flex;justify-content:flex-end;margin-top:16px">
    <Button variant="primary">로그인</Button>
  </div>
  <hr
    style="margin:24px 0 16px;border:none;border-top:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1)"
  />
  <p style="margin:0 0 8px;font-size:14px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
    문자 인증으로 로그인할 수 있습니다.
  </p>
  <Button variant="secondary">문자 인증으로 로그인</Button>
</div>
```

</FrameworkPreview>

**로그인 방식을 우선으로 화면에 배치한다.**
작은 화면에 특성 상 사용자가 로그인방식의 전체를 한눈에 확인 할 수 있도록 화면에 배치한다.

### 로그인 정보 입력

**필수: 아이디 형식에 대한 단서를 사전에 제공한다.**
이메일 주소를 아이디로 사용하는 경우, 아이디를 이메일 형식으로 입력해야 한다는 것을 안내하지 않으면 기억에 의존해야 하므로 많은 시간이 걸린다. 사용자가 입력해야 하는 정보를 추측하지 않도록 레이블 및 도움말을 플레이스홀더가 아닌 레이블로 제공한다.

**피해야 할 사례**

<FrameworkPreview
  kind="bad"
  title="아이디 형식에 대한 단서를 사전에 제공한다."
>
  <div slot="react">
    <div
      style={{
        maxWidth: "360px",
        margin: "0 auto",
        padding: "24px",
        border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        borderRadius: "8px",
      }}
    >
      <ReactTextInput
        name="id"
        label="아이디"
        type="text"
        placeholder="이메일 형식으로 입력해 주세요"
        client:only="react"
      />
      <ReactTextInput name="password" label="비밀번호" type="password" client:only="react" />
      <div style={{ display: "flex", justifyContent: "flex-end", marginTop: "16px" }}>
        <ReactButton variant="primary" client:only="react">
          로그인
        </ReactButton>
      </div>
    </div>
  </div>
  <div slot="vue">
    <div
      style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
    >
      <VueTextInput
        name="id"
        label="아이디"
        type="text"
        placeholder="이메일 형식으로 입력해 주세요"
        client:only="vue"
      />
      <VueTextInput name="password" label="비밀번호" type="password" client:only="vue" />
      <div style="display:flex;justify-content:flex-end;margin-top:16px">
        <VueButton variant="primary" client:only="vue">
          로그인
        </VueButton>
      </div>
    </div>
  </div>
  <div slot="svelte">
    <div
      style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
    >
      <SvelteTextInput
        name="id"
        label="아이디"
        type="text"
        placeholder="이메일 형식으로 입력해 주세요"
        client:only="svelte"
      />
      <SvelteTextInput name="password" label="비밀번호" type="password" client:only="svelte" />
      <div style="display:flex;justify-content:flex-end;margin-top:16px">
        <SvelteButton variant="primary" client:only="svelte">
          로그인
        </SvelteButton>
      </div>
    </div>
  </div>
  <div slot="solid">
    <div
      style={{
        maxWidth: "360px",
        margin: "0 auto",
        padding: "24px",
        border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        borderRadius: "8px",
      }}
    >
      <SolidTextInput
        name="id"
        label="아이디"
        type="text"
        placeholder="이메일 형식으로 입력해 주세요"
        client:only="solid-js"
      />
      <SolidTextInput name="password" label="비밀번호" type="password" client:only="solid-js" />
      <div style={{ display: "flex", justifyContent: "flex-end", marginTop: "16px" }}>
        <SolidButton variant="primary" client:only="solid-js">
          로그인
        </SolidButton>
      </div>
    </div>
  </div>
  <div slot="astro">
    <div
      style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
    >
      <AstroTextInput
        name="id"
        label="아이디"
        type="text"
        placeholder="이메일 형식으로 입력해 주세요"
      />
      <AstroTextInput name="password" label="비밀번호" type="password" />
      <div style="display:flex;justify-content:flex-end;margin-top:16px">
        <AstroButton variant="primary">로그인</AstroButton>
      </div>
    </div>
  </div>
  <div slot="angular">
    <div
      style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
    >
      <AngularTextInput
        name="id"
        label="아이디"
        type="text"
        placeholder="이메일 형식으로 입력해 주세요"
        client:only="@analogjs/astro-angular"
      />
      <AngularTextInput name="password" label="비밀번호" type="password" client:only="@analogjs/astro-angular" />
      <div style="display:flex;justify-content:flex-end;margin-top:16px">
        <AngularButton
          variant="primary"
          label="로그인"
          client:only="@analogjs/astro-angular"
        >
          로그인
        </AngularButton>
      </div>
    </div>
  </div>

```tsx fw=react
import { TextInput, Button } from "@krds-community/react";

export function Example() {
  return (
    <div
      style={{
        maxWidth: "360px",
        margin: "0 auto",
        padding: "24px",
        border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        borderRadius: "8px",
      }}
    >
      <TextInput name="id" label="아이디" type="text" placeholder="이메일 형식으로 입력해 주세요" />
      <TextInput name="password" label="비밀번호" type="password" />
      <div style={{ display: "flex", justifyContent: "flex-end", marginTop: "16px" }}>
        <Button variant="primary">로그인</Button>
      </div>
    </div>
  );
}
```

```vue fw=vue
<script setup>
import { TextInput, Button } from "@krds-community/vue";
</script>

<template>
  <div
    style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
  >
    <TextInput name="id" label="아이디" type="text" placeholder="이메일 형식으로 입력해 주세요" />
    <TextInput name="password" label="비밀번호" type="password" />
    <div style="display:flex;justify-content:flex-end;margin-top:16px">
      <Button variant="primary">로그인</Button>
    </div>
  </div>
</template>
```

```svelte fw=svelte
<script>
import { TextInput, Button } from '@krds-community/svelte';
</script>

<div
  style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
>
  <TextInput
    name="id"
    label="아이디"
    type="text"
    placeholder="이메일 형식으로 입력해 주세요"
  />
  <TextInput name="password" label="비밀번호" type="password" />
  <div style="display:flex;justify-content:flex-end;margin-top:16px">
    <Button variant="primary">로그인</Button>
  </div>
</div>
```

```tsx fw=solid
import { TextInput, Button } from "@krds-community/solid";

export function Example() {
  return (
    <div
      style={{
        maxWidth: "360px",
        margin: "0 auto",
        padding: "24px",
        border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        borderRadius: "8px",
      }}
    >
      <TextInput name="id" label="아이디" type="text" placeholder="이메일 형식으로 입력해 주세요" />
      <TextInput name="password" label="비밀번호" type="password" />
      <div style={{ display: "flex", justifyContent: "flex-end", marginTop: "16px" }}>
        <Button variant="primary">로그인</Button>
      </div>
    </div>
  );
}
```

```ts fw=angular
import { KrdsTextInputComponent, KrdsButtonComponent } from '@krds-community/angular';

// template
<div
  style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
>
  <krds-text-input
    name="id"
    label="아이디"
    type="text"
    placeholder="이메일 형식으로 입력해 주세요"
  ></krds-text-input>
  <krds-text-input name="password" label="비밀번호" type="password"></krds-text-input>
  <div style="display:flex;justify-content:flex-end;margin-top:16px">
    <krds-button variant="primary" label="로그인">로그인</krds-button>
  </div>
</div>
```

```astro fw=astro
---
import { TextInput, Button } from "@krds-community/astro";
---

<div
  style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
>
  <TextInput
    name="id"
    label="아이디"
    type="text"
    placeholder="이메일 형식으로 입력해 주세요"
  />
  <TextInput name="password" label="비밀번호" type="password" />
  <div style="display:flex;justify-content:flex-end;margin-top:16px">
    <Button variant="primary">로그인</Button>
  </div>
</div>
```

</FrameworkPreview>

**필수: 비밀번호를 표시할 수 있는 옵션을 제공한다.**
사용자가 비밀번호를 입력할 때 공공장소에 있을 수 있으므로 기본적으로 비밀번호를 숨겨야 한다. 이때, 사용자가 원하는 경우 비밀번호를 볼 수 있도록 '비밀번호 표시' 버튼을 통해 사용자가 비밀번호를 입력할 수 있도록 돕는다. 이를 통해 사용자가 비밀번호를 올바르게 입력하는 동시에, 필요할 때 개인정보를 보호할 수 있다.

**필수: 복사/붙여넣기를 제한하지 않는다.**
아이디와 비밀번호를 키보드/키패드로만 입력하도록 제한하게 되면 사용자의 실수를 유발할 수 있다. 계정 정보의 기억에 어려움이 있거나 키보드/키패드를 빠르고 정확하게 입력하기 어려운 사용자를 위해 복사/붙여넣기 기능을 제한하지 않아야 한다.

**우수: 로그인 정보를 저장할 수 있는 옵션을 제공한다.**
로그인 시 사용자의 최근 로그인 방식을 저장하거나, 사용자가 선호하는 로그인 방식을 직접 설정할 수 있게 함으로써 자주 방문하는 사용자의 로그인 경로를 단축할 수 있다. 부가적으로 아이디나 비밀번호 중 어떤 정보가 저장되는지, 얼마 동안 저장되는지에 관한 정보를 제공할 수 있다.

**필수: 아이디/비밀번호 찾기 링크를 제공한다.**
대부분의 로그인 오류는 사용자가 계정 정보를 잊어버리는 것에서 기인한다. 사용자가 로그인에 필요한 정보를 쉽게 검색할 수 있도록 로그인 양식 주변에 아이디/비밀번호 찾기 링크를 제공해야 한다.

**필수: 오류 메시지는 명확하고 간결하게 제공한다.**
사용자가 무엇이 잘못되었는지 이해할 수 있도록 도와주고 오류를 해결할 수 있는 방법을 안내해야 한다. 오류 메시지는 가능한 한 구체적으로 작성한다.

**모범 사례**

<FrameworkPreview
  kind="good"
  title="오류 메시지는 명확하고 간결하게 제공한다."
>
  <div slot="react">
    <div
      style={{
        maxWidth: "360px",
        margin: "0 auto",
        padding: "24px",
        border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        borderRadius: "8px",
      }}
    >
      <ReactTextInput name="id" label="아이디" type="text" client:only="react" />
      <ReactTextInput
        name="password"
        label="비밀번호"
        type="password"
        state="error"
        hint="비밀번호가 일치하지 않습니다. 다시 확인해 주세요."
        client:only="react"
      />
      <div style={{ display: "flex", justifyContent: "flex-end", marginTop: "16px" }}>
        <ReactButton variant="primary" client:only="react">
          로그인
        </ReactButton>
      </div>
    </div>
  </div>
  <div slot="vue">
    <div
      style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
    >
      <VueTextInput name="id" label="아이디" type="text" client:only="vue" />
      <VueTextInput
        name="password"
        label="비밀번호"
        type="password"
        state="error"
        hint="비밀번호가 일치하지 않습니다. 다시 확인해 주세요."
        client:only="vue"
      />
      <div style="display:flex;justify-content:flex-end;margin-top:16px">
        <VueButton variant="primary" client:only="vue">
          로그인
        </VueButton>
      </div>
    </div>
  </div>
  <div slot="svelte">
    <div
      style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
    >
      <SvelteTextInput name="id" label="아이디" type="text" client:only="svelte" />
      <SvelteTextInput
        name="password"
        label="비밀번호"
        type="password"
        state="error"
        hint="비밀번호가 일치하지 않습니다. 다시 확인해 주세요."
        client:only="svelte"
      />
      <div style="display:flex;justify-content:flex-end;margin-top:16px">
        <SvelteButton variant="primary" client:only="svelte">
          로그인
        </SvelteButton>
      </div>
    </div>
  </div>
  <div slot="solid">
    <div
      style={{
        maxWidth: "360px",
        margin: "0 auto",
        padding: "24px",
        border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        borderRadius: "8px",
      }}
    >
      <SolidTextInput name="id" label="아이디" type="text" client:only="solid-js" />
      <SolidTextInput
        name="password"
        label="비밀번호"
        type="password"
        state="error"
        hint="비밀번호가 일치하지 않습니다. 다시 확인해 주세요."
        client:only="solid-js"
      />
      <div style={{ display: "flex", justifyContent: "flex-end", marginTop: "16px" }}>
        <SolidButton variant="primary" client:only="solid-js">
          로그인
        </SolidButton>
      </div>
    </div>
  </div>
  <div slot="astro">
    <div
      style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
    >
      <AstroTextInput name="id" label="아이디" type="text" />
      <AstroTextInput
        name="password"
        label="비밀번호"
        type="password"
        state="error"
        hint="비밀번호가 일치하지 않습니다. 다시 확인해 주세요."
      />
      <div style="display:flex;justify-content:flex-end;margin-top:16px">
        <AstroButton variant="primary">로그인</AstroButton>
      </div>
    </div>
  </div>
  <div slot="angular">
    <div
      style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
    >
      <AngularTextInput name="id" label="아이디" type="text" client:only="@analogjs/astro-angular" />
      <AngularTextInput
        name="password"
        label="비밀번호"
        type="password"
        state="error"
        hint="비밀번호가 일치하지 않습니다. 다시 확인해 주세요."
        client:only="@analogjs/astro-angular"
      />
      <div style="display:flex;justify-content:flex-end;margin-top:16px">
        <AngularButton
          variant="primary"
          label="로그인"
          client:only="@analogjs/astro-angular"
        >
          로그인
        </AngularButton>
      </div>
    </div>
  </div>

```tsx fw=react
import { TextInput, Button } from "@krds-community/react";

export function Example() {
  return (
    <div
      style={{
        maxWidth: "360px",
        margin: "0 auto",
        padding: "24px",
        border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        borderRadius: "8px",
      }}
    >
      <TextInput name="id" label="아이디" type="text" />
      <TextInput
        name="password"
        label="비밀번호"
        type="password"
        state="error"
        hint="비밀번호가 일치하지 않습니다. 다시 확인해 주세요."
      />
      <div style={{ display: "flex", justifyContent: "flex-end", marginTop: "16px" }}>
        <Button variant="primary">로그인</Button>
      </div>
    </div>
  );
}
```

```vue fw=vue
<script setup>
import { TextInput, Button } from "@krds-community/vue";
</script>

<template>
  <div
    style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
  >
    <TextInput name="id" label="아이디" type="text" />
    <TextInput
      name="password"
      label="비밀번호"
      type="password"
      state="error"
      hint="비밀번호가 일치하지 않습니다. 다시 확인해 주세요."
    />
    <div style="display:flex;justify-content:flex-end;margin-top:16px">
      <Button variant="primary">로그인</Button>
    </div>
  </div>
</template>
```

```svelte fw=svelte
<script>
import { TextInput, Button } from '@krds-community/svelte';
</script>

<div
  style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
>
  <TextInput name="id" label="아이디" type="text" />
  <TextInput
    name="password"
    label="비밀번호"
    type="password"
    state="error"
    hint="비밀번호가 일치하지 않습니다. 다시 확인해 주세요."
  />
  <div style="display:flex;justify-content:flex-end;margin-top:16px">
    <Button variant="primary">로그인</Button>
  </div>
</div>
```

```tsx fw=solid
import { TextInput, Button } from "@krds-community/solid";

export function Example() {
  return (
    <div
      style={{
        maxWidth: "360px",
        margin: "0 auto",
        padding: "24px",
        border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        borderRadius: "8px",
      }}
    >
      <TextInput name="id" label="아이디" type="text" />
      <TextInput
        name="password"
        label="비밀번호"
        type="password"
        state="error"
        hint="비밀번호가 일치하지 않습니다. 다시 확인해 주세요."
      />
      <div style={{ display: "flex", justifyContent: "flex-end", marginTop: "16px" }}>
        <Button variant="primary">로그인</Button>
      </div>
    </div>
  );
}
```

```ts fw=angular
import { KrdsTextInputComponent, KrdsButtonComponent } from '@krds-community/angular';

// template
<div
  style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
>
  <krds-text-input name="id" label="아이디" type="text"></krds-text-input>
  <krds-text-input
    name="password"
    label="비밀번호"
    type="password"
    state="error"
    hint="비밀번호가 일치하지 않습니다. 다시 확인해 주세요."
  ></krds-text-input>
  <div style="display:flex;justify-content:flex-end;margin-top:16px">
    <krds-button variant="primary" label="로그인">로그인</krds-button>
  </div>
</div>
```

```astro fw=astro
---
import { TextInput, Button } from "@krds-community/astro";
---

<div
  style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
>
  <TextInput name="id" label="아이디" type="text" />
  <TextInput
    name="password"
    label="비밀번호"
    type="password"
    state="error"
    hint="비밀번호가 일치하지 않습니다. 다시 확인해 주세요."
  />
  <div style="display:flex;justify-content:flex-end;margin-top:16px">
    <Button variant="primary">로그인</Button>
  </div>
</div>
```

</FrameworkPreview>

**피해야 할 사례 1**

<FrameworkPreview
  kind="bad"
  title="오류 메시지는 명확하고 간결하게 제공한다."
>
  <div slot="react">
    <div
      style={{
        maxWidth: "360px",
        margin: "0 auto",
        padding: "24px",
        border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        borderRadius: "8px",
      }}
    >
      <ReactTextInput name="id" label="아이디" type="text" client:only="react" />
      <ReactTextInput
        name="password"
        label="비밀번호"
        type="password"
        state="error"
        hint="입력하신 내용을 확인해 주세요."
        client:only="react"
      />
      <div style={{ display: "flex", justifyContent: "flex-end", marginTop: "16px" }}>
        <ReactButton variant="primary" client:only="react">
          로그인
        </ReactButton>
      </div>
    </div>
  </div>
  <div slot="vue">
    <div
      style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
    >
      <VueTextInput name="id" label="아이디" type="text" client:only="vue" />
      <VueTextInput
        name="password"
        label="비밀번호"
        type="password"
        state="error"
        hint="입력하신 내용을 확인해 주세요."
        client:only="vue"
      />
      <div style="display:flex;justify-content:flex-end;margin-top:16px">
        <VueButton variant="primary" client:only="vue">
          로그인
        </VueButton>
      </div>
    </div>
  </div>
  <div slot="svelte">
    <div
      style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
    >
      <SvelteTextInput name="id" label="아이디" type="text" client:only="svelte" />
      <SvelteTextInput
        name="password"
        label="비밀번호"
        type="password"
        state="error"
        hint="입력하신 내용을 확인해 주세요."
        client:only="svelte"
      />
      <div style="display:flex;justify-content:flex-end;margin-top:16px">
        <SvelteButton variant="primary" client:only="svelte">
          로그인
        </SvelteButton>
      </div>
    </div>
  </div>
  <div slot="solid">
    <div
      style={{
        maxWidth: "360px",
        margin: "0 auto",
        padding: "24px",
        border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        borderRadius: "8px",
      }}
    >
      <SolidTextInput name="id" label="아이디" type="text" client:only="solid-js" />
      <SolidTextInput
        name="password"
        label="비밀번호"
        type="password"
        state="error"
        hint="입력하신 내용을 확인해 주세요."
        client:only="solid-js"
      />
      <div style={{ display: "flex", justifyContent: "flex-end", marginTop: "16px" }}>
        <SolidButton variant="primary" client:only="solid-js">
          로그인
        </SolidButton>
      </div>
    </div>
  </div>
  <div slot="astro">
    <div
      style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
    >
      <AstroTextInput name="id" label="아이디" type="text" />
      <AstroTextInput
        name="password"
        label="비밀번호"
        type="password"
        state="error"
        hint="입력하신 내용을 확인해 주세요."
      />
      <div style="display:flex;justify-content:flex-end;margin-top:16px">
        <AstroButton variant="primary">로그인</AstroButton>
      </div>
    </div>
  </div>
  <div slot="angular">
    <div
      style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
    >
      <AngularTextInput name="id" label="아이디" type="text" client:only="@analogjs/astro-angular" />
      <AngularTextInput
        name="password"
        label="비밀번호"
        type="password"
        state="error"
        hint="입력하신 내용을 확인해 주세요."
        client:only="@analogjs/astro-angular"
      />
      <div style="display:flex;justify-content:flex-end;margin-top:16px">
        <AngularButton
          variant="primary"
          label="로그인"
          client:only="@analogjs/astro-angular"
        >
          로그인
        </AngularButton>
      </div>
    </div>
  </div>

```tsx fw=react
import { TextInput, Button } from "@krds-community/react";

export function Example() {
  return (
    <div
      style={{
        maxWidth: "360px",
        margin: "0 auto",
        padding: "24px",
        border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        borderRadius: "8px",
      }}
    >
      <TextInput name="id" label="아이디" type="text" />
      <TextInput
        name="password"
        label="비밀번호"
        type="password"
        state="error"
        hint="입력하신 내용을 확인해 주세요."
      />
      <div style={{ display: "flex", justifyContent: "flex-end", marginTop: "16px" }}>
        <Button variant="primary">로그인</Button>
      </div>
    </div>
  );
}
```

```vue fw=vue
<script setup>
import { TextInput, Button } from "@krds-community/vue";
</script>

<template>
  <div
    style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
  >
    <TextInput name="id" label="아이디" type="text" />
    <TextInput
      name="password"
      label="비밀번호"
      type="password"
      state="error"
      hint="입력하신 내용을 확인해 주세요."
    />
    <div style="display:flex;justify-content:flex-end;margin-top:16px">
      <Button variant="primary">로그인</Button>
    </div>
  </div>
</template>
```

```svelte fw=svelte
<script>
import { TextInput, Button } from '@krds-community/svelte';
</script>

<div
  style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
>
  <TextInput name="id" label="아이디" type="text" />
  <TextInput
    name="password"
    label="비밀번호"
    type="password"
    state="error"
    hint="입력하신 내용을 확인해 주세요."
  />
  <div style="display:flex;justify-content:flex-end;margin-top:16px">
    <Button variant="primary">로그인</Button>
  </div>
</div>
```

```tsx fw=solid
import { TextInput, Button } from "@krds-community/solid";

export function Example() {
  return (
    <div
      style={{
        maxWidth: "360px",
        margin: "0 auto",
        padding: "24px",
        border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        borderRadius: "8px",
      }}
    >
      <TextInput name="id" label="아이디" type="text" />
      <TextInput
        name="password"
        label="비밀번호"
        type="password"
        state="error"
        hint="입력하신 내용을 확인해 주세요."
      />
      <div style={{ display: "flex", justifyContent: "flex-end", marginTop: "16px" }}>
        <Button variant="primary">로그인</Button>
      </div>
    </div>
  );
}
```

```ts fw=angular
import { KrdsTextInputComponent, KrdsButtonComponent } from '@krds-community/angular';

// template
<div
  style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
>
  <krds-text-input name="id" label="아이디" type="text"></krds-text-input>
  <krds-text-input
    name="password"
    label="비밀번호"
    type="password"
    state="error"
    hint="입력하신 내용을 확인해 주세요."
  ></krds-text-input>
  <div style="display:flex;justify-content:flex-end;margin-top:16px">
    <krds-button variant="primary" label="로그인">로그인</krds-button>
  </div>
</div>
```

```astro fw=astro
---
import { TextInput, Button } from "@krds-community/astro";
---

<div
  style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
>
  <TextInput name="id" label="아이디" type="text" />
  <TextInput
    name="password"
    label="비밀번호"
    type="password"
    state="error"
    hint="입력하신 내용을 확인해 주세요."
  />
  <div style="display:flex;justify-content:flex-end;margin-top:16px">
    <Button variant="primary">로그인</Button>
  </div>
</div>
```

</FrameworkPreview>

**피해야 할 사례 2**

<FrameworkPreview
  kind="bad"
  title="오류 메시지는 명확하고 간결하게 제공한다."
>
  <div slot="react">
    <div
      style={{
        maxWidth: "360px",
        margin: "0 auto",
        padding: "24px",
        border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        borderRadius: "8px",
      }}
    >
      <ReactTextInput name="id" label="아이디" type="text" client:only="react" />
      <ReactTextInput
        name="password"
        label="비밀번호"
        type="password"
        state="error"
        hint="로그인 정보가 올바르지 않습니다. 아이디와 비밀번호를 다시 한번 확인해 주시고, 그래도 문제가 계속되면 관리자에게 문의해 주시기 바랍니다."
        client:only="react"
      />
      <div style={{ display: "flex", justifyContent: "flex-end", marginTop: "16px" }}>
        <ReactButton variant="primary" client:only="react">
          로그인
        </ReactButton>
      </div>
    </div>
  </div>
  <div slot="vue">
    <div
      style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
    >
      <VueTextInput name="id" label="아이디" type="text" client:only="vue" />
      <VueTextInput
        name="password"
        label="비밀번호"
        type="password"
        state="error"
        hint="로그인 정보가 올바르지 않습니다. 아이디와 비밀번호를 다시 한번 확인해 주시고, 그래도 문제가 계속되면 관리자에게 문의해 주시기 바랍니다."
        client:only="vue"
      />
      <div style="display:flex;justify-content:flex-end;margin-top:16px">
        <VueButton variant="primary" client:only="vue">
          로그인
        </VueButton>
      </div>
    </div>
  </div>
  <div slot="svelte">
    <div
      style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
    >
      <SvelteTextInput name="id" label="아이디" type="text" client:only="svelte" />
      <SvelteTextInput
        name="password"
        label="비밀번호"
        type="password"
        state="error"
        hint="로그인 정보가 올바르지 않습니다. 아이디와 비밀번호를 다시 한번 확인해 주시고, 그래도 문제가 계속되면 관리자에게 문의해 주시기 바랍니다."
        client:only="svelte"
      />
      <div style="display:flex;justify-content:flex-end;margin-top:16px">
        <SvelteButton variant="primary" client:only="svelte">
          로그인
        </SvelteButton>
      </div>
    </div>
  </div>
  <div slot="solid">
    <div
      style={{
        maxWidth: "360px",
        margin: "0 auto",
        padding: "24px",
        border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        borderRadius: "8px",
      }}
    >
      <SolidTextInput name="id" label="아이디" type="text" client:only="solid-js" />
      <SolidTextInput
        name="password"
        label="비밀번호"
        type="password"
        state="error"
        hint="로그인 정보가 올바르지 않습니다. 아이디와 비밀번호를 다시 한번 확인해 주시고, 그래도 문제가 계속되면 관리자에게 문의해 주시기 바랍니다."
        client:only="solid-js"
      />
      <div style={{ display: "flex", justifyContent: "flex-end", marginTop: "16px" }}>
        <SolidButton variant="primary" client:only="solid-js">
          로그인
        </SolidButton>
      </div>
    </div>
  </div>
  <div slot="astro">
    <div
      style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
    >
      <AstroTextInput name="id" label="아이디" type="text" />
      <AstroTextInput
        name="password"
        label="비밀번호"
        type="password"
        state="error"
        hint="로그인 정보가 올바르지 않습니다. 아이디와 비밀번호를 다시 한번 확인해 주시고, 그래도 문제가 계속되면 관리자에게 문의해 주시기 바랍니다."
      />
      <div style="display:flex;justify-content:flex-end;margin-top:16px">
        <AstroButton variant="primary">로그인</AstroButton>
      </div>
    </div>
  </div>
  <div slot="angular">
    <div
      style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
    >
      <AngularTextInput name="id" label="아이디" type="text" client:only="@analogjs/astro-angular" />
      <AngularTextInput
        name="password"
        label="비밀번호"
        type="password"
        state="error"
        hint="로그인 정보가 올바르지 않습니다. 아이디와 비밀번호를 다시 한번 확인해 주시고, 그래도 문제가 계속되면 관리자에게 문의해 주시기 바랍니다."
        client:only="@analogjs/astro-angular"
      />
      <div style="display:flex;justify-content:flex-end;margin-top:16px">
        <AngularButton
          variant="primary"
          label="로그인"
          client:only="@analogjs/astro-angular"
        >
          로그인
        </AngularButton>
      </div>
    </div>
  </div>

```tsx fw=react
import { TextInput, Button } from "@krds-community/react";

export function Example() {
  return (
    <div
      style={{
        maxWidth: "360px",
        margin: "0 auto",
        padding: "24px",
        border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        borderRadius: "8px",
      }}
    >
      <TextInput name="id" label="아이디" type="text" />
      <TextInput
        name="password"
        label="비밀번호"
        type="password"
        state="error"
        hint="로그인 정보가 올바르지 않습니다. 아이디와 비밀번호를 다시 한번 확인해 주시고, 그래도 문제가 계속되면 관리자에게 문의해 주시기 바랍니다."
      />
      <div style={{ display: "flex", justifyContent: "flex-end", marginTop: "16px" }}>
        <Button variant="primary">로그인</Button>
      </div>
    </div>
  );
}
```

```vue fw=vue
<script setup>
import { TextInput, Button } from "@krds-community/vue";
</script>

<template>
  <div
    style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
  >
    <TextInput name="id" label="아이디" type="text" />
    <TextInput
      name="password"
      label="비밀번호"
      type="password"
      state="error"
      hint="로그인 정보가 올바르지 않습니다. 아이디와 비밀번호를 다시 한번 확인해 주시고, 그래도 문제가 계속되면 관리자에게 문의해 주시기 바랍니다."
    />
    <div style="display:flex;justify-content:flex-end;margin-top:16px">
      <Button variant="primary">로그인</Button>
    </div>
  </div>
</template>
```

```svelte fw=svelte
<script>
import { TextInput, Button } from '@krds-community/svelte';
</script>

<div
  style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
>
  <TextInput name="id" label="아이디" type="text" />
  <TextInput
    name="password"
    label="비밀번호"
    type="password"
    state="error"
    hint="로그인 정보가 올바르지 않습니다. 아이디와 비밀번호를 다시 한번 확인해 주시고, 그래도 문제가 계속되면 관리자에게 문의해 주시기 바랍니다."
  />
  <div style="display:flex;justify-content:flex-end;margin-top:16px">
    <Button variant="primary">로그인</Button>
  </div>
</div>
```

```tsx fw=solid
import { TextInput, Button } from "@krds-community/solid";

export function Example() {
  return (
    <div
      style={{
        maxWidth: "360px",
        margin: "0 auto",
        padding: "24px",
        border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        borderRadius: "8px",
      }}
    >
      <TextInput name="id" label="아이디" type="text" />
      <TextInput
        name="password"
        label="비밀번호"
        type="password"
        state="error"
        hint="로그인 정보가 올바르지 않습니다. 아이디와 비밀번호를 다시 한번 확인해 주시고, 그래도 문제가 계속되면 관리자에게 문의해 주시기 바랍니다."
      />
      <div style={{ display: "flex", justifyContent: "flex-end", marginTop: "16px" }}>
        <Button variant="primary">로그인</Button>
      </div>
    </div>
  );
}
```

```ts fw=angular
import { KrdsTextInputComponent, KrdsButtonComponent } from '@krds-community/angular';

// template
<div
  style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
>
  <krds-text-input name="id" label="아이디" type="text"></krds-text-input>
  <krds-text-input
    name="password"
    label="비밀번호"
    type="password"
    state="error"
    hint="로그인 정보가 올바르지 않습니다. 아이디와 비밀번호를 다시 한번 확인해 주시고, 그래도 문제가 계속되면 관리자에게 문의해 주시기 바랍니다."
  ></krds-text-input>
  <div style="display:flex;justify-content:flex-end;margin-top:16px">
    <krds-button variant="primary" label="로그인">로그인</krds-button>
  </div>
</div>
```

```astro fw=astro
---
import { TextInput, Button } from "@krds-community/astro";
---

<div
  style="max-width:360px;margin:0 auto;padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
>
  <TextInput name="id" label="아이디" type="text" />
  <TextInput
    name="password"
    label="비밀번호"
    type="password"
    state="error"
    hint="로그인 정보가 올바르지 않습니다. 아이디와 비밀번호를 다시 한번 확인해 주시고, 그래도 문제가 계속되면 관리자에게 문의해 주시기 바랍니다."
  />
  <div style="display:flex;justify-content:flex-end;margin-top:16px">
    <Button variant="primary">로그인</Button>
  </div>
</div>
```

</FrameworkPreview>

**초점이 논리적인 순서로 이동할 수 있도록 제공한다.**
로그인 입력 양식의 시각적 배열에 상관없이 초점은 논리적인 순서대로 이동할 수 있도록 제공해야 한다. 비밀번호 입력 양식 이전에 로그인 버튼으로 초점이 이동하거나, 로그인 버튼 이후에 아이디 및 비밀번호 저장 옵션으로 초점이 이동해서는 안 된다.

**입력 필드에 자동 완성 속성을 설정한다.**
정보 입력에 필요한 사용자의 인지적, 신체적 노력을 최소화할 수 있도록 사용자 에이전트가 사용자가 기존에 입력한 정보를 활용할 수 있는 기술을 제공해야 한다. 웹사이트에서는 HTML 표준인 autocomplete 속성을 사용하여 입력 필드의 목적에 맞는 자동완성 정보를 사용자 에이전트에 전달할 수 있다.

**제출하기 전 가능한 한 많은 사용자 데이터의 유효성을 검사한다.**
실시간 유효성 검사는 입력 필드가 포커스를 잃을 때 발생하며 유효하지 않은 문자나 빈 필드와 같은 입력 오류를 확인해야 한다. 이를 통해 사용자는 로그인 양식을 제출하기 전에 실수를 쉽게 식별하고 수정할 수 있다.

**모범 사례**

<FrameworkPreview
  kind="good"
  title="실시간 유효성 검사는 입력 필드가 포커스를 잃을 때 발생한다"
>
  <div slot="react">
    <div style={{ display: "flex", flexDirection: "column", gap: "12px", maxWidth: "360px" }}>
      <ReactValidatedInput
        label="아이디"
        name="id"
        placeholder="아이디를 입력하세요"
        hint="영문 4자 이상 입력하세요."
        successMessage="사용 가능한 아이디입니다."
        validate="min-length:4"
        client:only="react"
      />
      <ReactValidatedInput
        label="비밀번호"
        name="password"
        type="password"
        placeholder="비밀번호를 입력하세요"
        validate="required"
        client:only="react"
      />
      <div style={{ display: "flex", justifyContent: "flex-end" }}>
        <ReactButton variant="primary" client:only="react">로그인</ReactButton>
      </div>
    </div>
  </div>
  <div slot="vue">
    <div style="display: flex; flex-direction: column; gap: 12px; max-width: 360px">
      <VueValidatedInput
        label="아이디"
        name="id"
        placeholder="아이디를 입력하세요"
        hint="영문 4자 이상 입력하세요."
        success-message="사용 가능한 아이디입니다."
        validate="min-length:4"
        client:only="vue"
      />
      <VueValidatedInput
        label="비밀번호"
        name="password"
        type="password"
        placeholder="비밀번호를 입력하세요"
        validate="required"
        client:only="vue"
      />
      <div style="display: flex; justify-content: flex-end">
        <VueButton variant="primary" client:only="vue">로그인</VueButton>
      </div>
    </div>
  </div>
  <div slot="svelte">
    <div style="display: flex; flex-direction: column; gap: 12px; max-width: 360px">
      <SvelteValidatedInput
        label="아이디"
        name="id"
        placeholder="아이디를 입력하세요"
        hint="영문 4자 이상 입력하세요."
        successMessage="사용 가능한 아이디입니다."
        validate="min-length:4"
        client:only="svelte"
      />
      <SvelteValidatedInput
        label="비밀번호"
        name="password"
        type="password"
        placeholder="비밀번호를 입력하세요"
        validate="required"
        client:only="svelte"
      />
      <div style="display: flex; justify-content: flex-end">
        <SvelteButton variant="primary" client:only="svelte">로그인</SvelteButton>
      </div>
    </div>
  </div>
  <div slot="solid">
    <div style={{ display: "flex", flexDirection: "column", gap: "12px", maxWidth: "360px" }}>
      <SolidValidatedInput
        label="아이디"
        name="id"
        placeholder="아이디를 입력하세요"
        hint="영문 4자 이상 입력하세요."
        successMessage="사용 가능한 아이디입니다."
        validate="min-length:4"
        client:only="solid-js"
      />
      <SolidValidatedInput
        label="비밀번호"
        name="password"
        type="password"
        placeholder="비밀번호를 입력하세요"
        validate="required"
        client:only="solid-js"
      />
      <div style={{ display: "flex", justifyContent: "flex-end" }}>
        <SolidButton variant="primary" client:only="solid-js">로그인</SolidButton>
      </div>
    </div>
  </div>
  <div slot="astro">
    <div style="display: flex; flex-direction: column; gap: 12px; max-width: 360px">
      <AstroValidatedInput
        label="아이디"
        name="id"
        placeholder="아이디를 입력하세요"
        hint="영문 4자 이상 입력하세요."
        successMessage="사용 가능한 아이디입니다."
        validate="min-length:4"
      />
      <AstroValidatedInput
        label="비밀번호"
        name="password"
        type="password"
        placeholder="비밀번호를 입력하세요"
        validate="required"
      />
      <div style="display: flex; justify-content: flex-end">
        <AstroButton variant="primary">로그인</AstroButton>
      </div>
    </div>
  </div>
  <div slot="angular">
    <div style="display: flex; flex-direction: column; gap: 12px; max-width: 360px">
      <AngularValidatedInput
        label="아이디"
        name="id"
        placeholder="아이디를 입력하세요"
        hint="영문 4자 이상 입력하세요."
        successMessage="사용 가능한 아이디입니다."
        validate="min-length:4"
        client:only="@analogjs/astro-angular"
      />
      <AngularValidatedInput
        label="비밀번호"
        name="password"
        type="password"
        placeholder="비밀번호를 입력하세요"
        validate="required"
        client:only="@analogjs/astro-angular"
      />
      <div style="display: flex; justify-content: flex-end">
        <AngularButton variant="primary" label="로그인" client:only="@analogjs/astro-angular">로그인</AngularButton>
      </div>
    </div>
  </div>

```tsx fw=react
import { Button } from "@krds-community/react";
import { ValidatedInput } from "@krds-community/react/extra";

export function Example() {
  const validateId = (value: string) =>
    value.trim().length >= 4
      ? { valid: true }
      : { valid: false, message: "4자 이상 입력해 주세요." };

  return (
    <div style={{ display: "flex", flexDirection: "column", gap: 12, maxWidth: 360 }}>
      <ValidatedInput
        label="아이디"
        name="id"
        placeholder="아이디를 입력하세요"
        hint="영문 4자 이상 입력하세요."
        successMessage="사용 가능한 아이디입니다."
        validate={validateId}
      />
      <ValidatedInput
        label="비밀번호"
        name="password"
        type="password"
        placeholder="비밀번호를 입력하세요"
        validate="required"
      />
      <div style={{ display: "flex", justifyContent: "flex-end" }}>
        <Button variant="primary">로그인</Button>
      </div>
    </div>
  );
}
```

```vue fw=vue
<script setup>
import { Button } from "@krds-community/vue";
import { ValidatedInput } from "@krds-community/vue/extra";

const validateId = (value) =>
  value.trim().length >= 4 ? { valid: true } : { valid: false, message: "4자 이상 입력해 주세요." };
</script>

<template>
  <div style="display: flex; flex-direction: column; gap: 12px; max-width: 360px">
    <ValidatedInput
      label="아이디"
      name="id"
      placeholder="아이디를 입력하세요"
      hint="영문 4자 이상 입력하세요."
      success-message="사용 가능한 아이디입니다."
      :validate="validateId"
    />
    <ValidatedInput
      label="비밀번호"
      name="password"
      type="password"
      placeholder="비밀번호를 입력하세요"
      validate="required"
    />
    <div style="display: flex; justify-content: flex-end">
      <Button variant="primary">로그인</Button>
    </div>
  </div>
</template>
```

```svelte fw=svelte
<script>
import { Button } from "@krds-community/svelte";
import { ValidatedInput } from "@krds-community/svelte/extra";

const validateId = (value) =>
  value.trim().length >= 4
    ? { valid: true }
    : { valid: false, message: "4자 이상 입력해 주세요." };
</script>

<div style="display: flex; flex-direction: column; gap: 12px; max-width: 360px">
  <ValidatedInput
    label="아이디"
    name="id"
    placeholder="아이디를 입력하세요"
    hint="영문 4자 이상 입력하세요."
    successMessage="사용 가능한 아이디입니다."
    validate={validateId}
  />
  <ValidatedInput
    label="비밀번호"
    name="password"
    type="password"
    placeholder="비밀번호를 입력하세요"
    validate="required"
  />
  <div style="display: flex; justify-content: flex-end">
    <Button variant="primary">로그인</Button>
  </div>
</div>
```

```tsx fw=solid
import { Button } from "@krds-community/solid";
import { ValidatedInput } from "@krds-community/solid/extra";

export function Example() {
  const validateId = (value: string) =>
    value.trim().length >= 4
      ? { valid: true }
      : { valid: false, message: "4자 이상 입력해 주세요." };

  return (
    <div style={{ display: "flex", flexDirection: "column", gap: 12, maxWidth: 360 }}>
      <ValidatedInput
        label="아이디"
        name="id"
        placeholder="아이디를 입력하세요"
        hint="영문 4자 이상 입력하세요."
        successMessage="사용 가능한 아이디입니다."
        validate={validateId}
      />
      <ValidatedInput
        label="비밀번호"
        name="password"
        type="password"
        placeholder="비밀번호를 입력하세요"
        validate="required"
      />
      <div style={{ display: "flex", justifyContent: "flex-end" }}>
        <Button variant="primary">로그인</Button>
      </div>
    </div>
  );
}
```

```ts fw=angular
import { Component } from "@angular/core";
import { KrdsButtonComponent } from "@krds-community/angular";
import { KrdsValidatedInputComponent } from "@krds-community/angular/extra";

@Component({
  selector: "app-example",
  imports: [KrdsValidatedInputComponent, KrdsButtonComponent],
  template: `
    <div style="display:flex;flex-direction:column;gap:12px;max-width:360px">
      <krds-validated-input
        label="아이디"
        name="id"
        placeholder="아이디를 입력하세요"
        hint="영문 4자 이상 입력하세요."
        successMessage="사용 가능한 아이디입니다."
        [validate]="validateId"
      ></krds-validated-input>
      <krds-validated-input
        label="비밀번호"
        name="password"
        type="password"
        placeholder="비밀번호를 입력하세요"
        validate="required"
      ></krds-validated-input>
      <div style="display:flex;justify-content:flex-end">
        <krds-button variant="primary" label="로그인"></krds-button>
      </div>
    </div>
  `,
})
export class Example {
  validateId = (value: string) =>
    value.trim().length >= 4
      ? { valid: true }
      : { valid: false, message: "4자 이상 입력해 주세요." };
}
```

```astro fw=astro
---
import { Button } from "@krds-community/astro";
import { ValidatedInput } from "@krds-community/astro/extra";
---

<div style="display: flex; flex-direction: column; gap: 12px; max-width: 360px">
  <ValidatedInput
    label="아이디"
    name="id"
    placeholder="아이디를 입력하세요"
    hint="영문 4자 이상 입력하세요."
    successMessage="사용 가능한 아이디입니다."
    validate="min-length:4"
  />
  <ValidatedInput
    label="비밀번호"
    name="password"
    type="password"
    placeholder="비밀번호를 입력하세요"
    validate="required"
  />
  <div style="display: flex; justify-content: flex-end">
    <Button variant="primary">로그인</Button>
  </div>
</div>
```

</FrameworkPreview>

**사용자가 로그인 폼을 제출하였을 때 오류가 발생한 경우 해당 항목으로 초점을 이동시킨다.**
서버 측 오류가 발생하면 화면을 다시 로드하고 비밀번호 필드를 지운 다음 사용자 이름 입력 필드로 사용자를 돌려보내야 한다. 인라인 메시지 등의 방식을 활용하여 오류를 표시하고 사용자가 문제를 해결하는 방법에 대한 명확한 지침을 제공한다.

**생채인식을 사용 할 경우 자동로그인 기능을 추가한다.**
모바일 앱에 특성 상 사용자의 생채인식을 사용할 경우 자동로그인 기능을 사용할 수 있도록 한다. 로그인 화면 하단 또는 성공 직후 안내 모달에서 로그인 유지 옵션을 제시하되, 기본값은 기관 보안정책에 따른다

### 로그인 완료

**권장: 로그인이 완료된 후, 로그인 상황에 적합한 화면으로 이동해야 한다.**
로그인 플로(Flow)에 진입하기 전의 사용자 여정이 단절되지 않도록 로그인을 시도한 상황에 적합한 화면으로 연결해야 한다.

**모범 사례**

<FrameworkPreview
  kind="good"
  title="로그인이 완료된 후, 로그인 상황에 적합한 화면으로 이동해야 한다."
>
  <div slot="react">
    <div>
      <ReactHeader
        logoLabel="서비스명"
        myMenu={{ label: "마이페이지", userName: "홍길동" }}
        aria-label="서비스 헤더"
        client:only="react"
      />
      <div style={{ padding: "24px", maxWidth: "720px", margin: "0 auto" }}>
        <h3 style={{ margin: "0 0 8px" }}>나의 신청 내역</h3>
        <p style={{ margin: "0", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
          로그인 전 탐색하던 화면으로 돌아와 작업을 이어갈 수 있습니다.
        </p>
      </div>
    </div>
  </div>
  <div slot="vue">
    <div>
      <VueHeader
        logoLabel="서비스명"
        myMenu={{ label: "마이페이지", userName: "홍길동" }}
        aria-label="서비스 헤더"
        client:only="vue"
      />
      <div style="padding:24px;max-width:720px;margin:0 auto">
        <h3 style="margin:0 0 8px">나의 신청 내역</h3>
        <p style="margin:0;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
          로그인 전 탐색하던 화면으로 돌아와 작업을 이어갈 수 있습니다.
        </p>
      </div>
    </div>
  </div>
  <div slot="svelte">
    <div>
      <SvelteHeader
        logoLabel="서비스명"
        myMenu={{ label: "마이페이지", userName: "홍길동" }}
        aria-label="서비스 헤더"
        client:only="svelte"
      />
      <div style="padding:24px;max-width:720px;margin:0 auto">
        <h3 style="margin:0 0 8px">나의 신청 내역</h3>
        <p style="margin:0;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
          로그인 전 탐색하던 화면으로 돌아와 작업을 이어갈 수 있습니다.
        </p>
      </div>
    </div>
  </div>
  <div slot="solid">
    <div>
      <SolidHeader
        logoLabel="서비스명"
        myMenu={{ label: "마이페이지", userName: "홍길동" }}
        aria-label="서비스 헤더"
        client:only="solid-js"
      />
      <div style={{ padding: "24px", maxWidth: "720px", margin: "0 auto" }}>
        <h3 style={{ margin: "0 0 8px" }}>나의 신청 내역</h3>
        <p style={{ margin: "0", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
          로그인 전 탐색하던 화면으로 돌아와 작업을 이어갈 수 있습니다.
        </p>
      </div>
    </div>
  </div>
  <div slot="astro">
    <div>
      <AstroHeader
        logoLabel="서비스명"
        myMenu={{ label: "마이페이지", userName: "홍길동" }}
        aria-label="서비스 헤더"
      />
      <div style="padding:24px;max-width:720px;margin:0 auto">
        <h3 style="margin:0 0 8px">나의 신청 내역</h3>
        <p style="margin:0;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
          로그인 전 탐색하던 화면으로 돌아와 작업을 이어갈 수 있습니다.
        </p>
      </div>
    </div>
  </div>
  <div slot="angular">
    <div>
      <AngularHeader
        logoLabel="서비스명"
        myMenu={{ label: "마이페이지", userName: "홍길동" }}
        aria-label="서비스 헤더"
        client:only="@analogjs/astro-angular"
      />
      <div style="padding:24px;max-width:720px;margin:0 auto">
        <h3 style="margin:0 0 8px">나의 신청 내역</h3>
        <p style="margin:0;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
          로그인 전 탐색하던 화면으로 돌아와 작업을 이어갈 수 있습니다.
        </p>
      </div>
    </div>
  </div>

```tsx fw=react
import { Header } from "@krds-community/react";

export function Example() {
  return (
    <div>
      <Header
        logoLabel="서비스명"
        myMenu={{ label: "마이페이지", userName: "홍길동" }}
        aria-label="서비스 헤더"
      />
      <div style={{ padding: "24px", maxWidth: "720px", margin: "0 auto" }}>
        <h3 style={{ margin: "0 0 8px" }}>나의 신청 내역</h3>
        <p style={{ margin: "0", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
          로그인 전 탐색하던 화면으로 돌아와 작업을 이어갈 수 있습니다.
        </p>
      </div>
    </div>
  );
}
```

```vue fw=vue
<script setup>
import { Header } from "@krds-community/vue";
</script>

<template>
  <div>
    <Header
      logoLabel="서비스명"
      :myMenu="{ label: '마이페이지', userName: '홍길동' }"
      aria-label="서비스 헤더"
    />
    <div style="padding:24px;max-width:720px;margin:0 auto">
      <h3 style="margin:0 0 8px">나의 신청 내역</h3>
      <p style="margin:0;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
        로그인 전 탐색하던 화면으로 돌아와 작업을 이어갈 수 있습니다.
      </p>
    </div>
  </div>
</template>
```

```svelte fw=svelte
<script>
import { Header } from '@krds-community/svelte';
</script>

<div>
  <Header
    logoLabel="서비스명"
    myMenu={{ label: "마이페이지", userName: "홍길동" }}
    aria-label="서비스 헤더"
  />
  <div style="padding:24px;max-width:720px;margin:0 auto">
    <h3 style="margin:0 0 8px">나의 신청 내역</h3>
    <p style="margin:0;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
      로그인 전 탐색하던 화면으로 돌아와 작업을 이어갈 수 있습니다.
    </p>
  </div>
</div>
```

```tsx fw=solid
import { Header } from "@krds-community/solid";

export function Example() {
  return (
    <div>
      <Header
        logoLabel="서비스명"
        myMenu={{ label: "마이페이지", userName: "홍길동" }}
        aria-label="서비스 헤더"
      />
      <div style={{ padding: "24px", maxWidth: "720px", margin: "0 auto" }}>
        <h3 style={{ margin: "0 0 8px" }}>나의 신청 내역</h3>
        <p style={{ margin: "0", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
          로그인 전 탐색하던 화면으로 돌아와 작업을 이어갈 수 있습니다.
        </p>
      </div>
    </div>
  );
}
```

```ts fw=angular
import { KrdsHeaderComponent } from '@krds-community/angular';

// template
<div>
  <krds-header
    logoLabel="서비스명"
    [myMenu]="{ label: '마이페이지', userName: '홍길동' }"
    aria-label="서비스 헤더"
  ></krds-header>
  <div style="padding:24px;max-width:720px;margin:0 auto">
    <h3 style="margin:0 0 8px">나의 신청 내역</h3>
    <p style="margin:0;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
      로그인 전 탐색하던 화면으로 돌아와 작업을 이어갈 수 있습니다.
    </p>
  </div>
</div>
```

```astro fw=astro
---
import { Header } from "@krds-community/astro";
---

<div>
  <Header
    logoLabel="서비스명"
    myMenu={{ label: "마이페이지", userName: "홍길동" }}
    aria-label="서비스 헤더"
  />
  <div style="padding:24px;max-width:720px;margin:0 auto">
    <h3 style="margin:0 0 8px">나의 신청 내역</h3>
    <p style="margin:0;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
      로그인 전 탐색하던 화면으로 돌아와 작업을 이어갈 수 있습니다.
    </p>
  </div>
</div>
```

</FrameworkPreview>

**피해야 할 사례**

<FrameworkPreview
  kind="bad"
  title="로그인이 완료된 후, 로그인 상황에 적합한 화면으로 이동해야 한다."
>
  <div slot="react">
    <div>
      <ReactHeader
        logoLabel="서비스명"
        myMenu={{ label: "마이페이지", userName: "홍길동" }}
        aria-label="서비스 헤더"
        client:only="react"
      />
      <div style={{ padding: "24px", maxWidth: "720px", margin: "0 auto" }}>
        <h3 style={{ margin: "0 0 8px" }}>메인 화면</h3>
        <p style={{ margin: "0", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
          로그인 전 이용 중이던 화면과 관계없이 메인 화면으로 이동합니다.
        </p>
      </div>
    </div>
  </div>
  <div slot="vue">
    <div>
      <VueHeader
        logoLabel="서비스명"
        myMenu={{ label: "마이페이지", userName: "홍길동" }}
        aria-label="서비스 헤더"
        client:only="vue"
      />
      <div style="padding:24px;max-width:720px;margin:0 auto">
        <h3 style="margin:0 0 8px">메인 화면</h3>
        <p style="margin:0;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
          로그인 전 이용 중이던 화면과 관계없이 메인 화면으로 이동합니다.
        </p>
      </div>
    </div>
  </div>
  <div slot="svelte">
    <div>
      <SvelteHeader
        logoLabel="서비스명"
        myMenu={{ label: "마이페이지", userName: "홍길동" }}
        aria-label="서비스 헤더"
        client:only="svelte"
      />
      <div style="padding:24px;max-width:720px;margin:0 auto">
        <h3 style="margin:0 0 8px">메인 화면</h3>
        <p style="margin:0;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
          로그인 전 이용 중이던 화면과 관계없이 메인 화면으로 이동합니다.
        </p>
      </div>
    </div>
  </div>
  <div slot="solid">
    <div>
      <SolidHeader
        logoLabel="서비스명"
        myMenu={{ label: "마이페이지", userName: "홍길동" }}
        aria-label="서비스 헤더"
        client:only="solid-js"
      />
      <div style={{ padding: "24px", maxWidth: "720px", margin: "0 auto" }}>
        <h3 style={{ margin: "0 0 8px" }}>메인 화면</h3>
        <p style={{ margin: "0", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
          로그인 전 이용 중이던 화면과 관계없이 메인 화면으로 이동합니다.
        </p>
      </div>
    </div>
  </div>
  <div slot="astro">
    <div>
      <AstroHeader
        logoLabel="서비스명"
        myMenu={{ label: "마이페이지", userName: "홍길동" }}
        aria-label="서비스 헤더"
      />
      <div style="padding:24px;max-width:720px;margin:0 auto">
        <h3 style="margin:0 0 8px">메인 화면</h3>
        <p style="margin:0;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
          로그인 전 이용 중이던 화면과 관계없이 메인 화면으로 이동합니다.
        </p>
      </div>
    </div>
  </div>
  <div slot="angular">
    <div>
      <AngularHeader
        logoLabel="서비스명"
        myMenu={{ label: "마이페이지", userName: "홍길동" }}
        aria-label="서비스 헤더"
        client:only="@analogjs/astro-angular"
      />
      <div style="padding:24px;max-width:720px;margin:0 auto">
        <h3 style="margin:0 0 8px">메인 화면</h3>
        <p style="margin:0;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
          로그인 전 이용 중이던 화면과 관계없이 메인 화면으로 이동합니다.
        </p>
      </div>
    </div>
  </div>

```tsx fw=react
import { Header } from "@krds-community/react";

export function Example() {
  return (
    <div>
      <Header
        logoLabel="서비스명"
        myMenu={{ label: "마이페이지", userName: "홍길동" }}
        aria-label="서비스 헤더"
      />
      <div style={{ padding: "24px", maxWidth: "720px", margin: "0 auto" }}>
        <h3 style={{ margin: "0 0 8px" }}>메인 화면</h3>
        <p style={{ margin: "0", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
          로그인 전 이용 중이던 화면과 관계없이 메인 화면으로 이동합니다.
        </p>
      </div>
    </div>
  );
}
```

```vue fw=vue
<script setup>
import { Header } from "@krds-community/vue";
</script>

<template>
  <div>
    <Header
      logoLabel="서비스명"
      :myMenu="{ label: '마이페이지', userName: '홍길동' }"
      aria-label="서비스 헤더"
    />
    <div style="padding:24px;max-width:720px;margin:0 auto">
      <h3 style="margin:0 0 8px">메인 화면</h3>
      <p style="margin:0;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
        로그인 전 이용 중이던 화면과 관계없이 메인 화면으로 이동합니다.
      </p>
    </div>
  </div>
</template>
```

```svelte fw=svelte
<script>
import { Header } from '@krds-community/svelte';
</script>

<div>
  <Header
    logoLabel="서비스명"
    myMenu={{ label: "마이페이지", userName: "홍길동" }}
    aria-label="서비스 헤더"
  />
  <div style="padding:24px;max-width:720px;margin:0 auto">
    <h3 style="margin:0 0 8px">메인 화면</h3>
    <p style="margin:0;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
      로그인 전 이용 중이던 화면과 관계없이 메인 화면으로 이동합니다.
    </p>
  </div>
</div>
```

```tsx fw=solid
import { Header } from "@krds-community/solid";

export function Example() {
  return (
    <div>
      <Header
        logoLabel="서비스명"
        myMenu={{ label: "마이페이지", userName: "홍길동" }}
        aria-label="서비스 헤더"
      />
      <div style={{ padding: "24px", maxWidth: "720px", margin: "0 auto" }}>
        <h3 style={{ margin: "0 0 8px" }}>메인 화면</h3>
        <p style={{ margin: "0", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
          로그인 전 이용 중이던 화면과 관계없이 메인 화면으로 이동합니다.
        </p>
      </div>
    </div>
  );
}
```

```ts fw=angular
import { KrdsHeaderComponent } from '@krds-community/angular';

// template
<div>
  <krds-header
    logoLabel="서비스명"
    [myMenu]="{ label: '마이페이지', userName: '홍길동' }"
    aria-label="서비스 헤더"
  ></krds-header>
  <div style="padding:24px;max-width:720px;margin:0 auto">
    <h3 style="margin:0 0 8px">메인 화면</h3>
    <p style="margin:0;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
      로그인 전 이용 중이던 화면과 관계없이 메인 화면으로 이동합니다.
    </p>
  </div>
</div>
```

```astro fw=astro
---
import { Header } from "@krds-community/astro";
---

<div>
  <Header
    logoLabel="서비스명"
    myMenu={{ label: "마이페이지", userName: "홍길동" }}
    aria-label="서비스 헤더"
  />
  <div style="padding:24px;max-width:720px;margin:0 auto">
    <h3 style="margin:0 0 8px">메인 화면</h3>
    <p style="margin:0;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
      로그인 전 이용 중이던 화면과 관계없이 메인 화면으로 이동합니다.
    </p>
  </div>
</div>
```

</FrameworkPreview>

**권장: 로그인 상태로 전환되었음을 명확하게 표현한다.**
로그인 기능이 사라지고 로그아웃 옵션을 표시하는 등의 방법으로 현재 로그인 상태에 있음을 사용자에게 알려주어야 한다. 마이페이지 버튼을 실행시켜 활성화된 레이어에 사용자 이름을 제공하면 로그인 상태를 표현하면서도 개인 정보를 보호할 수 있다.

**로그인 완료 후 화면이 로딩되었을 때 초점은 문서 가장 처음부터 접근되도록 한다.**
로그인 완료 후 화면이 로딩되었을 때 키보드 및 스크린 리더 초점은 문서 가장 처음부터 접근하도록 구현한다. 문서의 시작 영역이 아닌 헤더나 본문에 초점이 자동으로 이동하게 예측하지 못한 위치에서 탐색이 시작되어 사용자가 당황할 수 있다.

### 서비스 이용

사용자가 서비스를 이용하는 중에 전역적으로 제공되는 로그인 만료 안내로 모달 양식으로 제공된다. 사용자의 이용 속도가 느린 화면/단계에서 로그인 세션 만료 시 사용자가 작업 중인 데이터의 손실이 발생하는 등의 돌이킬 수 없는 결과로 이어질 수 있는 경우에 제공되는 안내 사항이다. 로그인 만료 안내 모달과는 달리 로그인 기능을 찾거나 로그인 정보를 입력하는 시점 또는 사용자가 특정 기능을 사용하기 시작하기 전에 사전적으로 세션 유지 시간 제한에 대해 안내함으로써 오류를 예방하기 위한 목적으로 사용한다.

**필수: 사용자에게 로그인 세션이 만료되기 전에 유지 시간에 제한이 있음을 안내한다.**
로그인 세션 유지 시간 등의 사유로 인해 서비스 이용 도중 로그아웃되는 상황이 발생할 수 있음을 사용자가 분명히 인지할 수 있도록 안내를 제공한다.

**모범 사례 1**

<FrameworkPreview
  kind="good"
  title="사용자에게 로그인 세션이 만료되기 전에 유지 시간에 제한이 있음을 안내한다."
>
  <div slot="react">
    <div style={{ maxWidth: "480px", margin: "0 auto" }}>
      <div
        role="status"
        style={{
          padding: "16px",
          marginBottom: "16px",
          border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
          borderRadius: "8px",
          backgroundColor: "var(--krds-mode-light-color-surface-subtle, #f7f7f7)",
        }}
      >
        <strong style={{ display: "block", marginBottom: "4px" }}>
          로그인 세션은 30분 동안 유지됩니다.
        </strong>
        <span style={{ fontSize: "14px", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
          시간이 지나면 자동으로 로그아웃될 수 있습니다.
        </span>
      </div>
      <ReactButton variant="primary" client:only="react">
        연장하기
      </ReactButton>
    </div>
  </div>
  <div slot="vue">
    <div style="max-width:480px;margin:0 auto">
      <div
        role="status"
        style="padding:16px;margin-bottom:16px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px;background-color:var(--krds-mode-light-color-surface-subtle,#f7f7f7)"
      >
        <strong style="display:block;margin-bottom:4px">
          로그인 세션은 30분 동안 유지됩니다.
        </strong>
        <span style="font-size:14px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
          시간이 지나면 자동으로 로그아웃될 수 있습니다.
        </span>
      </div>
      <VueButton variant="primary" client:only="vue">
        연장하기
      </VueButton>
    </div>
  </div>
  <div slot="svelte">
    <div style="max-width:480px;margin:0 auto">
      <div
        role="status"
        style="padding:16px;margin-bottom:16px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px;background-color:var(--krds-mode-light-color-surface-subtle,#f7f7f7)"
      >
        <strong style="display:block;margin-bottom:4px">
          로그인 세션은 30분 동안 유지됩니다.
        </strong>
        <span style="font-size:14px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
          시간이 지나면 자동으로 로그아웃될 수 있습니다.
        </span>
      </div>
      <SvelteButton variant="primary" client:only="svelte">
        연장하기
      </SvelteButton>
    </div>
  </div>
  <div slot="solid">
    <div style={{ maxWidth: "480px", margin: "0 auto" }}>
      <div
        role="status"
        style={{
          padding: "16px",
          marginBottom: "16px",
          border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
          borderRadius: "8px",
          backgroundColor: "var(--krds-mode-light-color-surface-subtle, #f7f7f7)",
        }}
      >
        <strong style={{ display: "block", marginBottom: "4px" }}>
          로그인 세션은 30분 동안 유지됩니다.
        </strong>
        <span style={{ fontSize: "14px", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
          시간이 지나면 자동으로 로그아웃될 수 있습니다.
        </span>
      </div>
      <SolidButton variant="primary" client:only="solid-js">
        연장하기
      </SolidButton>
    </div>
  </div>
  <div slot="astro">
    <div style="max-width:480px;margin:0 auto">
      <div
        role="status"
        style="padding:16px;margin-bottom:16px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px;background-color:var(--krds-mode-light-color-surface-subtle,#f7f7f7)"
      >
        <strong style="display:block;margin-bottom:4px">
          로그인 세션은 30분 동안 유지됩니다.
        </strong>
        <span style="font-size:14px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
          시간이 지나면 자동으로 로그아웃될 수 있습니다.
        </span>
      </div>
      <AstroButton variant="primary">연장하기</AstroButton>
    </div>
  </div>
  <div slot="angular">
    <div style="max-width:480px;margin:0 auto">
      <div
        role="status"
        style="padding:16px;margin-bottom:16px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px;background-color:var(--krds-mode-light-color-surface-subtle,#f7f7f7)"
      >
        <strong style="display:block;margin-bottom:4px">
          로그인 세션은 30분 동안 유지됩니다.
        </strong>
        <span style="font-size:14px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
          시간이 지나면 자동으로 로그아웃될 수 있습니다.
        </span>
      </div>
      <AngularButton
        variant="primary"
        label="연장하기"
        client:only="@analogjs/astro-angular"
      >
        연장하기
      </AngularButton>
    </div>
  </div>

```tsx fw=react
import { Button } from "@krds-community/react";

export function Example() {
  return (
    <div style={{ maxWidth: "480px", margin: "0 auto" }}>
      <div
        role="status"
        style={{
          padding: "16px",
          marginBottom: "16px",
          border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
          borderRadius: "8px",
          backgroundColor: "var(--krds-mode-light-color-surface-subtle, #f7f7f7)",
        }}
      >
        <strong style={{ display: "block", marginBottom: "4px" }}>
          로그인 세션은 30분 동안 유지됩니다.
        </strong>
        <span
          style={{ fontSize: "14px", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}
        >
          시간이 지나면 자동으로 로그아웃될 수 있습니다.
        </span>
      </div>
      <Button variant="primary">연장하기</Button>
    </div>
  );
}
```

```vue fw=vue
<script setup>
import { Button } from "@krds-community/vue";
</script>

<template>
  <div style="max-width:480px;margin:0 auto">
    <div
      role="status"
      style="padding:16px;margin-bottom:16px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px;background-color:var(--krds-mode-light-color-surface-subtle,#f7f7f7)"
    >
      <strong style="display:block;margin-bottom:4px"> 로그인 세션은 30분 동안 유지됩니다. </strong>
      <span style="font-size:14px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
        시간이 지나면 자동으로 로그아웃될 수 있습니다.
      </span>
    </div>
    <Button variant="primary">연장하기</Button>
  </div>
</template>
```

```svelte fw=svelte
<script>
import { Button } from '@krds-community/svelte';
</script>

<div style="max-width:480px;margin:0 auto">
  <div
    role="status"
    style="padding:16px;margin-bottom:16px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px;background-color:var(--krds-mode-light-color-surface-subtle,#f7f7f7)"
  >
    <strong style="display:block;margin-bottom:4px">
      로그인 세션은 30분 동안 유지됩니다.
    </strong>
    <span style="font-size:14px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
      시간이 지나면 자동으로 로그아웃될 수 있습니다.
    </span>
  </div>
  <Button variant="primary">연장하기</Button>
</div>
```

```tsx fw=solid
import { Button } from "@krds-community/solid";

export function Example() {
  return (
    <div style={{ maxWidth: "480px", margin: "0 auto" }}>
      <div
        role="status"
        style={{
          padding: "16px",
          marginBottom: "16px",
          border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
          borderRadius: "8px",
          backgroundColor: "var(--krds-mode-light-color-surface-subtle, #f7f7f7)",
        }}
      >
        <strong style={{ display: "block", marginBottom: "4px" }}>
          로그인 세션은 30분 동안 유지됩니다.
        </strong>
        <span
          style={{ fontSize: "14px", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}
        >
          시간이 지나면 자동으로 로그아웃될 수 있습니다.
        </span>
      </div>
      <Button variant="primary">연장하기</Button>
    </div>
  );
}
```

```ts fw=angular
import { KrdsButtonComponent } from '@krds-community/angular';

// template
<div style="max-width:480px;margin:0 auto">
  <div
    role="status"
    style="padding:16px;margin-bottom:16px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px;background-color:var(--krds-mode-light-color-surface-subtle,#f7f7f7)"
  >
    <strong style="display:block;margin-bottom:4px">
      로그인 세션은 30분 동안 유지됩니다.
    </strong>
    <span style="font-size:14px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
      시간이 지나면 자동으로 로그아웃될 수 있습니다.
    </span>
  </div>
  <krds-button variant="primary" label="연장하기">연장하기</krds-button>
</div>
```

```astro fw=astro
---
import { Button } from "@krds-community/astro";
---

<div style="max-width:480px;margin:0 auto">
  <div
    role="status"
    style="padding:16px;margin-bottom:16px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px;background-color:var(--krds-mode-light-color-surface-subtle,#f7f7f7)"
  >
    <strong style="display:block;margin-bottom:4px">
      로그인 세션은 30분 동안 유지됩니다.
    </strong>
    <span style="font-size:14px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
      시간이 지나면 자동으로 로그아웃될 수 있습니다.
    </span>
  </div>
  <Button variant="primary">연장하기</Button>
</div>
```

</FrameworkPreview>

**모범 사례 2**

<FrameworkPreview
  kind="good"
  title="사용자에게 로그인 세션이 만료되기 전에 유지 시간에 제한이 있음을 안내한다."
>
  <div slot="react">
    <div style={{ maxWidth: "480px", margin: "0 auto" }}>
      <p style={{ margin: "0 0 16px", fontSize: "14px", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
        작성 중인 내용 보호를 위해 로그인 세션은 30분 동안 유지됩니다. 작성 시간이 오래 걸릴 경우 중간에 저장해 주세요.
      </p>
      <div
        style={{
          padding: "24px",
          border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
          borderRadius: "8px",
        }}
      >
        <ReactTextInput name="title" label="제목" type="text" client:only="react" />
        <ReactTextInput name="content" label="내용" type="text" client:only="react" />
        <div style={{ display: "flex", justifyContent: "flex-end", marginTop: "16px" }}>
          <ReactButton variant="primary" client:only="react">
            저장
          </ReactButton>
        </div>
      </div>
    </div>
  </div>
  <div slot="vue">
    <div style="max-width:480px;margin:0 auto">
      <p style="margin:0 0 16px;font-size:14px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
        작성 중인 내용 보호를 위해 로그인 세션은 30분 동안 유지됩니다. 작성 시간이 오래 걸릴 경우 중간에 저장해 주세요.
      </p>
      <div
        style="padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
      >
        <VueTextInput name="title" label="제목" type="text" client:only="vue" />
        <VueTextInput name="content" label="내용" type="text" client:only="vue" />
        <div style="display:flex;justify-content:flex-end;margin-top:16px">
          <VueButton variant="primary" client:only="vue">
            저장
          </VueButton>
        </div>
      </div>
    </div>
  </div>
  <div slot="svelte">
    <div style="max-width:480px;margin:0 auto">
      <p style="margin:0 0 16px;font-size:14px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
        작성 중인 내용 보호를 위해 로그인 세션은 30분 동안 유지됩니다. 작성 시간이 오래 걸릴 경우 중간에 저장해 주세요.
      </p>
      <div
        style="padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
      >
        <SvelteTextInput name="title" label="제목" type="text" client:only="svelte" />
        <SvelteTextInput name="content" label="내용" type="text" client:only="svelte" />
        <div style="display:flex;justify-content:flex-end;margin-top:16px">
          <SvelteButton variant="primary" client:only="svelte">
            저장
          </SvelteButton>
        </div>
      </div>
    </div>
  </div>
  <div slot="solid">
    <div style={{ maxWidth: "480px", margin: "0 auto" }}>
      <p style={{ margin: "0 0 16px", fontSize: "14px", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
        작성 중인 내용 보호를 위해 로그인 세션은 30분 동안 유지됩니다. 작성 시간이 오래 걸릴 경우 중간에 저장해 주세요.
      </p>
      <div
        style={{
          padding: "24px",
          border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
          borderRadius: "8px",
        }}
      >
        <SolidTextInput name="title" label="제목" type="text" client:only="solid-js" />
        <SolidTextInput name="content" label="내용" type="text" client:only="solid-js" />
        <div style={{ display: "flex", justifyContent: "flex-end", marginTop: "16px" }}>
          <SolidButton variant="primary" client:only="solid-js">
            저장
          </SolidButton>
        </div>
      </div>
    </div>
  </div>
  <div slot="astro">
    <div style="max-width:480px;margin:0 auto">
      <p style="margin:0 0 16px;font-size:14px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
        작성 중인 내용 보호를 위해 로그인 세션은 30분 동안 유지됩니다. 작성 시간이 오래 걸릴 경우 중간에 저장해 주세요.
      </p>
      <div
        style="padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
      >
        <AstroTextInput name="title" label="제목" type="text" />
        <AstroTextInput name="content" label="내용" type="text" />
        <div style="display:flex;justify-content:flex-end;margin-top:16px">
          <AstroButton variant="primary">저장</AstroButton>
        </div>
      </div>
    </div>
  </div>
  <div slot="angular">
    <div style="max-width:480px;margin:0 auto">
      <p style="margin:0 0 16px;font-size:14px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
        작성 중인 내용 보호를 위해 로그인 세션은 30분 동안 유지됩니다. 작성 시간이 오래 걸릴 경우 중간에 저장해 주세요.
      </p>
      <div
        style="padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
      >
        <AngularTextInput name="title" label="제목" type="text" client:only="@analogjs/astro-angular" />
        <AngularTextInput name="content" label="내용" type="text" client:only="@analogjs/astro-angular" />
        <div style="display:flex;justify-content:flex-end;margin-top:16px">
          <AngularButton
            variant="primary"
            label="저장"
            client:only="@analogjs/astro-angular"
          >
            저장
          </AngularButton>
        </div>
      </div>
    </div>
  </div>

```tsx fw=react
import { TextInput, Button } from "@krds-community/react";

export function Example() {
  return (
    <div style={{ maxWidth: "480px", margin: "0 auto" }}>
      <p
        style={{
          margin: "0 0 16px",
          fontSize: "14px",
          color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)",
        }}
      >
        작성 중인 내용 보호를 위해 로그인 세션은 30분 동안 유지됩니다. 작성 시간이 오래 걸릴 경우
        중간에 저장해 주세요.
      </p>
      <div
        style={{
          padding: "24px",
          border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
          borderRadius: "8px",
        }}
      >
        <TextInput name="title" label="제목" type="text" />
        <TextInput name="content" label="내용" type="text" />
        <div style={{ display: "flex", justifyContent: "flex-end", marginTop: "16px" }}>
          <Button variant="primary">저장</Button>
        </div>
      </div>
    </div>
  );
}
```

```vue fw=vue
<script setup>
import { TextInput, Button } from "@krds-community/vue";
</script>

<template>
  <div style="max-width:480px;margin:0 auto">
    <p
      style="margin:0 0 16px;font-size:14px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)"
    >
      작성 중인 내용 보호를 위해 로그인 세션은 30분 동안 유지됩니다. 작성 시간이 오래 걸릴 경우
      중간에 저장해 주세요.
    </p>
    <div
      style="padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
    >
      <TextInput name="title" label="제목" type="text" />
      <TextInput name="content" label="내용" type="text" />
      <div style="display:flex;justify-content:flex-end;margin-top:16px">
        <Button variant="primary">저장</Button>
      </div>
    </div>
  </div>
</template>
```

```svelte fw=svelte
<script>
import { TextInput, Button } from '@krds-community/svelte';
</script>

<div style="max-width:480px;margin:0 auto">
  <p style="margin:0 0 16px;font-size:14px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
    작성 중인 내용 보호를 위해 로그인 세션은 30분 동안 유지됩니다. 작성 시간이 오래 걸릴 경우 중간에 저장해 주세요.
  </p>
  <div
    style="padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
  >
    <TextInput name="title" label="제목" type="text" />
    <TextInput name="content" label="내용" type="text" />
    <div style="display:flex;justify-content:flex-end;margin-top:16px">
      <Button variant="primary">저장</Button>
    </div>
  </div>
</div>
```

```tsx fw=solid
import { TextInput, Button } from "@krds-community/solid";

export function Example() {
  return (
    <div style={{ maxWidth: "480px", margin: "0 auto" }}>
      <p
        style={{
          margin: "0 0 16px",
          fontSize: "14px",
          color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)",
        }}
      >
        작성 중인 내용 보호를 위해 로그인 세션은 30분 동안 유지됩니다. 작성 시간이 오래 걸릴 경우
        중간에 저장해 주세요.
      </p>
      <div
        style={{
          padding: "24px",
          border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
          borderRadius: "8px",
        }}
      >
        <TextInput name="title" label="제목" type="text" />
        <TextInput name="content" label="내용" type="text" />
        <div style={{ display: "flex", justifyContent: "flex-end", marginTop: "16px" }}>
          <Button variant="primary">저장</Button>
        </div>
      </div>
    </div>
  );
}
```

```ts fw=angular
import { KrdsTextInputComponent, KrdsButtonComponent } from '@krds-community/angular';

// template
<div style="max-width:480px;margin:0 auto">
  <p style="margin:0 0 16px;font-size:14px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
    작성 중인 내용 보호를 위해 로그인 세션은 30분 동안 유지됩니다. 작성 시간이 오래 걸릴 경우 중간에 저장해 주세요.
  </p>
  <div
    style="padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
  >
    <krds-text-input name="title" label="제목" type="text"></krds-text-input>
    <krds-text-input name="content" label="내용" type="text"></krds-text-input>
    <div style="display:flex;justify-content:flex-end;margin-top:16px">
      <krds-button variant="primary" label="저장">저장</krds-button>
    </div>
  </div>
</div>
```

```astro fw=astro
---
import { TextInput, Button } from "@krds-community/astro";
---

<div style="max-width:480px;margin:0 auto">
  <p style="margin:0 0 16px;font-size:14px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
    작성 중인 내용 보호를 위해 로그인 세션은 30분 동안 유지됩니다. 작성 시간이 오래 걸릴 경우 중간에 저장해 주세요.
  </p>
  <div
    style="padding:24px;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
  >
    <TextInput name="title" label="제목" type="text" />
    <TextInput name="content" label="내용" type="text" />
    <div style="display:flex;justify-content:flex-end;margin-top:16px">
      <Button variant="primary">저장</Button>
    </div>
  </div>
</div>
```

</FrameworkPreview>

**권장: 로그인 세션 시간 만료에 대한 안내는 모달로 제공하여 사용자의 과업 맥락이 유지될 수 있도록 한다.**
탐색 중인 화면에 머무르고자 하는 사용자가 로그인 상태 연장 옵션을 선택했을 때 현재 화면이 유지됨을 인지할 수 있도록 모달 레이아웃을 이용하여 안내를 제공한다.

**모범 사례**

<FrameworkPreview
  kind="good"
  title="로그인 세션 시간 만료에 대한 안내는 모달로 제공하여 사용자의 과업 맥락이 유지될 수 있도록 한다."
>
  <div slot="react">
    <ReactModal
      open="true"
      title="로그인 세션이 곧 만료됩니다"
      description="보안을 위해 로그인 세션이 곧 만료됩니다. 시간을 연장하면 현재 화면에서 계속 이용할 수 있습니다."
      cancelLabel="로그아웃하기"
      confirmLabel="연장하기"
      client:only="react"
    />
  </div>
  <div slot="vue">
    <VueModal
      open="true"
      title="로그인 세션이 곧 만료됩니다"
      description="보안을 위해 로그인 세션이 곧 만료됩니다. 시간을 연장하면 현재 화면에서 계속 이용할 수 있습니다."
      cancelLabel="로그아웃하기"
      confirmLabel="연장하기"
      client:only="vue"
    />
  </div>
  <div slot="svelte">
    <SvelteModal
      open="true"
      title="로그인 세션이 곧 만료됩니다"
      description="보안을 위해 로그인 세션이 곧 만료됩니다. 시간을 연장하면 현재 화면에서 계속 이용할 수 있습니다."
      cancelLabel="로그아웃하기"
      confirmLabel="연장하기"
      client:only="svelte"
    />
  </div>
  <div slot="solid">
    <SolidModal
      open="true"
      title="로그인 세션이 곧 만료됩니다"
      description="보안을 위해 로그인 세션이 곧 만료됩니다. 시간을 연장하면 현재 화면에서 계속 이용할 수 있습니다."
      cancelLabel="로그아웃하기"
      confirmLabel="연장하기"
      client:only="solid-js"
    />
  </div>
  <div slot="astro">
    <AstroModal
      open="true"
      title="로그인 세션이 곧 만료됩니다"
      description="보안을 위해 로그인 세션이 곧 만료됩니다. 시간을 연장하면 현재 화면에서 계속 이용할 수 있습니다."
      cancelLabel="로그아웃하기"
      confirmLabel="연장하기"
    />
  </div>
  <div slot="angular">
    <AngularModal
      open="true"
      title="로그인 세션이 곧 만료됩니다"
      description="보안을 위해 로그인 세션이 곧 만료됩니다. 시간을 연장하면 현재 화면에서 계속 이용할 수 있습니다."
      cancelLabel="로그아웃하기"
      confirmLabel="연장하기"
      client:only="@analogjs/astro-angular"
    />
  </div>

```tsx fw=react
import { Modal } from "@krds-community/react";

export function Example() {
  return (
    <Modal
      open="true"
      title="로그인 세션이 곧 만료됩니다"
      description="보안을 위해 로그인 세션이 곧 만료됩니다. 시간을 연장하면 현재 화면에서 계속 이용할 수 있습니다."
      cancelLabel="로그아웃하기"
      confirmLabel="연장하기"
    />
  );
}
```

```vue fw=vue
<script setup>
import { Modal } from "@krds-community/vue";
</script>

<template>
  <Modal
    open="true"
    title="로그인 세션이 곧 만료됩니다"
    description="보안을 위해 로그인 세션이 곧 만료됩니다. 시간을 연장하면 현재 화면에서 계속 이용할 수 있습니다."
    cancelLabel="로그아웃하기"
    confirmLabel="연장하기"
  />
</template>
```

```svelte fw=svelte
<script>
import { Modal } from '@krds-community/svelte';
</script>

<Modal
  open="true"
  title="로그인 세션이 곧 만료됩니다"
  description="보안을 위해 로그인 세션이 곧 만료됩니다. 시간을 연장하면 현재 화면에서 계속 이용할 수 있습니다."
  cancelLabel="로그아웃하기"
  confirmLabel="연장하기"
/>
```

```tsx fw=solid
import { Modal } from "@krds-community/solid";

export function Example() {
  return (
    <Modal
      open="true"
      title="로그인 세션이 곧 만료됩니다"
      description="보안을 위해 로그인 세션이 곧 만료됩니다. 시간을 연장하면 현재 화면에서 계속 이용할 수 있습니다."
      cancelLabel="로그아웃하기"
      confirmLabel="연장하기"
    />
  );
}
```

```ts fw=angular
import { KrdsModalComponent } from '@krds-community/angular';

// template
<krds-modal
  open="true"
  title="로그인 세션이 곧 만료됩니다"
  description="보안을 위해 로그인 세션이 곧 만료됩니다. 시간을 연장하면 현재 화면에서 계속 이용할 수 있습니다."
  cancelLabel="로그아웃하기"
  confirmLabel="연장하기"
></krds-modal>
```

```astro fw=astro
---
import { Modal } from "@krds-community/astro";
---

<Modal
  open="true"
  title="로그인 세션이 곧 만료됩니다"
  description="보안을 위해 로그인 세션이 곧 만료됩니다. 시간을 연장하면 현재 화면에서 계속 이용할 수 있습니다."
  cancelLabel="로그아웃하기"
  confirmLabel="연장하기"
/>
```

</FrameworkPreview>

**필수: 로그인 세션 시간 만료에 대한 안내는 세션이 만료되기 최소 20초 전에 제공한다.**
사용자에 따라 로그인 만료 안내 내용을 확인하고 연장하기 버튼을 누르는 데 오랜 시간이 걸릴 수 있으므로 최소 20초 전에 안내 모달을 제공해야 한다. 시스템의 반응 속도가 느린 경우 더 긴 시간을 제공하는 것이 적절하다.

**필수: 로그인 만료 안내 모달에 시간 연장하기 버튼과 로그아웃하기 버튼을 제공한다.**
서비스를 계속 이용하고자 하는 사용자와 이용을 종료하고자 하는 사용자가 다음 행동을 선택할 수 있는 수단을 제공한다.

**모범 사례**

<FrameworkPreview
  kind="good"
  title="로그인 만료 안내 모달에 시간 연장하기 버튼과 로그아웃하기 버튼을 제공한다."
>
  <div slot="react">
    <ReactModal
      open="true"
      title="로그인 세션이 만료되었습니다"
      description="로그인 시간이 만료되었습니다. 시간을 연장하거나 로그아웃할 수 있습니다."
      cancelLabel="로그아웃하기"
      confirmLabel="시간 연장하기"
      client:only="react"
    />
  </div>
  <div slot="vue">
    <VueModal
      open="true"
      title="로그인 세션이 만료되었습니다"
      description="로그인 시간이 만료되었습니다. 시간을 연장하거나 로그아웃할 수 있습니다."
      cancelLabel="로그아웃하기"
      confirmLabel="시간 연장하기"
      client:only="vue"
    />
  </div>
  <div slot="svelte">
    <SvelteModal
      open="true"
      title="로그인 세션이 만료되었습니다"
      description="로그인 시간이 만료되었습니다. 시간을 연장하거나 로그아웃할 수 있습니다."
      cancelLabel="로그아웃하기"
      confirmLabel="시간 연장하기"
      client:only="svelte"
    />
  </div>
  <div slot="solid">
    <SolidModal
      open="true"
      title="로그인 세션이 만료되었습니다"
      description="로그인 시간이 만료되었습니다. 시간을 연장하거나 로그아웃할 수 있습니다."
      cancelLabel="로그아웃하기"
      confirmLabel="시간 연장하기"
      client:only="solid-js"
    />
  </div>
  <div slot="astro">
    <AstroModal
      open="true"
      title="로그인 세션이 만료되었습니다"
      description="로그인 시간이 만료되었습니다. 시간을 연장하거나 로그아웃할 수 있습니다."
      cancelLabel="로그아웃하기"
      confirmLabel="시간 연장하기"
    />
  </div>
  <div slot="angular">
    <AngularModal
      open="true"
      title="로그인 세션이 만료되었습니다"
      description="로그인 시간이 만료되었습니다. 시간을 연장하거나 로그아웃할 수 있습니다."
      cancelLabel="로그아웃하기"
      confirmLabel="시간 연장하기"
      client:only="@analogjs/astro-angular"
    />
  </div>

```tsx fw=react
import { Modal } from "@krds-community/react";

export function Example() {
  return (
    <Modal
      open="true"
      title="로그인 세션이 만료되었습니다"
      description="로그인 시간이 만료되었습니다. 시간을 연장하거나 로그아웃할 수 있습니다."
      cancelLabel="로그아웃하기"
      confirmLabel="시간 연장하기"
    />
  );
}
```

```vue fw=vue
<script setup>
import { Modal } from "@krds-community/vue";
</script>

<template>
  <Modal
    open="true"
    title="로그인 세션이 만료되었습니다"
    description="로그인 시간이 만료되었습니다. 시간을 연장하거나 로그아웃할 수 있습니다."
    cancelLabel="로그아웃하기"
    confirmLabel="시간 연장하기"
  />
</template>
```

```svelte fw=svelte
<script>
import { Modal } from '@krds-community/svelte';
</script>

<Modal
  open="true"
  title="로그인 세션이 만료되었습니다"
  description="로그인 시간이 만료되었습니다. 시간을 연장하거나 로그아웃할 수 있습니다."
  cancelLabel="로그아웃하기"
  confirmLabel="시간 연장하기"
/>
```

```tsx fw=solid
import { Modal } from "@krds-community/solid";

export function Example() {
  return (
    <Modal
      open="true"
      title="로그인 세션이 만료되었습니다"
      description="로그인 시간이 만료되었습니다. 시간을 연장하거나 로그아웃할 수 있습니다."
      cancelLabel="로그아웃하기"
      confirmLabel="시간 연장하기"
    />
  );
}
```

```ts fw=angular
import { KrdsModalComponent } from '@krds-community/angular';

// template
<krds-modal
  open="true"
  title="로그인 세션이 만료되었습니다"
  description="로그인 시간이 만료되었습니다. 시간을 연장하거나 로그아웃할 수 있습니다."
  cancelLabel="로그아웃하기"
  confirmLabel="시간 연장하기"
></krds-modal>
```

```astro fw=astro
---
import { Modal } from "@krds-community/astro";
---

<Modal
  open="true"
  title="로그인 세션이 만료되었습니다"
  description="로그인 시간이 만료되었습니다. 시간을 연장하거나 로그아웃할 수 있습니다."
  cancelLabel="로그아웃하기"
  confirmLabel="시간 연장하기"
/>
```

</FrameworkPreview>

**피해야 할 사례**

<FrameworkPreview
  kind="bad"
  title="로그인 만료 안내 모달에 시간 연장하기 버튼과 로그아웃하기 버튼을 제공한다."
>
  <div slot="react">
    <ReactModal
      open="true"
      title="로그인 세션이 만료되었습니다"
      description="로그인 시간이 만료되었습니다."
      confirmLabel="시간 연장하기"
      client:only="react"
    />
  </div>
  <div slot="vue">
    <VueModal
      open="true"
      title="로그인 세션이 만료되었습니다"
      description="로그인 시간이 만료되었습니다."
      confirmLabel="시간 연장하기"
      client:only="vue"
    />
  </div>
  <div slot="svelte">
    <SvelteModal
      open="true"
      title="로그인 세션이 만료되었습니다"
      description="로그인 시간이 만료되었습니다."
      confirmLabel="시간 연장하기"
      client:only="svelte"
    />
  </div>
  <div slot="solid">
    <SolidModal
      open="true"
      title="로그인 세션이 만료되었습니다"
      description="로그인 시간이 만료되었습니다."
      confirmLabel="시간 연장하기"
      client:only="solid-js"
    />
  </div>
  <div slot="astro">
    <AstroModal
      open="true"
      title="로그인 세션이 만료되었습니다"
      description="로그인 시간이 만료되었습니다."
      confirmLabel="시간 연장하기"
    />
  </div>
  <div slot="angular">
    <AngularModal
      open="true"
      title="로그인 세션이 만료되었습니다"
      description="로그인 시간이 만료되었습니다."
      confirmLabel="시간 연장하기"
      client:only="@analogjs/astro-angular"
    />
  </div>

```tsx fw=react
import { Modal } from "@krds-community/react";

export function Example() {
  return (
    <Modal
      open="true"
      title="로그인 세션이 만료되었습니다"
      description="로그인 시간이 만료되었습니다."
      confirmLabel="시간 연장하기"
    />
  );
}
```

```vue fw=vue
<script setup>
import { Modal } from "@krds-community/vue";
</script>

<template>
  <Modal
    open="true"
    title="로그인 세션이 만료되었습니다"
    description="로그인 시간이 만료되었습니다."
    confirmLabel="시간 연장하기"
  />
</template>
```

```svelte fw=svelte
<script>
import { Modal } from '@krds-community/svelte';
</script>

<Modal
  open="true"
  title="로그인 세션이 만료되었습니다"
  description="로그인 시간이 만료되었습니다."
  confirmLabel="시간 연장하기"
/>
```

```tsx fw=solid
import { Modal } from "@krds-community/solid";

export function Example() {
  return (
    <Modal
      open="true"
      title="로그인 세션이 만료되었습니다"
      description="로그인 시간이 만료되었습니다."
      confirmLabel="시간 연장하기"
    />
  );
}
```

```ts fw=angular
import { KrdsModalComponent } from '@krds-community/angular';

// template
<krds-modal
  open="true"
  title="로그인 세션이 만료되었습니다"
  description="로그인 시간이 만료되었습니다."
  confirmLabel="시간 연장하기"
></krds-modal>
```

```astro fw=astro
---
import { Modal } from "@krds-community/astro";
---

<Modal
  open="true"
  title="로그인 세션이 만료되었습니다"
  description="로그인 시간이 만료되었습니다."
  confirmLabel="시간 연장하기"
/>
```

</FrameworkPreview>

**로그인 만료 안내 모달이 활성화된 상태에서 아무 키나 눌렀을 때 연장 가능하도록 한다.**
로그인 만료 안내 모달이 활성화된 상태에서 반드시 '연장하기' 버튼을 누르지 않더라도 아무 키나 눌렀을 때 시간이 연장되도록 하는 방안을 고려한다. 사용자마다 시간 연장 버튼을 누르는 데 필요한 노력의 정도가 다를 수 있으므로 가능한 한 단순한 방법으로 로그인 연장 기능을 실행할 수 있는 방안을 제공하는 것을 고려한다.

**시간 연장은 최소 10회 이상 수행할 수 있도록 제공한다.**
사용자에 따라 서비스 이용에 필요한 시간이 다를 수 있음을 고려하여 로그인 시간 연장은 최소 10번 이상 실행할 수 있도록 한다.

### 로그아웃

서비스의 이용을 완전하게 종료하기로 결정한 사용자 또는 로그인 상태를 해제하고자 하는 사용자가 로그아웃을 시도한 상황, 서비스 이용 중 로그인 세션 시간이 만료되어 로그인 만료 안내 모달 출현 후 로그아웃이 실행된 상황에서 사용한다.

**권장: 로그아웃 상태로 전환되었음을 분명하게 보여준다.**
로그인 실행 버튼의 레이블을 ‘로그인’으로 전환하는 등의 방법으로 로그아웃 상태에 있음을 사용자에게 알려주어야 한다.

**우수: 사용자 요청에 의해 로그아웃이 실행되는 경우 결과 화면과 로그아웃 실행 사이에 스피너를 제공한다.**
시스템이 사용자의 요청에 따라 로그아웃 동작을 실행하는 반응이 진행되고 있음을 사용자가 인지할 수 있도록 의도적으로 스피너를 통해 피드백을 제공한다.

**권장: 로그아웃이 완료된 후, 상황에 적합한 화면으로 이동해야 한다.**
사용자가 의도적으로 로그아웃 버튼을 눌러 로그아웃한 경우, 메인 화면으로 연결되어야 한다. 이용 세션 만료에 의해 로그아웃된 경우, 로그아웃 완료 화면으로 연결되어야 한다.

**모범 사례 1**

<FrameworkPreview
  kind="good"
  title="로그아웃이 완료된 후, 상황에 적합한 화면으로 이동해야 한다."
>
  <div slot="react">
    <div>
      <ReactHeader
        logoLabel="서비스명"
        loginLabel="로그인"
        loginHref="#login"
        aria-label="서비스 헤더"
        client:only="react"
      />
      <div style={{ padding: "24px", maxWidth: "720px", margin: "0 auto" }}>
        <h3 style={{ margin: "0 0 8px" }}>메인 화면</h3>
        <p style={{ margin: "0", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
          의도적으로 로그아웃한 경우 메인 화면으로 연결합니다.
        </p>
      </div>
    </div>
  </div>
  <div slot="vue">
    <div>
      <VueHeader
        logoLabel="서비스명"
        loginLabel="로그인"
        loginHref="#login"
        aria-label="서비스 헤더"
        client:only="vue"
      />
      <div style="padding:24px;max-width:720px;margin:0 auto">
        <h3 style="margin:0 0 8px">메인 화면</h3>
        <p style="margin:0;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
          의도적으로 로그아웃한 경우 메인 화면으로 연결합니다.
        </p>
      </div>
    </div>
  </div>
  <div slot="svelte">
    <div>
      <SvelteHeader
        logoLabel="서비스명"
        loginLabel="로그인"
        loginHref="#login"
        aria-label="서비스 헤더"
        client:only="svelte"
      />
      <div style="padding:24px;max-width:720px;margin:0 auto">
        <h3 style="margin:0 0 8px">메인 화면</h3>
        <p style="margin:0;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
          의도적으로 로그아웃한 경우 메인 화면으로 연결합니다.
        </p>
      </div>
    </div>
  </div>
  <div slot="solid">
    <div>
      <SolidHeader
        logoLabel="서비스명"
        loginLabel="로그인"
        loginHref="#login"
        aria-label="서비스 헤더"
        client:only="solid-js"
      />
      <div style={{ padding: "24px", maxWidth: "720px", margin: "0 auto" }}>
        <h3 style={{ margin: "0 0 8px" }}>메인 화면</h3>
        <p style={{ margin: "0", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
          의도적으로 로그아웃한 경우 메인 화면으로 연결합니다.
        </p>
      </div>
    </div>
  </div>
  <div slot="astro">
    <div>
      <AstroHeader
        logoLabel="서비스명"
        loginLabel="로그인"
        loginHref="#login"
        aria-label="서비스 헤더"
      />
      <div style="padding:24px;max-width:720px;margin:0 auto">
        <h3 style="margin:0 0 8px">메인 화면</h3>
        <p style="margin:0;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
          의도적으로 로그아웃한 경우 메인 화면으로 연결합니다.
        </p>
      </div>
    </div>
  </div>
  <div slot="angular">
    <div>
      <AngularHeader
        logoLabel="서비스명"
        loginLabel="로그인"
        loginHref="#login"
        aria-label="서비스 헤더"
        client:only="@analogjs/astro-angular"
      />
      <div style="padding:24px;max-width:720px;margin:0 auto">
        <h3 style="margin:0 0 8px">메인 화면</h3>
        <p style="margin:0;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
          의도적으로 로그아웃한 경우 메인 화면으로 연결합니다.
        </p>
      </div>
    </div>
  </div>

```tsx fw=react
import { Header } from "@krds-community/react";

export function Example() {
  return (
    <div>
      <Header
        logoLabel="서비스명"
        loginLabel="로그인"
        loginHref="#login"
        aria-label="서비스 헤더"
      />
      <div style={{ padding: "24px", maxWidth: "720px", margin: "0 auto" }}>
        <h3 style={{ margin: "0 0 8px" }}>메인 화면</h3>
        <p style={{ margin: "0", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
          의도적으로 로그아웃한 경우 메인 화면으로 연결합니다.
        </p>
      </div>
    </div>
  );
}
```

```vue fw=vue
<script setup>
import { Header } from "@krds-community/vue";
</script>

<template>
  <div>
    <Header logoLabel="서비스명" loginLabel="로그인" loginHref="#login" aria-label="서비스 헤더" />
    <div style="padding:24px;max-width:720px;margin:0 auto">
      <h3 style="margin:0 0 8px">메인 화면</h3>
      <p style="margin:0;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
        의도적으로 로그아웃한 경우 메인 화면으로 연결합니다.
      </p>
    </div>
  </div>
</template>
```

```svelte fw=svelte
<script>
import { Header } from '@krds-community/svelte';
</script>

<div>
  <Header
    logoLabel="서비스명"
    loginLabel="로그인"
    loginHref="#login"
    aria-label="서비스 헤더"
  />
  <div style="padding:24px;max-width:720px;margin:0 auto">
    <h3 style="margin:0 0 8px">메인 화면</h3>
    <p style="margin:0;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
      의도적으로 로그아웃한 경우 메인 화면으로 연결합니다.
    </p>
  </div>
</div>
```

```tsx fw=solid
import { Header } from "@krds-community/solid";

export function Example() {
  return (
    <div>
      <Header
        logoLabel="서비스명"
        loginLabel="로그인"
        loginHref="#login"
        aria-label="서비스 헤더"
      />
      <div style={{ padding: "24px", maxWidth: "720px", margin: "0 auto" }}>
        <h3 style={{ margin: "0 0 8px" }}>메인 화면</h3>
        <p style={{ margin: "0", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
          의도적으로 로그아웃한 경우 메인 화면으로 연결합니다.
        </p>
      </div>
    </div>
  );
}
```

```ts fw=angular
import { KrdsHeaderComponent } from '@krds-community/angular';

// template
<div>
  <krds-header
    logoLabel="서비스명"
    loginLabel="로그인"
    loginHref="#login"
    aria-label="서비스 헤더"
  ></krds-header>
  <div style="padding:24px;max-width:720px;margin:0 auto">
    <h3 style="margin:0 0 8px">메인 화면</h3>
    <p style="margin:0;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
      의도적으로 로그아웃한 경우 메인 화면으로 연결합니다.
    </p>
  </div>
</div>
```

```astro fw=astro
---
import { Header } from "@krds-community/astro";
---

<div>
  <Header
    logoLabel="서비스명"
    loginLabel="로그인"
    loginHref="#login"
    aria-label="서비스 헤더"
  />
  <div style="padding:24px;max-width:720px;margin:0 auto">
    <h3 style="margin:0 0 8px">메인 화면</h3>
    <p style="margin:0;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
      의도적으로 로그아웃한 경우 메인 화면으로 연결합니다.
    </p>
  </div>
</div>
```

</FrameworkPreview>

**모범 사례 2**

<FrameworkPreview
  kind="good"
  title="로그아웃이 완료된 후, 상황에 적합한 화면으로 이동해야 한다."
>
  <div slot="react">
    <div
      style={{
        maxWidth: "480px",
        margin: "0 auto",
        padding: "48px 24px",
        textAlign: "center",
      }}
    >
      <h3 style={{ margin: "0 0 8px" }}>로그아웃되었습니다</h3>
      <p style={{ margin: "0 0 24px", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
        로그인 세션이 만료되어 로그아웃되었습니다.
      </p>
      <div style={{ display: "flex", justifyContent: "center", gap: "8px" }}>
        <ReactButton variant="primary" client:only="react">
          다시 로그인하기
        </ReactButton>
      </div>
    </div>
  </div>
  <div slot="vue">
    <div
      style="max-width:480px;margin:0 auto;padding:48px 24px;text-align:center"
    >
      <h3 style="margin:0 0 8px">로그아웃되었습니다</h3>
      <p style="margin:0 0 24px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
        로그인 세션이 만료되어 로그아웃되었습니다.
      </p>
      <div style="display:flex;justify-content:center;gap:8px">
        <VueButton variant="primary" client:only="vue">
          다시 로그인하기
        </VueButton>
      </div>
    </div>
  </div>
  <div slot="svelte">
    <div
      style="max-width:480px;margin:0 auto;padding:48px 24px;text-align:center"
    >
      <h3 style="margin:0 0 8px">로그아웃되었습니다</h3>
      <p style="margin:0 0 24px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
        로그인 세션이 만료되어 로그아웃되었습니다.
      </p>
      <div style="display:flex;justify-content:center;gap:8px">
        <SvelteButton variant="primary" client:only="svelte">
          다시 로그인하기
        </SvelteButton>
      </div>
    </div>
  </div>
  <div slot="solid">
    <div
      style={{
        maxWidth: "480px",
        margin: "0 auto",
        padding: "48px 24px",
        textAlign: "center",
      }}
    >
      <h3 style={{ margin: "0 0 8px" }}>로그아웃되었습니다</h3>
      <p style={{ margin: "0 0 24px", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
        로그인 세션이 만료되어 로그아웃되었습니다.
      </p>
      <div style={{ display: "flex", justifyContent: "center", gap: "8px" }}>
        <SolidButton variant="primary" client:only="solid-js">
          다시 로그인하기
        </SolidButton>
      </div>
    </div>
  </div>
  <div slot="astro">
    <div
      style="max-width:480px;margin:0 auto;padding:48px 24px;text-align:center"
    >
      <h3 style="margin:0 0 8px">로그아웃되었습니다</h3>
      <p style="margin:0 0 24px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
        로그인 세션이 만료되어 로그아웃되었습니다.
      </p>
      <div style="display:flex;justify-content:center;gap:8px">
        <AstroButton variant="primary">다시 로그인하기</AstroButton>
      </div>
    </div>
  </div>
  <div slot="angular">
    <div
      style="max-width:480px;margin:0 auto;padding:48px 24px;text-align:center"
    >
      <h3 style="margin:0 0 8px">로그아웃되었습니다</h3>
      <p style="margin:0 0 24px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
        로그인 세션이 만료되어 로그아웃되었습니다.
      </p>
      <div style="display:flex;justify-content:center;gap:8px">
        <AngularButton
          variant="primary"
          label="다시 로그인하기"
          client:only="@analogjs/astro-angular"
        >
          다시 로그인하기
        </AngularButton>
      </div>
    </div>
  </div>

```tsx fw=react
import { Button } from "@krds-community/react";

export function Example() {
  return (
    <div
      style={{
        maxWidth: "480px",
        margin: "0 auto",
        padding: "48px 24px",
        textAlign: "center",
      }}
    >
      <h3 style={{ margin: "0 0 8px" }}>로그아웃되었습니다</h3>
      <p style={{ margin: "0 0 24px", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
        로그인 세션이 만료되어 로그아웃되었습니다.
      </p>
      <div style={{ display: "flex", justifyContent: "center", gap: "8px" }}>
        <Button variant="primary">다시 로그인하기</Button>
      </div>
    </div>
  );
}
```

```vue fw=vue
<script setup>
import { Button } from "@krds-community/vue";
</script>

<template>
  <div style="max-width:480px;margin:0 auto;padding:48px 24px;text-align:center">
    <h3 style="margin:0 0 8px">로그아웃되었습니다</h3>
    <p style="margin:0 0 24px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
      로그인 세션이 만료되어 로그아웃되었습니다.
    </p>
    <div style="display:flex;justify-content:center;gap:8px">
      <Button variant="primary">다시 로그인하기</Button>
    </div>
  </div>
</template>
```

```svelte fw=svelte
<script>
import { Button } from '@krds-community/svelte';
</script>

<div
  style="max-width:480px;margin:0 auto;padding:48px 24px;text-align:center"
>
  <h3 style="margin:0 0 8px">로그아웃되었습니다</h3>
  <p style="margin:0 0 24px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
    로그인 세션이 만료되어 로그아웃되었습니다.
  </p>
  <div style="display:flex;justify-content:center;gap:8px">
    <Button variant="primary">다시 로그인하기</Button>
  </div>
</div>
```

```tsx fw=solid
import { Button } from "@krds-community/solid";

export function Example() {
  return (
    <div
      style={{
        maxWidth: "480px",
        margin: "0 auto",
        padding: "48px 24px",
        textAlign: "center",
      }}
    >
      <h3 style={{ margin: "0 0 8px" }}>로그아웃되었습니다</h3>
      <p style={{ margin: "0 0 24px", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
        로그인 세션이 만료되어 로그아웃되었습니다.
      </p>
      <div style={{ display: "flex", justifyContent: "center", gap: "8px" }}>
        <Button variant="primary">다시 로그인하기</Button>
      </div>
    </div>
  );
}
```

```ts fw=angular
import { KrdsButtonComponent } from '@krds-community/angular';

// template
<div
  style="max-width:480px;margin:0 auto;padding:48px 24px;text-align:center"
>
  <h3 style="margin:0 0 8px">로그아웃되었습니다</h3>
  <p style="margin:0 0 24px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
    로그인 세션이 만료되어 로그아웃되었습니다.
  </p>
  <div style="display:flex;justify-content:center;gap:8px">
    <krds-button variant="primary" label="다시 로그인하기">다시 로그인하기</krds-button>
  </div>
</div>
```

```astro fw=astro
---
import { Button } from "@krds-community/astro";
---

<div
  style="max-width:480px;margin:0 auto;padding:48px 24px;text-align:center"
>
  <h3 style="margin:0 0 8px">로그아웃되었습니다</h3>
  <p style="margin:0 0 24px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
    로그인 세션이 만료되어 로그아웃되었습니다.
  </p>
  <div style="display:flex;justify-content:center;gap:8px">
    <Button variant="primary">다시 로그인하기</Button>
  </div>
</div>
```

</FrameworkPreview>

**우수: 로그아웃에 대한 안내는 별도의 화면으로 구성하여 제공한다.**
로그아웃 상태로 이용 맥락이 완전히 변경되었음을 직관적으로 인지할 수 있도록 별도 화면에서 안내를 제공한다.

**모범 사례**

<FrameworkPreview
  kind="good"
  title="로그아웃에 대한 안내는 별도의 화면으로 구성하여 제공한다."
>
  <div slot="react">
    <div
      style={{
        maxWidth: "480px",
        margin: "0 auto",
        padding: "48px 24px",
        textAlign: "center",
        border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        borderRadius: "8px",
      }}
    >
      <h3 style={{ margin: "0 0 8px" }}>로그아웃되었습니다</h3>
      <p style={{ margin: "0", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
        다음에 또 만나요. 로그아웃 상태로 이용 맥락이 전환되었습니다.
      </p>
    </div>
  </div>
  <div slot="vue">
    <div
      style="max-width:480px;margin:0 auto;padding:48px 24px;text-align:center;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
    >
      <h3 style="margin:0 0 8px">로그아웃되었습니다</h3>
      <p style="margin:0;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
        다음에 또 만나요. 로그아웃 상태로 이용 맥락이 전환되었습니다.
      </p>
    </div>
  </div>
  <div slot="svelte">
    <div
      style="max-width:480px;margin:0 auto;padding:48px 24px;text-align:center;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
    >
      <h3 style="margin:0 0 8px">로그아웃되었습니다</h3>
      <p style="margin:0;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
        다음에 또 만나요. 로그아웃 상태로 이용 맥락이 전환되었습니다.
      </p>
    </div>
  </div>
  <div slot="solid">
    <div
      style={{
        maxWidth: "480px",
        margin: "0 auto",
        padding: "48px 24px",
        textAlign: "center",
        border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        borderRadius: "8px",
      }}
    >
      <h3 style={{ margin: "0 0 8px" }}>로그아웃되었습니다</h3>
      <p style={{ margin: "0", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
        다음에 또 만나요. 로그아웃 상태로 이용 맥락이 전환되었습니다.
      </p>
    </div>
  </div>
  <div slot="astro">
    <div
      style="max-width:480px;margin:0 auto;padding:48px 24px;text-align:center;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
    >
      <h3 style="margin:0 0 8px">로그아웃되었습니다</h3>
      <p style="margin:0;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
        다음에 또 만나요. 로그아웃 상태로 이용 맥락이 전환되었습니다.
      </p>
    </div>
  </div>
  <div slot="angular">
    <div
      style="max-width:480px;margin:0 auto;padding:48px 24px;text-align:center;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
    >
      <h3 style="margin:0 0 8px">로그아웃되었습니다</h3>
      <p style="margin:0;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
        다음에 또 만나요. 로그아웃 상태로 이용 맥락이 전환되었습니다.
      </p>
    </div>
  </div>

```tsx fw=react
export function Example() {
  return (
    <div
      style={{
        maxWidth: "480px",
        margin: "0 auto",
        padding: "48px 24px",
        textAlign: "center",
        border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        borderRadius: "8px",
      }}
    >
      <h3 style={{ margin: "0 0 8px" }}>로그아웃되었습니다</h3>
      <p style={{ margin: "0", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
        다음에 또 만나요. 로그아웃 상태로 이용 맥락이 전환되었습니다.
      </p>
    </div>
  );
}
```

```vue fw=vue
<template>
  <div
    style="max-width:480px;margin:0 auto;padding:48px 24px;text-align:center;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
  >
    <h3 style="margin:0 0 8px">로그아웃되었습니다</h3>
    <p style="margin:0;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
      다음에 또 만나요. 로그아웃 상태로 이용 맥락이 전환되었습니다.
    </p>
  </div>
</template>
```

```svelte fw=svelte
<div
  style="max-width:480px;margin:0 auto;padding:48px 24px;text-align:center;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
>
  <h3 style="margin:0 0 8px">로그아웃되었습니다</h3>
  <p style="margin:0;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
    다음에 또 만나요. 로그아웃 상태로 이용 맥락이 전환되었습니다.
  </p>
</div>
```

```tsx fw=solid
export function Example() {
  return (
    <div
      style={{
        maxWidth: "480px",
        margin: "0 auto",
        padding: "48px 24px",
        textAlign: "center",
        border: "1px solid var(--krds-mode-light-color-border-gray, #d9dbe1)",
        borderRadius: "8px",
      }}
    >
      <h3 style={{ margin: "0 0 8px" }}>로그아웃되었습니다</h3>
      <p style={{ margin: "0", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
        다음에 또 만나요. 로그아웃 상태로 이용 맥락이 전환되었습니다.
      </p>
    </div>
  );
}
```

```ts fw=angular
// template
<div
  style="max-width:480px;margin:0 auto;padding:48px 24px;text-align:center;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
>
  <h3 style="margin:0 0 8px">로그아웃되었습니다</h3>
  <p style="margin:0;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
    다음에 또 만나요. 로그아웃 상태로 이용 맥락이 전환되었습니다.
  </p>
</div>
```

```astro fw=astro
<div
  style="max-width:480px;margin:0 auto;padding:48px 24px;text-align:center;border:1px solid var(--krds-mode-light-color-border-gray,#d9dbe1);border-radius:8px"
>
  <h3 style="margin:0 0 8px">로그아웃되었습니다</h3>
  <p style="margin:0;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
    다음에 또 만나요. 로그아웃 상태로 이용 맥락이 전환되었습니다.
  </p>
</div>
```

</FrameworkPreview>

**피해야 할 사례**

<FrameworkPreview
  kind="bad"
  title="로그아웃에 대한 안내는 별도의 화면으로 구성하여 제공한다."
>
  <div slot="react">
    <ReactModal
      open="true"
      title="로그아웃되었습니다"
      description="로그아웃이 완료되었습니다."
      confirmLabel="확인"
      client:only="react"
    />
  </div>
  <div slot="vue">
    <VueModal
      open="true"
      title="로그아웃되었습니다"
      description="로그아웃이 완료되었습니다."
      confirmLabel="확인"
      client:only="vue"
    />
  </div>
  <div slot="svelte">
    <SvelteModal
      open="true"
      title="로그아웃되었습니다"
      description="로그아웃이 완료되었습니다."
      confirmLabel="확인"
      client:only="svelte"
    />
  </div>
  <div slot="solid">
    <SolidModal
      open="true"
      title="로그아웃되었습니다"
      description="로그아웃이 완료되었습니다."
      confirmLabel="확인"
      client:only="solid-js"
    />
  </div>
  <div slot="astro">
    <AstroModal
      open="true"
      title="로그아웃되었습니다"
      description="로그아웃이 완료되었습니다."
      confirmLabel="확인"
    />
  </div>
  <div slot="angular">
    <AngularModal
      open="true"
      title="로그아웃되었습니다"
      description="로그아웃이 완료되었습니다."
      confirmLabel="확인"
      client:only="@analogjs/astro-angular"
    />
  </div>

```tsx fw=react
import { Modal } from "@krds-community/react";

export function Example() {
  return (
    <Modal
      open="true"
      title="로그아웃되었습니다"
      description="로그아웃이 완료되었습니다."
      confirmLabel="확인"
    />
  );
}
```

```vue fw=vue
<script setup>
import { Modal } from "@krds-community/vue";
</script>

<template>
  <Modal
    open="true"
    title="로그아웃되었습니다"
    description="로그아웃이 완료되었습니다."
    confirmLabel="확인"
  />
</template>
```

```svelte fw=svelte
<script>
import { Modal } from '@krds-community/svelte';
</script>

<Modal
  open="true"
  title="로그아웃되었습니다"
  description="로그아웃이 완료되었습니다."
  confirmLabel="확인"
/>
```

```tsx fw=solid
import { Modal } from "@krds-community/solid";

export function Example() {
  return (
    <Modal
      open="true"
      title="로그아웃되었습니다"
      description="로그아웃이 완료되었습니다."
      confirmLabel="확인"
    />
  );
}
```

```ts fw=angular
import { KrdsModalComponent } from '@krds-community/angular';

// template
<krds-modal
  open="true"
  title="로그아웃되었습니다"
  description="로그아웃이 완료되었습니다."
  confirmLabel="확인"
></krds-modal>
```

```astro fw=astro
---
import { Modal } from "@krds-community/astro";
---

<Modal
  open="true"
  title="로그아웃되었습니다"
  description="로그아웃이 완료되었습니다."
  confirmLabel="확인"
/>
```

</FrameworkPreview>

**우수: 로그아웃 안내 화면에 사용자의 행동을 유도할 수 있는 액션 버튼을 제공한다.**
사용자가 기존에 이용 중이던 맥락으로 돌아갈 수 있도록 '다시 로그인하기' 버튼을 제공하고 로그인이 완료되었을 때 사용자가 탐색 중이던 화면으로 연결되어야 한다.

**모범 사례**

<FrameworkPreview
  kind="good"
  title="로그아웃 안내 화면에 사용자의 행동을 유도할 수 있는 액션 버튼을 제공한다."
>
  <div slot="react">
    <div
      style={{
        maxWidth: "480px",
        margin: "0 auto",
        padding: "48px 24px",
        textAlign: "center",
      }}
    >
      <h3 style={{ margin: "0 0 8px" }}>로그아웃되었습니다</h3>
      <p style={{ margin: "0 0 24px", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
        로그아웃이 완료되었습니다. 다시 로그인하면 이용 중이던 화면으로 돌아갈 수 있습니다.
      </p>
      <div style={{ display: "flex", justifyContent: "center", gap: "8px" }}>
        <ReactButton variant="primary" client:only="react">
          다시 로그인하기
        </ReactButton>
      </div>
    </div>
  </div>
  <div slot="vue">
    <div
      style="max-width:480px;margin:0 auto;padding:48px 24px;text-align:center"
    >
      <h3 style="margin:0 0 8px">로그아웃되었습니다</h3>
      <p style="margin:0 0 24px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
        로그아웃이 완료되었습니다. 다시 로그인하면 이용 중이던 화면으로 돌아갈 수 있습니다.
      </p>
      <div style="display:flex;justify-content:center;gap:8px">
        <VueButton variant="primary" client:only="vue">
          다시 로그인하기
        </VueButton>
      </div>
    </div>
  </div>
  <div slot="svelte">
    <div
      style="max-width:480px;margin:0 auto;padding:48px 24px;text-align:center"
    >
      <h3 style="margin:0 0 8px">로그아웃되었습니다</h3>
      <p style="margin:0 0 24px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
        로그아웃이 완료되었습니다. 다시 로그인하면 이용 중이던 화면으로 돌아갈 수 있습니다.
      </p>
      <div style="display:flex;justify-content:center;gap:8px">
        <SvelteButton variant="primary" client:only="svelte">
          다시 로그인하기
        </SvelteButton>
      </div>
    </div>
  </div>
  <div slot="solid">
    <div
      style={{
        maxWidth: "480px",
        margin: "0 auto",
        padding: "48px 24px",
        textAlign: "center",
      }}
    >
      <h3 style={{ margin: "0 0 8px" }}>로그아웃되었습니다</h3>
      <p style={{ margin: "0 0 24px", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
        로그아웃이 완료되었습니다. 다시 로그인하면 이용 중이던 화면으로 돌아갈 수 있습니다.
      </p>
      <div style={{ display: "flex", justifyContent: "center", gap: "8px" }}>
        <SolidButton variant="primary" client:only="solid-js">
          다시 로그인하기
        </SolidButton>
      </div>
    </div>
  </div>
  <div slot="astro">
    <div
      style="max-width:480px;margin:0 auto;padding:48px 24px;text-align:center"
    >
      <h3 style="margin:0 0 8px">로그아웃되었습니다</h3>
      <p style="margin:0 0 24px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
        로그아웃이 완료되었습니다. 다시 로그인하면 이용 중이던 화면으로 돌아갈 수 있습니다.
      </p>
      <div style="display:flex;justify-content:center;gap:8px">
        <AstroButton variant="primary">다시 로그인하기</AstroButton>
      </div>
    </div>
  </div>
  <div slot="angular">
    <div
      style="max-width:480px;margin:0 auto;padding:48px 24px;text-align:center"
    >
      <h3 style="margin:0 0 8px">로그아웃되었습니다</h3>
      <p style="margin:0 0 24px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
        로그아웃이 완료되었습니다. 다시 로그인하면 이용 중이던 화면으로 돌아갈 수 있습니다.
      </p>
      <div style="display:flex;justify-content:center;gap:8px">
        <AngularButton
          variant="primary"
          label="다시 로그인하기"
          client:only="@analogjs/astro-angular"
        >
          다시 로그인하기
        </AngularButton>
      </div>
    </div>
  </div>

```tsx fw=react
import { Button } from "@krds-community/react";

export function Example() {
  return (
    <div
      style={{
        maxWidth: "480px",
        margin: "0 auto",
        padding: "48px 24px",
        textAlign: "center",
      }}
    >
      <h3 style={{ margin: "0 0 8px" }}>로그아웃되었습니다</h3>
      <p style={{ margin: "0 0 24px", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
        로그아웃이 완료되었습니다. 다시 로그인하면 이용 중이던 화면으로 돌아갈 수 있습니다.
      </p>
      <div style={{ display: "flex", justifyContent: "center", gap: "8px" }}>
        <Button variant="primary">다시 로그인하기</Button>
      </div>
    </div>
  );
}
```

```vue fw=vue
<script setup>
import { Button } from "@krds-community/vue";
</script>

<template>
  <div style="max-width:480px;margin:0 auto;padding:48px 24px;text-align:center">
    <h3 style="margin:0 0 8px">로그아웃되었습니다</h3>
    <p style="margin:0 0 24px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
      로그아웃이 완료되었습니다. 다시 로그인하면 이용 중이던 화면으로 돌아갈 수 있습니다.
    </p>
    <div style="display:flex;justify-content:center;gap:8px">
      <Button variant="primary">다시 로그인하기</Button>
    </div>
  </div>
</template>
```

```svelte fw=svelte
<script>
import { Button } from '@krds-community/svelte';
</script>

<div
  style="max-width:480px;margin:0 auto;padding:48px 24px;text-align:center"
>
  <h3 style="margin:0 0 8px">로그아웃되었습니다</h3>
  <p style="margin:0 0 24px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
    로그아웃이 완료되었습니다. 다시 로그인하면 이용 중이던 화면으로 돌아갈 수 있습니다.
  </p>
  <div style="display:flex;justify-content:center;gap:8px">
    <Button variant="primary">다시 로그인하기</Button>
  </div>
</div>
```

```tsx fw=solid
import { Button } from "@krds-community/solid";

export function Example() {
  return (
    <div
      style={{
        maxWidth: "480px",
        margin: "0 auto",
        padding: "48px 24px",
        textAlign: "center",
      }}
    >
      <h3 style={{ margin: "0 0 8px" }}>로그아웃되었습니다</h3>
      <p style={{ margin: "0 0 24px", color: "var(--krds-mode-light-color-text-subtle, #6a6a6a)" }}>
        로그아웃이 완료되었습니다. 다시 로그인하면 이용 중이던 화면으로 돌아갈 수 있습니다.
      </p>
      <div style={{ display: "flex", justifyContent: "center", gap: "8px" }}>
        <Button variant="primary">다시 로그인하기</Button>
      </div>
    </div>
  );
}
```

```ts fw=angular
import { KrdsButtonComponent } from '@krds-community/angular';

// template
<div
  style="max-width:480px;margin:0 auto;padding:48px 24px;text-align:center"
>
  <h3 style="margin:0 0 8px">로그아웃되었습니다</h3>
  <p style="margin:0 0 24px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
    로그아웃이 완료되었습니다. 다시 로그인하면 이용 중이던 화면으로 돌아갈 수 있습니다.
  </p>
  <div style="display:flex;justify-content:center;gap:8px">
    <krds-button variant="primary" label="다시 로그인하기">다시 로그인하기</krds-button>
  </div>
</div>
```

```astro fw=astro
---
import { Button } from "@krds-community/astro";
---

<div
  style="max-width:480px;margin:0 auto;padding:48px 24px;text-align:center"
>
  <h3 style="margin:0 0 8px">로그아웃되었습니다</h3>
  <p style="margin:0 0 24px;color:var(--krds-mode-light-color-text-subtle,#6a6a6a)">
    로그아웃이 완료되었습니다. 다시 로그인하면 이용 중이던 화면으로 돌아갈 수 있습니다.
  </p>
  <div style="display:flex;justify-content:center;gap:8px">
    <Button variant="primary">다시 로그인하기</Button>
  </div>
</div>
```

</FrameworkPreview>
