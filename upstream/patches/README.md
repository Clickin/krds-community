# Upstream patches

Project-specific corrections are declarative, revision-scoped metadata. Never
modify `upstream/krds-html` to apply a correction. Each patch must identify the
upstream file, the accessibility or semantic issue, the implementation behavior,
and the conformance normalization rule.
